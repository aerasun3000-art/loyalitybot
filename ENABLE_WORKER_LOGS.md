# 🔧 Как включить логирование Worker в Cloudflare

**Проблема:** Бот не отвечает, и логи недоступны.

**Причина:** Workers Logs отключены в настройках Worker.

---

## ✅ Решение 1: Включить Workers Logs в Dashboard

1. Откройте Cloudflare Dashboard:
   - https://dash.cloudflare.com
   - Workers & Pages → `loyalitybot-partner-webhook` → **Settings**

2. Найдите секцию **"Observability"**

3. Нажмите на **"Workers Logs"** (где написано "Disabled")

4. Нажмите **"Enable"** или **"Edit"** для включения логирования

5. Выберите нужные настройки (рекомендуется оставить по умолчанию)

6. Сохраните изменения

7. Попробуйте снова отправить команду боту и проверьте логи

---

## ✅ Решение 2: Использовать Tail Worker (рекомендуется)

1. В Cloudflare Dashboard → Settings → Observability

2. Найдите **"Tail Worker"** (где написано "No Tail Workers connected")

3. Нажмите **"Connect"** рядом с Tail Worker

4. В терминале выполните:

```bash
cd /Users/ghbi/Downloads/loyalitybot/cloudflare/workers/partner-webhook
wrangler tail
```

5. Отправьте команду боту в другом терминале/приложении

6. В терминале с `wrangler tail` появятся логи в реальном времени

---

## 📋 После включения логирования

Проверьте логи при отправке команды боту. Вы должны увидеть:

```
[Worker] Request received: ...
[Worker] Processing POST request
[routeUpdate] Received update: ...
[handleCallback] Received callback: ...
```

Если логов нет - запросы не доходят до Worker (проблема с webhook).

Если логи есть, но есть ошибки - посмотрите, на каком этапе возникает проблема.

---

**Дата:** 2026-01-18
