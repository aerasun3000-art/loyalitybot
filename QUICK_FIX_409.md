# ⚡ Быстрое решение ошибки 409

## 🚨 Проблема повторяется?

**Ошибка 409 возникает, когда бот запущен в ДВУХ местах одновременно:**
- На Fly.io ✅ (правильно)
- Локально ❌ (конфликт!)

## ✅ Решение за 3 шага:

### 1. Остановите ВСЕ локальные боты:

```bash
pkill -f bot.py
pkill -f client_handler.py
pkill -f admin_bot.py
```

### 2. Проверьте, что остановлено:

```bash
./check_bot_status.sh
```

Должно показать: `✅ Локальные боты не запущены`

### 3. Перезапустите бот на Fly.io:

```bash
flyctl machine restart e822d71b412d08 --app loyalitybot-partner
```

## 📋 Правило на будущее:

**НИКОГДА не запускайте бот локально, если он работает на Fly.io!**

### ✅ Правильно:
- Используйте бот ТОЛЬКО на Fly.io
- Для проверки: `flyctl logs --app loyalitybot-partner`

### ❌ Неправильно:
- Запускать `python bot.py` локально
- Запускать `./start_beta.sh` когда бот на Fly.io

## 🔍 Проверка перед любым действием:

```bash
./check_bot_status.sh
```

Этот скрипт покажет:
- ✅ Локальные боты запущены или нет
- ✅ Статус ботов на Fly.io

## 📚 Подробная документация:

См. `AVOID_409_ERROR.md` для полной информации.
