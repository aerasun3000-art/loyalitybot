# 🚀 Оптимизация процесса деплоя и перезапуска ботов

## 📊 Текущая ситуация

**Проблемы:**
- ❌ Ручной перезапуск ботов через `nohup`
- ❌ Нет автоматизации деплоя
- ❌ Сложно отслеживать статус ботов
- ❌ Нет автоматического восстановления при падении

**Решение:**
- ✅ Автоматизированный скрипт деплоя
- ✅ Облачный хостинг с автоперезапуском
- ✅ Мониторинг и логирование
- ✅ CI/CD для автоматического деплоя

---

## 🎯 Рекомендуемые варианты

### Вариант 1: Fly.io (⭐ Рекомендуется)

**Преимущества:**
- ✅ **Бесплатный план**: 3 VM бесплатно
- ✅ **Не засыпает**: постоянная работа
- ✅ **Автоматический перезапуск** при падении
- ✅ **Простая настройка**: один файл `fly.toml`
- ✅ **Быстрый деплой**: `flyctl deploy`
- ✅ **Мониторинг**: встроенные метрики

**Стоимость:**
- Бесплатно: до 3 VM
- Платно: от $5/месяц за дополнительные ресурсы

**Быстрый старт:**
```bash
# 1. Установите flyctl
brew install flyctl  # macOS
# или
curl -L https://fly.io/install.sh | sh

# 2. Войдите
flyctl auth login

# 3. Инициализируйте проект
flyctl launch

# 4. Настройте переменные окружения
flyctl secrets set SUPABASE_URL=...
flyctl secrets set SUPABASE_KEY=...
# ... остальные переменные

# 5. Задеплойте
./deploy.sh fly
```

**Конфигурация для 3 ботов:**
Создайте 3 отдельных приложения на Fly.io:
- `loyalitybot-admin` - админ-бот
- `loyalitybot-partner` - партнерский бот
- `loyalitybot-client` - клиентский бот

---

### Вариант 2: Render

**Преимущества:**
- ✅ **Бесплатный план** для веб-сервисов
- ✅ **Автоматический деплой** из GitHub
- ✅ **HTTPS из коробки**
- ⚠️ **Засыпает** после 15 минут бездействия (но просыпается)

**Стоимость:**
- Бесплатно: с ограничениями
- Starter: $7/месяц (без засыпания)

**Настройка:**
1. Создайте 3 отдельных Web Service на Render
2. Подключите GitHub репозиторий
3. Настройте:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python admin_bot.py` (для каждого бота свой)

---

### Вариант 3: Railway

**Преимущества:**
- ✅ **Простая настройка**
- ✅ **Автоматический деплой** из GitHub
- ✅ **Мониторинг** встроен

**Стоимость:**
- Hobby: $5/месяц
- Pro: $20/месяц

**Настройка:**
1. Создайте 3 отдельных проекта на Railway
2. Подключите GitHub
3. Настройте переменные окружения
4. Railway автоматически задеплоит

---

### Вариант 4: VPS + Systemd (для полного контроля)

**Преимущества:**
- ✅ **Полный контроль** над сервером
- ✅ **Нет ограничений** облачных платформ
- ✅ **Дешевле** при масштабировании

**Стоимость:**
- DigitalOcean: от $6/месяц
- Hetzner: от €4/месяц
- AWS Lightsail: от $3.50/месяц

**Настройка:**
```bash
# 1. Установите systemd сервисы
sudo cp systemd-services/*.service /etc/systemd/system/
sudo systemctl daemon-reload

# 2. Включите автозапуск
sudo systemctl enable loyalitybot-admin
sudo systemctl enable loyalitybot-client
sudo systemctl enable loyalitybot-partner

# 3. Запустите
sudo systemctl start loyalitybot-admin
sudo systemctl start loyalitybot-client
sudo systemctl start loyalitybot-partner

# 4. Используйте скрипт для перезапуска
./deploy.sh systemd
```

---

## 🛠️ Использование скрипта деплоя

### Локальный деплой (для разработки)
```bash
chmod +x deploy.sh
./deploy.sh local
```

### Systemd деплой (VPS)
```bash
./deploy.sh systemd
```

### Docker деплой
```bash
./deploy.sh docker
```

### Fly.io деплой
```bash
./deploy.sh fly
```

### Render деплой (через Git)
```bash
./deploy.sh render
```

---

## 🔄 Автоматизация через CI/CD

### GitHub Actions

Создайте `.github/workflows/deploy.yml`:

```yaml
name: Deploy Bots

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Deploy to Fly.io
        uses: superfly/flyctl-actions/setup-flyctl@master
      
      - name: Deploy Admin Bot
        run: |
          flyctl deploy --app loyalitybot-admin
      
      - name: Deploy Partner Bot
        run: |
          flyctl deploy --app loyalitybot-partner
      
      - name: Deploy Client Bot
        run: |
          flyctl deploy --app loyalitybot-client
```

---

## 📊 Сравнение вариантов

| Критерий | Fly.io | Render | Railway | VPS + Systemd |
|----------|--------|--------|---------|---------------|
| **Бесплатный план** | ✅ 3 VM | ✅ С ограничениями | ❌ $5/месяц | ❌ От $3/месяц |
| **Автоперезапуск** | ✅ | ✅ | ✅ | ✅ (systemd) |
| **Не засыпает** | ✅ | ❌ | ✅ | ✅ |
| **Простота настройки** | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐ |
| **Мониторинг** | ✅ | ✅ | ✅ | ⚠️ Нужна настройка |
| **Масштабируемость** | ✅ | ✅ | ✅ | ⚠️ Ручная настройка |

---

## 🎯 Рекомендация

**Для начала:** Fly.io (бесплатно, просто, надёжно)

**Для продакшена:** 
1. **Fly.io** - если нужна простота и автоматизация
2. **VPS + Systemd** - если нужен полный контроль и оптимизация затрат

---

## 📝 Чеклист миграции на облако

- [ ] Выбрать платформу (Fly.io рекомендуется)
- [ ] Создать аккаунт
- [ ] Установить CLI инструменты
- [ ] Настроить переменные окружения
- [ ] Создать 3 отдельных приложения (по одному на бота)
- [ ] Задеплоить первый бот
- [ ] Проверить работу
- [ ] Задеплоить остальные боты
- [ ] Настроить мониторинг
- [ ] Настроить CI/CD (опционально)
- [ ] Остановить локальные боты

---

## 🚨 Важные замечания

1. **Telegram Bot API**: Убедитесь, что только один экземпляр каждого бота работает одновременно
2. **Переменные окружения**: Храните секреты в безопасном месте (secrets management)
3. **Логирование**: Настройте централизованное логирование (Sentry уже интегрирован)
4. **Мониторинг**: Настройте алерты при падении ботов
5. **Бэкапы**: Регулярно делайте бэкапы базы данных (Supabase делает автоматически)

---

## 💡 Дополнительные оптимизации

### 1. Healthchecks
Добавьте healthcheck endpoints для мониторинга:
```python
# В каждом боте
@app.get("/health")
async def health():
    return {"status": "ok", "bot": "admin"}
```

### 2. Graceful shutdown
Обрабатывайте сигналы для корректного завершения:
```python
import signal
import asyncio

def signal_handler(sig, frame):
    logger.info("Получен сигнал завершения, останавливаем бота...")
    # Сохранить состояние, закрыть соединения
    asyncio.get_event_loop().stop()
```

### 3. Автоматический перезапуск при ошибках
Используйте `restart: unless-stopped` в Docker или systemd `Restart=always`

---

**Готовы начать?** Запустите `./deploy.sh fly` для деплоя на Fly.io! 🚀

