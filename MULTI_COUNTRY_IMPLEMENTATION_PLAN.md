# 🌍 ПОШАГОВЫЙ ПЛАН ПОДКЛЮЧЕНИЯ ПАРТНЕРОВ ИЗ НЕСКОЛЬКИХ СТРАН

**Дата:** Декабрь 2025  
**Цель:** Поддержка партнеров из разных стран с разными валютами и языками  
**Статус:** План реализации

---

## 📋 ОГЛАВЛЕНИЕ

1. [Этап 1: Подготовка и планирование](#этап-1-подготовка-и-планирование)
2. [Этап 2: Мультивалютность в БД](#этап-2-мультивалютность-в-бд)
3. [Этап 3: Мультивалютность в коде](#этап-3-мультивалютность-в-коде)
4. [Этап 4: Интернационализация (i18n)](#этап-4-интернационализация-i18n)
5. [Этап 5: Адаптация расчетов вознаграждений](#этап-5-адаптация-расчетов-вознаграждений)
6. [Этап 6: Обновление интерфейса](#этап-6-обновление-интерфейса)
7. [Этап 7: Миграция данных](#этап-7-миграция-данных)
8. [Этап 8: Тестирование](#этап-8-тестирование)
9. [Этап 9: Документация и обучение](#этап-9-документация-и-обучение)

---

## ЭТАП 1: ПОДГОТОВКА И ПЛАНИРОВАНИЕ

### Шаг 1.1: Определить поддерживаемые страны и валюты

**Файлы для создания:** `CURRENCIES_CONFIG.md`

**Действия:**

1. Создать список стран и валют:
   ```markdown
   ## Поддерживаемые валюты (обновлено)
   
   - **США** (Нью-Йорк, Лос-Анджелес, etc.): **USD** ($) - Английский (en)
   - **Россия** (Москва, СПб, etc.): **RUB** (₽) - Русский (ru)
   - **Вьетнам** (Нячанг): **VND** (₫) - Вьетнамский (vi)
   - **Казахстан** (Алматы, Астана): **KZT** (₸) - Казахский (kk)
   - **Киргизия** (Бишкек, Ош): **KGS** (сом) - Киргизский (ky)
   - **ОАЭ** (Дубай): **AED** (د.إ) - Арабский (ar) + Английский (en)
   ```

2. Определить список языков:
   - **Английский** (en) - для США, ОАЭ (основной)
   - **Русский** (ru) - для России, Казахстана, Киргизии
   - **Вьетнамский** (vi) - для Вьетнама
   - **Казахский** (kk) - для Казахстана
   - **Киргизский** (ky) - для Киргизии
   - **Арабский** (ar) - для ОАЭ (Дубай)

**Время:** 1-2 часа  
**Результат:** Документ с конфигурацией валют и языков

---

### Шаг 1.2: Определить базовую валюту для расчетов

**Решение:** USD (доллар США)

**Обоснование:**
- Стандартная валюта для международных расчетов
- Стабильность курса
- Легкость конвертации

**Документ:** Обновить `MULTI_CURRENCY_REWARD_SOLUTION.md`

---

### Шаг 1.3: Подготовить список задач (TODO)

**Файл:** Создать проект в Linear/GitHub Issues или TODO список

**Задачи:**
- [ ] Миграция БД для мультивалютности
- [ ] Добавление i18n во фронтенд
- [ ] Обновление расчетов вознаграждений
- [ ] Тестирование с разными валютами
- [ ] Документация

---

## ЭТАП 2: МУЛЬТИВАЛЮТНОСТЬ В БД

### Шаг 2.1: Создать миграцию для добавления поля `currency` в `transactions`

**Файл:** `migrations/add_currency_to_transactions.sql`

**Код:**
```sql
-- ============================================
-- Миграция: Добавление поля currency в transactions
-- Дата: 2025-12-XX
-- ============================================

-- 1. Добавить поле currency
ALTER TABLE transactions 
ADD COLUMN IF NOT EXISTS currency TEXT DEFAULT 'USD';

-- 2. Создать индекс для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_transactions_currency 
ON transactions(currency);

-- 3. Добавить комментарий
COMMENT ON COLUMN transactions.currency IS 'Валюта транзакции (USD, VND, RUB, EUR, GBP, AED)';

-- 4. Проверка: посмотреть структуру таблицы
SELECT column_name, data_type, column_default 
FROM information_schema.columns 
WHERE table_name = 'transactions' AND column_name = 'currency';
```

**Время:** 30 минут  
**Тестирование:**
- [ ] Выполнить миграцию в тестовой БД
- [ ] Проверить, что поле добавлено
- [ ] Проверить индексы

---

### Шаг 2.2: Создать таблицу курсов валют

**Файл:** `migrations/create_currency_exchange_rates.sql`

**Код:**
```sql
-- ============================================
-- Миграция: Создание таблицы курсов валют
-- Дата: 2025-12-XX
-- ============================================

CREATE TABLE IF NOT EXISTS currency_exchange_rates (
    id SERIAL PRIMARY KEY,
    from_currency TEXT NOT NULL,
    to_currency TEXT NOT NULL DEFAULT 'USD',
    rate NUMERIC(18, 8) NOT NULL,
    source TEXT DEFAULT 'manual',
    effective_from TIMESTAMP NOT NULL DEFAULT NOW(),
    effective_until TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    
    UNIQUE(from_currency, to_currency, effective_from),
    
    CONSTRAINT valid_currencies CHECK (
        from_currency IN ('USD', 'VND', 'RUB', 'KZT', 'KGS', 'AED') AND
        to_currency IN ('USD', 'VND', 'RUB', 'KZT', 'KGS', 'AED')
    )
);

-- Индексы
CREATE INDEX IF NOT EXISTS idx_currency_rates_lookup 
ON currency_exchange_rates(from_currency, to_currency, effective_from DESC);

CREATE INDEX IF NOT EXISTS idx_currency_rates_effective 
ON currency_exchange_rates(effective_from, effective_until);

-- Комментарии
COMMENT ON TABLE currency_exchange_rates IS 'Курсы обмена валют для конвертации транзакций';
COMMENT ON COLUMN currency_exchange_rates.rate IS 'Курс: 1 from_currency = rate to_currency';
COMMENT ON COLUMN currency_exchange_rates.source IS 'Источник курса: manual, api, etc.';
```

**Время:** 1 час  
**Тестирование:**
- [ ] Создать таблицу
- [ ] Проверить ограничения
- [ ] Проверить индексы

---

### Шаг 2.3: Заполнить начальные курсы валют

**Файл:** `migrations/insert_initial_exchange_rates.sql`

**Код:**
```sql
-- ============================================
-- Вставка начальных курсов валют
-- Курсы на декабрь 2025 (примерные)
-- ============================================

INSERT INTO currency_exchange_rates (from_currency, to_currency, rate, source, effective_from) VALUES
-- VND → USD (1 VND = 0.0000408 USD, т.е. 24,500 VND = 1 USD)
('VND', 'USD', 0.0000408, 'manual', NOW()),

-- RUB → USD (1 RUB = 0.011 USD, т.е. ~91 RUB = 1 USD)
('RUB', 'USD', 0.011, 'manual', NOW()),

-- KZT → USD (1 KZT = 0.0021 USD, т.е. ~476 KZT = 1 USD)
('KZT', 'USD', 0.0021, 'manual', NOW()),

-- KGS → USD (1 KGS = 0.011 USD, т.е. ~91 KGS = 1 USD)
('KGS', 'USD', 0.011, 'manual', NOW()),

-- AED → USD (1 AED = 0.272 USD, т.е. ~3.67 AED = 1 USD)
('AED', 'USD', 0.272, 'manual', NOW())

ON CONFLICT (from_currency, to_currency, effective_from) DO NOTHING;

-- Обратные курсы (USD → другие валюты)
INSERT INTO currency_exchange_rates (from_currency, to_currency, rate, source, effective_from) VALUES
-- USD → VND (1 USD = 24,500 VND)
('USD', 'VND', 24500, 'manual', NOW()),

-- USD → RUB (1 USD = ~91 RUB)
('USD', 'RUB', 91, 'manual', NOW()),

-- USD → KZT (1 USD = ~476 KZT)
('USD', 'KZT', 476, 'manual', NOW()),

-- USD → KGS (1 USD = ~91 KGS)
('USD', 'KGS', 91, 'manual', NOW()),

-- USD → AED (1 USD = ~3.67 AED)
('USD', 'AED', 3.67, 'manual', NOW())
ON CONFLICT DO NOTHING;
```

**Время:** 30 минут  
**Тестирование:**
- [ ] Проверить вставку данных
- [ ] Проверить уникальность
- [ ] Запросить несколько курсов

---

### Шаг 2.4: Обновить поле `city` в таблице `partners` (если нужно)

**Файл:** Проверить `setup_partners_table.sql`

**Действия:**
- Убедиться, что поле `city` существует
- Добавить города новых стран (если нужно)

**Время:** 30 минут

---

## ЭТАП 3: МУЛЬТИВАЛЮТНОСТЬ В КОДЕ

### Шаг 3.1: Обновить `currency_utils.py` - добавить все валюты

**Файл:** `currency_utils.py`

**Действия:**

1. Добавить города в `CITY_TO_CURRENCY`:
```python
# Вьетнам
'Nha Trang': 'VND',

# Казахстан
'Almaty': 'KZT',
'Astana': 'KZT',
'Алматы': 'KZT',
'Астана': 'KZT',

# Киргизия
'Bishkek': 'KGS',
'Osh': 'KGS',
'Бишкек': 'KGS',
'Ош': 'KGS',

# ОАЭ
'Dubai': 'AED',
'Дубай': 'AED',
```

2. Добавить все валюты в `CURRENCY_SYMBOLS`:
```python
'VND': '₫',
'KZT': '₸',
'KGS': 'сом',
'AED': 'د.إ',
```

3. Обновить DEFAULT_RATES для конвертации:
```python
DEFAULT_RATES = {
    'VND_USD': 0.0000408,  # 24,500 VND = 1 USD
    'RUB_USD': 0.011,      # ~91 RUB = 1 USD
    'KZT_USD': 0.0021,     # ~476 KZT = 1 USD
    'KGS_USD': 0.011,      # ~91 KGS = 1 USD
    'AED_USD': 0.272,      # ~3.67 AED = 1 USD
}
```

**Время:** 30 минут  
**Тестирование:**
- [ ] Проверить `get_currency_by_city('Nha Trang')` → `'VND'`
- [ ] Проверить `get_currency_symbol('VND')` → `'₫'`

---

### Шаг 3.2: Добавить функции конвертации валют в `currency_utils.py`

**Файл:** `currency_utils.py`

**Код:**
```python
def get_exchange_rate(from_currency: str, to_currency: str = 'USD', 
                     date: Optional[datetime] = None) -> float:
    """
    Получает курс обмена валют из БД
    
    Args:
        from_currency: Исходная валюта (VND, RUB, etc.)
        to_currency: Целевая валюта (по умолчанию USD)
        date: Дата для получения исторического курса
    
    Returns:
        float: Курс обмена
    """
    # TODO: Реализовать запрос к БД
    # Использовать DEFAULT_RATES как fallback
    pass

def convert_currency(amount: float, from_currency: str, 
                    to_currency: str = 'USD',
                    date: Optional[datetime] = None) -> float:
    """
    Конвертирует сумму из одной валюты в другую
    
    Args:
        amount: Сумма в исходной валюте
        from_currency: Исходная валюта
        to_currency: Целевая валюта (по умолчанию USD)
        date: Дата для исторической конвертации
    
    Returns:
        float: Сумма в целевой валюте
    """
    # TODO: Реализовать конвертацию
    pass
```

**Время:** 2-3 часа  
**Тестирование:**
- [ ] Тесты для `get_exchange_rate()`
- [ ] Тесты для `convert_currency()`
- [ ] Проверить округление

---

### Шаг 3.3: Обновить `record_transaction()` для сохранения валюты

**Файл:** `supabase_manager.py`

**Код:**
```python
def record_transaction(self, client_chat_id: int, partner_chat_id: int, 
                       points: int, transaction_type: str, 
                       description: str, raw_amount: float = 0.00) -> bool:
    """Записывает транзакцию в таблицу 'transactions'."""
    
    # ✅ Получаем валюту партнера
    try:
        partner_city = self.get_partner_city(partner_chat_id)
        from currency_utils import get_currency_by_city
        currency = get_currency_by_city(partner_city) if partner_city else 'USD'
    except Exception as e:
        logging.warning(f"Не удалось определить валюту для партнера {partner_chat_id}: {e}")
        currency = 'USD'
    
    try:
        data = {
            "client_chat_id": str(client_chat_id),
            "partner_chat_id": str(partner_chat_id),
            "date_time": datetime.datetime.now().isoformat(),
            "total_amount": raw_amount,
            "currency": currency,  # ✅ Добавлено поле currency
            "earned_points": earned,
            "spent_points": spent,
            "operation_type": transaction_type,
            "description": description,
        }
        self.client.from_(TRANSACTION_TABLE).insert(data).execute()
        return True
    except Exception as e:
        logging.error(f"Error recording transaction: {e}")
        return False
```

**Время:** 1 час  
**Тестирование:**
- [ ] Создать транзакцию с партнером из Нячанга → проверить `currency = 'VND'`
- [ ] Создать транзакцию с партнером из NY → проверить `currency = 'USD'`

---

### Шаг 3.4: Обновить фронтенд `currency.js` - добавить VND

**Файл:** `frontend/src/utils/currency.js`

**Действия:**

1. Добавить "Nha Trang" в `CITY_TO_CURRENCY`:
```javascript
'Nha Trang': 'VND',
```

2. Добавить VND в `CURRENCY_SYMBOLS`:
```javascript
'VND': '₫',
```

3. Добавить VND в `CURRENCY_LOCALES`:
```javascript
'VND': 'vi-VN',
```

4. Обновить форматирование для VND (без десятичных знаков):
```javascript
export const formatCurrency = (value, city = null, currency = null) => {
  const finalCurrency = currency || getCurrencyByCity(city)
  const locale = getCurrencyLocale(finalCurrency)
  
  // Специальная обработка для VND (без десятичных знаков)
  const options = {
    style: 'currency',
    currency: finalCurrency,
    minimumFractionDigits: 0,
    maximumFractionDigits: 0
  }
  
  return new Intl.NumberFormat(locale, options).format(value)
}
```

**Время:** 1 час  
**Тестирование:**
- [ ] `formatCurrency(500000, 'Nha Trang')` → `"₫500,000"` или `"500.000₫"`
- [ ] Проверить форматирование в браузере

---

## ЭТАП 4: ИНТЕРНАЦИОНАЛИЗАЦИЯ (I18N)

### Шаг 4.1: Проверить текущую систему i18n

**Файлы для проверки:**
- `frontend/src/utils/i18n.js`
- `frontend/src/store/languageStore.js`

**Действия:**
- Посмотреть, какие языки уже поддерживаются
- Определить, нужно ли добавлять новые

**Время:** 30 минут

---

### Шаг 4.2: Добавить переводы для всех необходимых языков

**Файлы для создания:**
- `frontend/src/locales/en.json` (английский - обновить)
- `frontend/src/locales/ru.json` (русский - обновить)
- `frontend/src/locales/vi.json` (вьетнамский - создать)
- `frontend/src/locales/kk.json` (казахский - создать)
- `frontend/src/locales/ky.json` (киргизский - создать)
- `frontend/src/locales/ar.json` (арабский - создать)

**Пример структуры:**
```json
{
  "common": {
    "currency": "Валюта",
    "amount": "Сумма",
    "balance": "Баланс"
  },
  "partner": {
    "analytics": "Аналитика",
    "revenue": "Доход"
  }
}
```

**Время:** 8-12 часов (в зависимости от объема переводов)

**Приоритеты переводов:**
1. Ключевые слова интерфейса (en, ru, vi) - 4-6 часов
2. Казахский и киргизский (kk, ky) - 2-3 часа
3. Арабский (ar) - 2-3 часа

**Рекомендация:** Начать с основных страниц (Home, Services, Analytics), затем расширять

---

### Шаг 4.3: Добавить переключатель языка в интерфейс партнера

**Файл:** `frontend/src/components/LanguageSwitcher.jsx` (если есть)

**Действия:**
- Проверить, есть ли уже компонент
- Если нет - создать
- Добавить поддержку всех языков

**Время:** 1-2 часа

---

## ЭТАП 5: АДАПТАЦИЯ РАСЧЕТОВ ВОЗНАГРАЖДЕНИЙ

### Шаг 5.1: Обновить `_get_system_revenue()` с конвертацией валют

**Файл:** `partner_revenue_share.py`

**Код:**
```python
def _get_system_revenue(
    self,
    partner_chat_id: str,
    period_start: date,
    period_end: date
) -> float:
    """
    Получает доход системы с партнера за период (в USD)
    ✅ ВСЕ СУММЫ КОНВЕРТИРУЮТСЯ В USD
    """
    try:
        # Получаем PV партнера
        partner_data = self.db.client.table('partners').select(
            'pv_percent, city'
        ).eq('chat_id', partner_chat_id).single().execute()
        
        if not partner_data.data:
            return 0.0
        
        pv_percent = float(partner_data.data.get('pv_percent', 10.0))
        
        # ✅ Получаем транзакции с валютами
        transactions = self.db.client.table('transactions').select(
            'total_amount, currency, date_time'
        ).eq('partner_chat_id', partner_chat_id).gte(
            'date_time', period_start.isoformat()
        ).lte('date_time', period_end.isoformat()).execute()
        
        total_turnover_usd = 0.0
        
        # ✅ Конвертируем каждую транзакцию в USD
        from currency_utils import convert_currency
        from datetime import datetime
        
        for txn in transactions.data:
            amount = float(txn.get('total_amount', 0))
            currency = txn.get('currency', 'USD')
            txn_date = datetime.fromisoformat(txn['date_time'].replace('Z', '+00:00'))
            
            # Конвертируем в USD
            amount_usd = convert_currency(
                amount, 
                from_currency=currency, 
                to_currency='USD',
                date=txn_date
            )
            
            total_turnover_usd += amount_usd
        
        # Доход системы = Оборот (в USD) × PV%
        system_revenue_usd = total_turnover_usd * (pv_percent / 100.0)
        
        return round(system_revenue_usd, 2)
        
    except Exception as e:
        logger.error(f"Ошибка при получении дохода системы: {e}")
        return 0.0
```

**Время:** 2-3 часа  
**Тестирование:**
- [ ] Тест с транзакциями в VND
- [ ] Тест с транзакциями в USD
- [ ] Тест с транзакциями в разных валютах

---

### Шаг 5.2: Обновить `process_referral_transaction_bonuses()` с конвертацией

**Файл:** `supabase_manager.py`

**Код:**
```python
def process_referral_transaction_bonuses(
    self, 
    user_chat_id: str, 
    earned_points: int, 
    transaction_id: int = None,
    raw_amount: Optional[float] = None, 
    seller_partner_id: Optional[str] = None
) -> bool:
    """
    Обрабатывает реферальные бонусы с транзакции
    ✅ С учетом конвертации валют
    """
    
    if REFERRAL_CALCULATOR_AVAILABLE and raw_amount and seller_partner_id:
        try:
            # ✅ Получаем валюту транзакции
            txn_data = self.client.table('transactions').select(
                'currency, date_time'
            ).eq('id', transaction_id).single().execute()
            
            currency = txn_data.data.get('currency', 'USD') if txn_data.data else 'USD'
            txn_date = datetime.fromisoformat(
                txn_data.data['date_time'].replace('Z', '+00:00')
            ) if txn_data.data else datetime.now()
            
            # ✅ Конвертируем сумму в USD для расчета комиссий
            from currency_utils import convert_currency
            
            raw_amount_usd = convert_currency(
                raw_amount,
                from_currency=currency,
                to_currency='USD',
                date=txn_date
            )
            
            # ... остальная логика с использованием raw_amount_usd ...
            
        except Exception as e:
            logging.error(f"Error in referral commission calculation: {e}")
            # Fallback на старую логику
    
    # ...
```

**Время:** 2-3 часа  
**Тестирование:**
- [ ] Тест реферальных комиссий с VND транзакциями
- [ ] Тест с USD транзакциями
- [ ] Проверить правильность расчетов

---

## ЭТАП 6: ОБНОВЛЕНИЕ ИНТЕРФЕЙСА

### Шаг 6.1: Обновить компоненты для отображения валют

**Файлы:**
- `frontend/src/pages/PartnerAnalytics.jsx`
- `frontend/src/pages/Analytics.jsx`
- `frontend/src/pages/AdminAnalytics.jsx`

**Действия:**
- Убедиться, что используется `formatCurrency(value, partnerCity)`
- Проверить, что все суммы форматируются с учетом валюты

**Время:** 2-3 часа  
**Тестирование:**
- [ ] Проверить отображение VND сумм
- [ ] Проверить отображение USD сумм
- [ ] Проверить на разных устройствах

---

### Шаг 6.2: Обновить форму регистрации партнера (добавить города новых стран)

**Файл:** `frontend/src/utils/locations.js`

**Действия:**

1. Добавить города Казахстана в `PARTNER_CITIES`:
```javascript
{
  name: 'Almaty',
  label: 'Almaty / Алматы',
  districts: ['All', 'Центральный', 'Алмалинский', 'Медеуский', 'Турксибский', 'Жетысуский', 'Ауэзовский', 'Бостандыкский']
},
{
  name: 'Astana',
  label: 'Astana / Астана',
  districts: ['All', 'Алматинский', 'Есильский', 'Сарыаркинский', 'Байконур']
},
```

2. Добавить города Киргизии:
```javascript
{
  name: 'Bishkek',
  label: 'Bishkek / Бишкек',
  districts: ['All', 'Ленинский', 'Октябрьский', 'Первомайский', 'Свердловский']
},
{
  name: 'Osh',
  label: 'Osh / Ош',
  districts: ['All']
},
```

3. Убедиться, что "Dubai" уже есть (или добавить):
```javascript
{
  name: 'Dubai',
  label: 'Dubai / Дубай',
  districts: ['All', 'Downtown', 'Marina', 'Business Bay', 'JBR', 'Palm Jumeirah']
},
```

**Время:** 2-3 часа  
**Тестирование:**
- [ ] Проверить, что города отображаются в форме
- [ ] Проверить сохранение города в БД
- [ ] Проверить определение валюты по городу

---

### Шаг 6.3: Добавить отображение валюты в статистике партнера

**Файл:** `frontend/src/pages/PartnerAnalytics.jsx`

**Код:**
```javascript
// Уже должно работать через formatCurrency, но проверить:
const formatCurrencyValue = (value) => {
  return formatCurrency(value, partnerCity)
}
```

**Время:** 30 минут

---

## ЭТАП 7: МИГРАЦИЯ ДАННЫХ

### Шаг 7.1: Создать скрипт для обновления существующих транзакций

**Файл:** `migrations/update_existing_transactions_currency.sql`

**Код:**
```sql
-- ============================================
-- Обновление валюты для существующих транзакций
-- ============================================

-- Обновить валюту на основе города партнера
UPDATE transactions t
SET currency = (
    SELECT 
        CASE 
            WHEN p.city = 'Nha Trang' THEN 'VND'
            WHEN p.city IN ('Москва', 'Санкт-Петербург', 'Новосибирск', 
                           'Екатеринбург', 'Казань', 'Нижний Новгород') THEN 'RUB'
            WHEN p.city IN ('Almaty', 'Astana', 'Алматы', 'Астана') THEN 'KZT'
            WHEN p.city IN ('Bishkek', 'Osh', 'Бишкек', 'Ош') THEN 'KGS'
            WHEN p.city IN ('Dubai', 'Дубай') THEN 'AED'
            WHEN p.city IN ('Nha Trang') THEN 'VND'
            WHEN p.city IN ('New York', 'Los Angeles', 'Bay Area', 'Chicago', 
                           'Miami', 'Boston', 'Seattle', 'San Francisco', 
                           'Washington', 'Dallas', 'Houston', 'Atlanta', 
                           'Phoenix', 'Detroit', 'Philadelphia') THEN 'USD'
            WHEN p.city IN ('Online', 'Все', 'All') THEN 'USD'
            ELSE 'USD'  -- По умолчанию USD
        END
    FROM partners p
    WHERE p.chat_id = t.partner_chat_id
)
WHERE t.currency IS NULL OR t.currency = 'USD';

-- Проверка результатов
SELECT currency, COUNT(*) as count
FROM transactions
GROUP BY currency
ORDER BY count DESC;
```

**Время:** 1 час  
**Тестирование:**
- [ ] Выполнить на тестовой БД
- [ ] Проверить результаты обновления
- [ ] Проверить, что нет NULL значений

---

### Шаг 7.2: Валидация данных после миграции

**Файл:** `migrations/validate_currency_migration.sql`

**Код:**
```sql
-- Проверка миграции валют
-- 1. Нет NULL значений
SELECT COUNT(*) as null_currency_count
FROM transactions
WHERE currency IS NULL;

-- 2. Все транзакции имеют корректную валюту
SELECT DISTINCT currency
FROM transactions
ORDER BY currency;

-- 3. Соответствие городов и валют
SELECT 
    p.city,
    t.currency,
    COUNT(*) as count
FROM transactions t
JOIN partners p ON p.chat_id = t.partner_chat_id
GROUP BY p.city, t.currency
ORDER BY p.city, t.currency;
```

**Время:** 30 минут

---

## ЭТАП 8: ТЕСТИРОВАНИЕ

### Шаг 8.1: Модульные тесты для функций конвертации валют

**Файл:** `tests/test_currency_utils.py`

**Код:**
```python
import unittest
from currency_utils import convert_currency, get_exchange_rate

class TestCurrencyConversion(unittest.TestCase):
    
    def test_vnd_to_usd(self):
        # 500,000 VND should be approximately $20.40
        result = convert_currency(500000, 'VND', 'USD')
        self.assertAlmostEqual(result, 20.40, places=2)
    
    def test_kzt_to_usd(self):
        # 10,000 KZT should be approximately $21
        result = convert_currency(10000, 'KZT', 'USD')
        self.assertAlmostEqual(result, 21.0, places=1)
    
    def test_kgs_to_usd(self):
        # 1,000 KGS should be approximately $11
        result = convert_currency(1000, 'KGS', 'USD')
        self.assertAlmostEqual(result, 11.0, places=1)
    
    def test_aed_to_usd(self):
        # 100 AED should be approximately $27.20
        result = convert_currency(100, 'AED', 'USD')
        self.assertAlmostEqual(result, 27.20, places=2)
    
    def test_same_currency(self):
        result = convert_currency(100, 'USD', 'USD')
        self.assertEqual(result, 100)
```

**Время:** 2 часа

---

### Шаг 8.2: Интеграционные тесты для транзакций

**Файл:** `tests/test_multi_currency_transactions.py`

**Сценарии:**
1. Создать транзакцию с партнером из Нячанга → проверить `currency = 'VND'`
2. Создать транзакцию с партнером из NY → проверить `currency = 'USD'`
3. Рассчитать Revenue Share с транзакциями в разных валютах

**Время:** 3-4 часа

---

### Шаг 8.3: Ручное тестирование в интерфейсе

**Чеклист:**
- [ ] Зарегистрировать партнера из Нячанга
- [ ] Создать транзакцию → проверить отображение суммы в VND
- [ ] Открыть аналитику партнера → проверить форматирование сумм
- [ ] Проверить расчет Revenue Share
- [ ] Проверить переключение языков (если добавлено)

**Время:** 2-3 часа

---

## ЭТАП 9: ДОКУМЕНТАЦИЯ И ОБУЧЕНИЕ

### Шаг 9.1: Обновить документацию для разработчиков

**Файлы:**
- `README.md` - добавить раздел про мультивалютность
- `MULTI_CURRENCY_REWARD_SOLUTION.md` - уже создан
- `NHATRANG_ADAPTATION_TZ.md` - уже создан

**Действия:**
- Добавить примеры использования функций конвертации
- Описать процесс обновления курсов валют

**Время:** 2-3 часа

---

### Шаг 9.2: Создать руководство для партнеров (многоязычное)

**Файлы:**
- `PARTNER_GUIDE_MULTI_CURRENCY.md` (английский)
- `PARTNER_GUIDE_MULTI_CURRENCY_VI.md` (вьетнамский)
- `PARTNER_GUIDE_MULTI_CURRENCY_RU.md` (русский)

**Содержание:**
- Как работает система валют
- Как понимать цены и суммы
- Примеры расчетов

**Время:** 3-4 часа

---

### Шаг 9.3: Создать инструкцию по обновлению курсов валют

**Файл:** `CURRENCY_RATES_UPDATE_GUIDE.md`

**Содержание:**
- Как обновлять курсы вручную
- Как настроить автоматическое обновление (если планируется)
- Частота обновления

**Время:** 1-2 часа

---

## 📊 ОБЩАЯ ОЦЕНКА ВРЕМЕНИ

| Этап | Время |
|------|-------|
| Этап 1: Подготовка | 2-4 часа |
| Этап 2: Мультивалютность в БД | 3-4 часа |
| Этап 3: Мультивалютность в коде | 6-8 часов |
| Этап 4: Интернационализация | 4-8 часов |
| Этап 5: Расчеты вознаграждений | 5-7 часов |
| Этап 6: Интерфейс | 4-5 часов |
| Этап 7: Миграция данных | 2-3 часа |
| Этап 8: Тестирование | 8-10 часов |
| Этап 9: Документация | 6-9 часов |
| **ИТОГО** | **40-58 часов** (~5-7 рабочих дней) |

---

## 🎯 ПРИОРИТЕТЫ РЕАЛИЗАЦИИ

### MVP (Минимально жизнеспособный продукт):
1. ✅ Этап 2: Мультивалютность в БД (обязательно)
2. ✅ Этап 3: Базовые функции конвертации (обязательно)
3. ✅ Этап 5: Расчеты вознаграждений (обязательно)
4. ✅ Этап 7: Миграция данных (обязательно)

### Вторая очередь:
5. Этап 4: Интернационализация (желательно)
6. Этап 6: Обновление интерфейса (желательно)
7. Этап 8: Полное тестирование (желательно)

### Третья очередь:
8. Этап 9: Расширенная документация (опционально)
9. Автоматическое обновление курсов через API (опционально)

---

## ✅ ЧЕКЛИСТ ГОТОВНОСТИ

Перед запуском в production проверить:

- [ ] Все миграции БД выполнены
- [ ] Валюты определены для всех партнеров
- [ ] Функции конвертации протестированы
- [ ] Расчеты Revenue Share работают корректно
- [ ] Реферальные комиссии рассчитываются правильно
- [ ] Интерфейс отображает валюты корректно
- [ ] Миграция данных завершена
- [ ] Тесты пройдены
- [ ] Документация обновлена

---

**Документ создан:** Декабрь 2025  
**Версия:** 1.0  
**Статус:** Готов к реализации
