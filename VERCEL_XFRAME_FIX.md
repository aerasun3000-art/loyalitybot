# 🔧 Решение проблемы X-Frame-Options в Vercel

## ❌ Проблема

Ошибка: `Refused to display in a frame because it set 'X-Frame-Options' to 'deny'`

Vercel может устанавливать `X-Frame-Options: deny` по умолчанию для безопасности, и это блокирует загрузку приложения в iframe Telegram Web App.

## ✅ Решения

### Решение 1: Настройка через Vercel Dashboard (РЕКОМЕНДУЕТСЯ)

1. Откройте [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите проект `frontend`
3. Перейдите в **Settings** → **Headers**
4. Добавьте новый header:
   - **Source:** `/*`
   - **Header:** `Content-Security-Policy`
   - **Value:** `frame-ancestors https://web.telegram.org https://telegram.org *; frame-src *`
5. Сохраните изменения
6. Перезапустите деплой

### Решение 2: Использовать другой домен

Если проблема сохраняется, можно:
1. Использовать кастомный домен
2. Или использовать другой хостинг (Netlify, Cloudflare Pages)

### Решение 3: Проверить настройки проекта

В Vercel Dashboard:
1. Settings → General
2. Проверьте, что нет настроек безопасности, которые блокируют iframe
3. Отключите "Force HTTPS" если включено (может влиять на заголовки)

## 🔍 Проверка

После настройки проверьте заголовки:
```bash
curl -I https://frontend-32191inzw-alekseis-projects-3b6bffb8.vercel.app
```

Должны быть:
- `Content-Security-Policy: frame-ancestors ...`
- НЕ должно быть `X-Frame-Options: deny`

## 📝 Текущий статус

- ✅ `vercel.json` настроен с правильными заголовками
- ✅ `Content-Security-Policy` установлен
- ⚠️ Если ошибка сохраняется, нужно настроить через Vercel Dashboard

## 🎯 Новый URL

**URL:** `https://frontend-32191inzw-alekseis-projects-3b6bffb8.vercel.app`

Обновите Web App URL в BotFather на этот адрес.

