# 🤖 Интеграция AI с Linear для постановки задач

## ✅ Статус настройки

Интеграция **уже настроена** и готова к использованию!

- ✅ API ключ настроен в `.env`
- ✅ Подключение к Linear работает
- ✅ Доступны команды: **MARK** (MAR) и **ENGINE** (ENGI)

## 🚀 Как это работает

Я (AI ассистент) могу создавать задачи в Linear напрямую, используя функцию `create_linear_task()` из модуля `linear_task_creator.py`.

### Пример использования

Когда вы попросите меня поставить задачу, я могу выполнить:

```python
from linear_task_creator import create_linear_task

result = create_linear_task(
    title="Название задачи",
    description="Описание задачи",
    team_key="ENGI",  # или "MAR" для маркетинга
    priority=2  # 1=Urgent, 2=High, 3=Medium, 4=Low
)
```

## 📋 Доступные команды

- **ENGINE** (ключ: `ENGI`) - для технических задач
- **MARK** (ключ: `MAR`) - для маркетинговых задач

## 🎯 Приоритеты задач

- `1` - Urgent (Критично)
- `2` - High (Высокий)
- `3` - Medium (Средний) - по умолчанию
- `4` - Low (Низкий)

## 💡 Примеры запросов

Вы можете попросить меня:

- "Создай задачу в Linear: добавить функцию экспорта данных"
- "Поставь задачу в ENGINE: исправить баг с авторизацией"
- "Создай задачу в MAR: подготовить пост для соцсетей"

Я автоматически:
1. Создам задачу в нужной команде
2. Укажу приоритет (если не указан - Medium)
3. Добавлю описание
4. Сообщу вам идентификатор задачи (например, ENGI-123)

## 🔧 Технические детали

- Модуль: `linear_task_creator.py`
- API: Linear GraphQL API
- Ключ хранится в `.env` как `LINEAR_API_KEY`

## 🧪 Тестирование

Для проверки работы можно запустить:

```bash
python3 linear_task_creator.py
```

Это создаст тестовую задачу в Linear.

## 📝 Примечания

- Если команда не указана, задача создается в первой доступной команде
- Можно назначить задачу конкретному пользователю (нужен `assignee_id`)
- Можно привязать задачу к проекту (нужен `project_id`)

