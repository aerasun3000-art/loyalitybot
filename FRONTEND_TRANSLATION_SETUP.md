# 🌐 Настройка перевода новостей на фронтенде

## ⚠️ Важно

**Если бэкенд не задеплоен:**
- Переводы будут отключены автоматически
- Показывается оригинальный текст (русский)
- Ошибок не будет, всё работает корректно

**Если бэкенд задеплоен:**
- Установите `VITE_API_URL` в Vercel (см. ниже)

## ⚠️ Проблема

Если переводы не работают, скорее всего не установлена переменная окружения `VITE_API_URL` в Vercel.

## ✅ Решение

### Шаг 1: Узнайте URL вашего бэкенд API

Ваш бэкенд (`secure_api.py`) должен быть задеплоен на одном из сервисов:

- **Render**: `https://your-app.onrender.com`
- **Railway**: `https://your-app.railway.app`
- **Fly.io**: `https://your-app.fly.dev`
- **Другой сервис**: ваш URL

### Шаг 2: Установите переменную окружения в Vercel

1. Откройте проект в Vercel: https://vercel.com/dashboard
2. Выберите ваш проект (frontend)
3. Перейдите в **Settings** → **Environment Variables**
4. Добавьте новую переменную:
   - **Name**: `VITE_API_URL`
   - **Value**: `https://your-backend-api-url.com` (без `/api/translate` в конце!)
   - **Environment**: Production, Preview, Development (выберите все)
5. Нажмите **Save**

### Шаг 3: Пересоберите проект

После добавления переменной:

1. В Vercel перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **Redeploy** (три точки → Redeploy)
4. Или сделайте новый коммит и push

### Шаг 4: Проверьте работу

1. Откройте приложение
2. Откройте консоль браузера (F12 → Console)
3. Переключите язык на английский
4. Должны увидеть логи:
   ```
   🌐 Translation request: { url: 'https://...', text: '...', targetLang: 'en', ... }
   ```

Если видите ошибку:
```
❌ API_BASE_URL не установлен!
```

Значит переменная `VITE_API_URL` не установлена или не пересобрана.

---

## 🔍 Отладка

### Проверка в консоли браузера

Откройте консоль (F12) и выполните:

```javascript
console.log('API URL:', import.meta.env.VITE_API_URL)
```

Если выводит `undefined` - переменная не установлена.

### Проверка API напрямую

Попробуйте вызвать API напрямую:

```bash
curl -X POST https://your-api-url.com/api/translate \
  -H "Content-Type: application/json" \
  -d '{"text": "Привет", "target_lang": "en", "source_lang": "ru"}'
```

Должен вернуть:
```json
{
  "success": true,
  "translated_text": "Hello",
  "original_text": "Привет",
  "source_lang": "ru",
  "target_lang": "en"
}
```

---

## 📝 Примеры URL

### Если бэкенд на Render:
```
VITE_API_URL=https://loyalitybot-api.onrender.com
```

### Если бэкенд на Railway:
```
VITE_API_URL=https://loyalitybot-api.railway.app
```

### Если бэкенд на Fly.io:
```
VITE_API_URL=https://loyalitybot-api.fly.dev
```

**Важно:** Не добавляйте `/api/translate` в URL! Только базовый URL.

---

## 🚨 Частые ошибки

### Ошибка: "API_BASE_URL не установлен"
- **Решение**: Установите `VITE_API_URL` в Vercel и пересоберите проект

### Ошибка: "CORS policy"
- **Решение**: Проверьте настройки CORS в `secure_api.py` - должен быть разрешен ваш фронтенд домен

### Ошибка: "404 Not Found"
- **Решение**: Проверьте, что бэкенд запущен и доступен по указанному URL

### Ошибка: "401 Unauthorized" или "500 Internal Server Error"
- **Решение**: Проверьте, что `OPENAI_API_KEY` установлен в `.env` на бэкенде

---

## ✅ Чеклист

- [ ] Бэкенд API задеплоен и доступен
- [ ] `VITE_API_URL` установлен в Vercel
- [ ] Проект пересобран после добавления переменной
- [ ] CORS настроен на бэкенде
- [ ] `OPENAI_API_KEY` установлен на бэкенде
- [ ] Проверена работа API через curl
- [ ] Проверена консоль браузера на ошибки

---

**После выполнения всех шагов переводы должны работать!** 🎉

