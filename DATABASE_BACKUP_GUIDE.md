# Database Backup & Restore Guide

## 🎯 Обзор

Система автоматического резервного копирования базы данных Supabase.

---

## 📦 Что входит

1. **`backup_database.py`** - скрипт создания бэкапов
2. **`restore_database.py`** - скрипт восстановления из бэкапов
3. **`setup_backup_cron.sh`** - настройка автоматических бэкапов
4. **`backups/`** - директория для хранения бэкапов

---

## 🚀 Быстрый старт

### 1. Создание ручного бэкапа

```bash
# Полный бэкап всех таблиц
python3 backup_database.py

# Инкрементный бэкап (изменения за последние 24 часа)
python3 backup_database.py --type incremental

# Инкрементный бэкап с определённой даты
python3 backup_database.py --type incremental --since 2025-11-01

# С очисткой старых бэкапов (>30 дней)
python3 backup_database.py --cleanup
```

### 2. Просмотр доступных бэкапов

```bash
python3 restore_database.py --list
```

### 3. Восстановление из бэкапа

```bash
# DRY RUN (безопасный просмотр - ничего не меняет)
python3 restore_database.py --file backups/backup_20251115_160000.json

# Реальное восстановление (⚠️ ОСТОРОЖНО!)
python3 restore_database.py --file backups/backup_20251115_160000.json --execute
```

---

## ⏰ Автоматические бэкапы

### Настройка cron задачи

```bash
# Настроить автоматические бэкапы (каждый день в 3:00 AM)
bash setup_backup_cron.sh
```

### Проверка cron задачи

```bash
# Просмотр установленных задач
crontab -l | grep backup

# Просмотр логов
tail -f logs/backup_cron.log
```

### Изменение расписания

Отредактируйте `setup_backup_cron.sh`:

```bash
# Каждый день в 3:00 AM
CRON_SCHEDULE="0 3 * * *"

# Каждые 6 часов
CRON_SCHEDULE="0 */6 * * *"

# Каждое воскресенье в 2:00 AM
CRON_SCHEDULE="0 2 * * 0"
```

Затем запустите снова:
```bash
bash setup_backup_cron.sh
```

---

## 📊 Таблицы включённые в бэкап

- ✅ `users` - клиенты
- ✅ `partners` - партнёры
- ✅ `transactions` - транзакции
- ✅ `partner_applications` - заявки партнёров
- ✅ `services` - услуги партнёров
- ✅ `promotions` - промоакции
- ✅ `news` - новости
- ✅ `app_settings` - настройки приложения

Для добавления новых таблиц отредактируйте `TABLES_TO_BACKUP` в `backup_database.py`.

---

## 💾 Типы бэкапов

### 1. Полный бэкап (Full)

Экспортирует **все записи** из всех таблиц.

**Когда использовать:**
- Первый бэкап
- Перед важными изменениями
- Периодически (например, еженедельно)

**Размер:** ~1-10 MB (зависит от количества данных)

### 2. Инкрементный бэкап (Incremental)

Экспортирует только **изменённые записи** с определённой даты.

**Когда использовать:**
- Ежедневные бэкапы
- Для экономии места
- Для быстрого восстановления последних изменений

**Размер:** ~100-500 KB (значительно меньше)

---

## 🔄 Стратегии бэкапов

### Рекомендуемая стратегия

1. **Полный бэкап** - каждую неделю (воскресенье в 2:00 AM)
2. **Инкрементный бэкап** - каждый день (в 3:00 AM)
3. **Очистка старых** - при каждом бэкапе (старше 30 дней)

### Настройка:

```bash
# Еженедельный полный бэкап (воскресенье 2:00 AM)
0 2 * * 0 cd /path/to/project && python3 backup_database.py --type full --cleanup

# Ежедневный инкрементный бэкап (каждый день 3:00 AM)
0 3 * * 1-6 cd /path/to/project && python3 backup_database.py --type incremental
```

---

## ⚠️ Восстановление из бэкапа

### Важные моменты:

1. **Всегда используйте DRY RUN сначала**
   ```bash
   python3 restore_database.py --file backups/backup_20251115_160000.json
   ```

2. **Проверьте что восстанавливаете**
   - Дата бэкапа корректная?
   - Количество записей разумное?
   - Нет ошибок в бэкапе?

3. **Создайте бэкап перед восстановлением**
   ```bash
   python3 backup_database.py  # текущее состояние
   python3 restore_database.py --file old_backup.json --execute  # восстановление
   ```

4. **Восстановление использует UPSERT**
   - Существующие записи обновляются
   - Новые записи вставляются
   - Записи НЕ удаляются (безопасно)

---

## 🗂️ Структура бэкапа

Файл бэкапа в JSON формате:

```json
{
  "timestamp": "20251115_160000",
  "datetime": "2025-11-15T16:00:00",
  "tables": {
    "users": {
      "table": "users",
      "timestamp": "20251115_160000",
      "count": 150,
      "data": [
        {"id": 1, "name": "John", ...},
        {"id": 2, "name": "Jane", ...}
      ]
    },
    "partners": { ... },
    "transactions": { ... }
  }
}
```

---

## 📈 Мониторинг бэкапов

### Проверка последнего бэкапа

```bash
# Список бэкапов
ls -lht backups/ | head -5

# Размер последнего бэкапа
du -h backups/backup_*.json | tail -1

# Просмотр метаданных последнего бэкапа
python3 -c "import json; print(json.dumps(json.load(open(max(__import__('glob').glob('backups/backup_*.json')), encoding='utf-8'))['datetime'], indent=2))"
```

### Уведомления о проблемах

Добавьте в cron задачу скрипт проверки:

```bash
#!/bin/bash
# check_backup_health.sh

BACKUP_DIR="/path/to/backups"
ALERT_EMAIL="admin@example.com"

# Проверка что бэкап был создан за последние 25 часов
LAST_BACKUP=$(find $BACKUP_DIR -name "backup_*.json" -mtime -1 | wc -l)

if [ $LAST_BACKUP -eq 0 ]; then
    echo "⚠️ Бэкап не создавался более 24 часов!" | mail -s "Backup Alert" $ALERT_EMAIL
fi
```

---

## 🔐 Безопасность бэкапов

### 1. Шифрование бэкапов (опционально)

```bash
# Шифрование бэкапа
gpg -c backups/backup_20251115_160000.json

# Расшифровка
gpg backups/backup_20251115_160000.json.gpg
```

### 2. Хранение в облаке

```bash
# AWS S3
aws s3 cp backups/ s3://my-bucket/backups/ --recursive

# Google Cloud Storage
gsutil -m cp -r backups/ gs://my-bucket/backups/

# Dropbox (через rclone)
rclone copy backups/ dropbox:loyalitybot/backups/
```

### 3. Права доступа

```bash
# Ограничение доступа к бэкапам
chmod 600 backups/*.json
chmod 700 backups/
```

---

## 🐛 Troubleshooting

### Ошибка: "SUPABASE_URL не найден"

```bash
# Проверьте .env файл
cat .env | grep SUPABASE

# Убедитесь что переменные экспортированы
source .env
echo $SUPABASE_URL
```

### Ошибка: "Permission denied"

```bash
# Сделайте скрипты исполняемыми
chmod +x backup_database.py restore_database.py
```

### Большой размер бэкапов

```bash
# Используйте инкрементные бэкапы
python3 backup_database.py --type incremental

# Сжимайте бэкапы
gzip backups/backup_*.json
```

### Медленное восстановление

- Восстанавливайте только нужные таблицы
- Используйте более мощный сервер БД
- Восстанавливайте в нерабочее время

---

## 📚 Дополнительные ресурсы

- [Supabase Backup Documentation](https://supabase.com/docs/guides/platform/backups)
- [PostgreSQL Backup Best Practices](https://www.postgresql.org/docs/current/backup.html)
- [Cron Syntax Guide](https://crontab.guru/)

---

## 🎯 Чеклист перед продакшеном

- [ ] Создан первый полный бэкап
- [ ] Настроен автоматический бэкап через cron
- [ ] Протестировано восстановление (dry run)
- [ ] Настроена очистка старых бэкапов
- [ ] Бэкапы копируются в облако (опционально)
- [ ] Настроены уведомления о проблемах
- [ ] Документированы процедуры восстановления
- [ ] Команда знает как восстанавливать данные

---

## 💡 Best Practices

1. **Тестируйте восстановление регулярно** - минимум раз в месяц
2. **Храните бэкапы в нескольких местах** - локально + облако
3. **Автоматизируйте всё** - минимум ручных действий
4. **Мониторьте статус бэкапов** - знайте когда что-то идёт не так
5. **Документируйте процедуры** - любой член команды должен уметь восстановить
6. **Шифруйте чувствительные данные** - GDPR compliance
7. **Тестируйте на staging** - перед восстановлением в продакшене


