# Настройка BOT_USERNAME для партнерского бота

**Дата:** 2026-01-18  
**Проблема:** Ссылка в сообщении "Пригласить клиента" не кликабельна

---

## ✅ Исправления

### 1. Исправлен код для кликабельных ссылок

Исправлены файлы:
- `cloudflare/handlers/partner.js`
- `cloudflare/workers/partner-webhook/partner.js`

**Изменения:**
- Ссылка теперь обернута в HTML тег `<a href="...">...</a>`
- Используется `parseMode: 'HTML'` для правильного форматирования
- Markdown `**` заменен на HTML `<b>` для жирного текста

---

## 🔧 Настройка BOT_USERNAME

### Шаг 1: Узнать username клиентского бота

Username клиентского бота - это имя бота без символа `@`.

**Способ 1: Через @BotFather**
1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите ваш клиентский бот
4. Выберите "API Token" или "Bot Settings"
5. Скопируйте username (без `@`)

**Способ 2: Через клиентского бота**
1. Откройте клиентского бота в Telegram
2. Посмотрите на имя бота в шапке чата
3. Username - это часть после `@` (например, если бот `@mindbeatybot`, то username: `mindbeatybot`)

**Способ 3: Из старого кода**
Судя по старым файлам, username может быть: `mindbeatybot` (без `@`)

---

### Шаг 2: Добавить BOT_USERNAME в секреты Cloudflare

#### Для production (основной deployment):

```bash
cd /Users/ghbi/Downloads/loyalitybot/cloudflare/workers/partner-webhook
echo "mindbeatybot" | wrangler secret put BOT_USERNAME --env=""
```

**Важно:** Замените `mindbeatybot` на реальный username вашего клиентского бота!

---

### Шаг 3: Проверить, что секрет добавлен

```bash
cd /Users/ghbi/Downloads/loyalitybot/cloudflare/workers/partner-webhook
wrangler secret list --env="" | grep BOT_USERNAME
```

Должно появиться:
```
- BOT_USERNAME: Value Encrypted
```

---

### Шаг 4: Передеплоить Worker (если код изменился)

После исправления кода нужно передеплоить:

```bash
cd /Users/ghbi/Downloads/loyalitybot/cloudflare/workers/partner-webhook
wrangler deploy --env=""
```

---

## 📋 Полный список секретов для партнерского бота

Проверьте, что все секреты настроены:

```bash
cd /Users/ghbi/Downloads/loyalitybot/cloudflare/workers/partner-webhook
wrangler secret list --env=""
```

Должны быть:
- ✅ `TOKEN_PARTNER` - токен партнерского бота
- ✅ `SUPABASE_URL` - URL Supabase проекта
- ✅ `SUPABASE_KEY` - ключ Supabase API
- ✅ `FRONTEND_URL` - URL фронтенда
- ✅ `BOT_USERNAME` - username клиентского бота (для реферальных ссылок)
- ⚠️ `WEBHOOK_SECRET_TOKEN` - опционально, для валидации webhooks

---

## ✅ Проверка работы

После настройки:

1. Откройте партнерского бота в Telegram
2. Нажмите `/start`
3. Нажмите кнопку "👥 Пригласить клиента"
4. Должно появиться сообщение с **кликабельной ссылкой**:
   ```
   👥 Пригласить клиента
   
   Поделитесь этой ссылкой с клиентами:
   
   🔗 https://t.me/mindbeatybot?start=partner_406631153
   
   Клиенты, зарегистрированные по этой ссылке, будут привязаны к вам.
   ```
5. **Проверьте:** Ссылка должна быть кликабельной (синяя, подчеркнутая)

---

## 🔍 Устранение проблем

### Ссылка все еще не кликабельна:

1. Проверьте, что используется `parseMode: 'HTML'`
2. Проверьте, что ссылка обернута в тег `<a href="...">...</a>`
3. Проверьте, что нет ошибок в логах Cloudflare

### Username неправильный:

1. Проверьте username через @BotFather
2. Обновите секрет `BOT_USERNAME`:
   ```bash
   cd cloudflare/workers/partner-webhook
   echo "правильный_username" | wrangler secret put BOT_USERNAME --env=""
   ```
3. Передеплойте Worker (если нужно)

---

## 📝 Примечания

- **Username без `@`:** Указывайте username без символа `@` (например, `mindbeatybot`, а не `@mindbeatybot`)
- **Чувствительность к регистру:** Username может быть регистро-зависимым
- **Проверка ссылки:** После настройки проверьте ссылку - она должна открывать клиентского бота с параметром `start=partner_...`

---

**Дата создания:** 2026-01-18  
**Последнее обновление:** 2026-01-18
