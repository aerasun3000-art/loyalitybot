# 🤖 Настройка OpenAI для бота поддержки и переводов

## 📋 Обзор

Инструкция по переходу с GigaChat на OpenAI (GPT) для AI помощника и переводов текста.

---

## 💰 Рекомендации по выбору модели

### 🎯 Для вашего проекта (переводы + AI помощник)

| Модель | Цена входных | Цена выходных | Рекомендация |
|--------|--------------|---------------|--------------|
| **GPT-5 nano** | $0.050 / 1M | $0.400 / 1M | ✅ **Лучший выбор** - самый дешевый, отлично для переводов |
| **GPT-5 mini** | $0.250 / 1M | $2.000 / 1M | ✅ Хороший баланс - быстрее и качественнее |
| **GPT-3.5-turbo** | $0.500 / 1M | $1.500 / 1M | ✅ Если GPT-5 недоступен - проверенный вариант |
| **GPT-4** | $30.00 / 1M | $60.00 / 1M | ⚠️ Дорого, только для сложных задач |
| **GPT-5.1** | $1.250 / 1M | $10.000 / 1M | ❌ Слишком дорого для базовых задач |
| **GPT-5 pro** | $15.00 / 1M | $120.00 / 1M | ❌ Избыточно для переводов |

### 💡 Наша рекомендация

**Начните с GPT-5 nano или GPT-5 mini** (если доступны):
- ✅ Дешевле всего
- ✅ Достаточно качественно для переводов
- ✅ Быстро работает

**Если GPT-5 недоступен, используйте GPT-3.5-turbo:**
- ✅ Проверенная модель
- ✅ Хорошее качество
- ✅ Доступна везде

### 📊 Пример расчета стоимости

**Сценарий:** 1000 запросов в день (вопросы + переводы)
- Средний запрос: 50 входных + 100 выходных токенов
- Итого: 50K входных + 100K выходных токенов/день

| Модель | Стоимость/день | Стоимость/месяц |
|--------|----------------|-----------------|
| GPT-5 nano | $0.04 | **~$1.20** 💰 |
| GPT-5 mini | $0.23 | **~$6.90** |
| GPT-3.5-turbo | $0.33 | **~$9.90** |
| GPT-4 | $9.00 | **~$270** ❌ |

**Вывод:** GPT-5 nano экономит **~$8.70/месяц** по сравнению с GPT-3.5-turbo!

---

## 🎯 Шаг 1: Получение API ключа OpenAI

### 1.1 Регистрация

1. Перейдите на сайт: **https://platform.openai.com/**
2. Зарегистрируйтесь или войдите в аккаунт
3. Перейдите в раздел **API Keys**: https://platform.openai.com/api-keys

### 1.2 Создание API ключа

1. Нажмите **"Create new secret key"**
2. Дайте ключу имя (например, "LoyalityBot")
3. **ВАЖНО:** Скопируйте ключ сразу - он показывается только один раз!
4. Сохраните ключ в безопасном месте

### 1.3 Пополнение баланса

1. Перейдите в **Billing**: https://platform.openai.com/account/billing
2. Добавьте способ оплаты (карта)
3. Установите лимит расходов (рекомендуется $10-50/месяц для начала)
4. Проверьте тарифы на странице: https://openai.com/pricing

---

## 🔧 Шаг 2: Установка библиотеки

### 2.1 Установка openai

```bash
pip install openai>=1.0.0
```

Или обновите зависимости:

```bash
pip install -r requirements.txt
```

---

## 📝 Шаг 3: Настройка переменных окружения

### 3.1 Обновите `.env` файл

Добавьте или замените:

```bash
# AI / OpenAI (опционально)
# ----------------------------------------------
# API ключ для OpenAI (получить на https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-...

# Модель OpenAI
# Рекомендуется: gpt-5-nano (если доступно) или gpt-3.5-turbo
OPENAI_MODEL=gpt-5-nano

# Максимальное количество токенов в ответе
OPENAI_MAX_TOKENS=500
```

### 3.2 Варианты моделей

```bash
# Самый дешевый (рекомендуется)
OPENAI_MODEL=gpt-5-nano

# Баланс цены и качества
OPENAI_MODEL=gpt-5-mini

# Если GPT-5 недоступен
OPENAI_MODEL=gpt-3.5-turbo

# Для более сложных задач (дороже)
OPENAI_MODEL=gpt-4
```

---

## ✅ Шаг 4: Проверка работы

### 4.1 Проверка переменных окружения

```bash
# Проверьте, что ключ установлен
cat .env | grep OPENAI

# Должно быть:
# OPENAI_API_KEY=sk-...
# OPENAI_MODEL=gpt-5-nano
```

### 4.2 Тест API перевода

```bash
curl -X POST http://localhost:8001/api/translate \
  -H "Content-Type: application/json" \
  -d '{"text": "Привет, мир!", "target_lang": "en", "source_lang": "ru"}'
```

Ожидаемый ответ:
```json
{
  "success": true,
  "translated_text": "Hello, world!",
  "original_text": "Привет, мир!",
  "source_lang": "ru",
  "target_lang": "en"
}
```

### 4.3 Проверка в логах

Запустите бота и проверьте логи:

```bash
# Должны увидеть:
# INFO: AI response generated for question: ...
# INFO: Text translated: ru -> en (10 chars)
```

---

## 🔧 Настройка параметров

### Выбор модели в коде

В `ai_helper.py` можно изменить параметры:

```python
# Температура (креативность ответов)
temperature=0.7  # 0.0-2.0, выше = креативнее

# Для переводов используется:
temperature=0.3  # Низкая температура для точности

# Максимальная длина ответа
max_tokens=500  # Больше = длиннее ответы
```

---

## ⚠️ Важные замечания

### 1. Доступность в РФ

⚠️ **OpenAI может быть недоступен в России!**

**Решения:**
- Используйте VPN или прокси
- Рассмотрите **Yandex GPT** (российский аналог)
- Используйте **Hugging Face** модели (self-hosted)

### 2. Доступность GPT-5 моделей

⚠️ **GPT-5 модели могут быть еще не доступны!**

Если получите ошибку "Model not found":
- Используйте `gpt-3.5-turbo` (всегда доступен)
- Проверьте доступные модели: https://platform.openai.com/docs/models

### 3. Безопасность API ключа

- ✅ Никогда не коммитьте `.env` в Git
- ✅ Используйте переменные окружения на сервере
- ✅ Регулярно ротируйте ключи
- ✅ Устанавливайте лимиты расходов в настройках OpenAI

### 4. Rate Limits

OpenAI имеет лимиты:
- **Free tier:** 3 запроса/минуту
- **Paid tier:** зависит от тарифа (обычно 60-500 запросов/минуту)

При превышении лимита добавьте retry логику в `ai_helper.py`:

```python
import time
from openai import RateLimitError

try:
    response = self.client.chat.completions.create(...)
except RateLimitError:
    time.sleep(60)  # Подождать минуту
    response = self.client.chat.completions.create(...)
```

---

## 🐛 Решение проблем

### ❌ "OpenAI API key not found"

**Решение:**
1. Проверьте `.env` файл
2. Убедитесь, что ключ начинается с `sk-`
3. Перезапустите бота

### ❌ "Model not found" или "Model gpt-5-nano does not exist"

**Решение:**
1. GPT-5 модели могут быть еще не доступны
2. Используйте `gpt-3.5-turbo` в `.env`:
   ```bash
   OPENAI_MODEL=gpt-3.5-turbo
   ```
3. Проверьте доступные модели: https://platform.openai.com/docs/models

### ❌ "Rate limit exceeded"

**Решение:**
1. Проверьте лимиты в дашборде OpenAI
2. Увеличьте тариф или подождите
3. Добавьте задержки между запросами

### ❌ "Insufficient quota"

**Решение:**
1. Пополните баланс в OpenAI
2. Проверьте лимиты расходов
3. Убедитесь, что способ оплаты активен

### ❌ "Invalid API key"

**Решение:**
1. Проверьте правильность ключа
2. Убедитесь, что ключ не истек
3. Создайте новый ключ в дашборде OpenAI

---

## 📊 Мониторинг расходов

### Просмотр использования

1. Перейдите в **Usage**: https://platform.openai.com/usage
2. Проверьте расходы по дням/месяцам
3. Настройте алерты при превышении лимита

### Установка лимитов

1. Перейдите в **Billing** → **Limits**
2. Установите **Hard limit** (максимальный расход)
3. Установите **Soft limit** (предупреждение)

---

## 🔄 Откат на GigaChat

Если нужно вернуться к GigaChat:

1. Восстановите старую версию `ai_helper.py` из Git
2. Установите `GIGACHAT_API_KEY` в `.env`
3. Раскомментируйте `gigachat>=0.1.20` в `requirements.txt`
4. Установите: `pip install gigachat>=0.1.20`

---

## 📚 Дополнительные ресурсы

- **Документация OpenAI:** https://platform.openai.com/docs
- **API Reference:** https://platform.openai.com/docs/api-reference
- **Pricing:** https://openai.com/pricing
- **Status:** https://status.openai.com
- **Models:** https://platform.openai.com/docs/models

---

## ✅ Чеклист миграции

- [ ] Получен API ключ OpenAI
- [ ] Пополнен баланс (минимум $5-10)
- [ ] Установлен лимит расходов
- [ ] Установлена библиотека `openai>=1.0.0`
- [ ] Обновлен `.env` с `OPENAI_API_KEY` и `OPENAI_MODEL`
- [ ] Протестирован API перевода
- [ ] Проверена работа AI помощника
- [ ] Настроен мониторинг расходов

---

**Готово!** 🎉 Теперь ваш бот использует OpenAI вместо GigaChat.

**Рекомендуемая модель:** `gpt-5-nano` (если доступна) или `gpt-3.5-turbo`

**Следующие шаги:**
1. Протестируйте работу AI помощника
2. Проверьте качество переводов
3. Настройте мониторинг расходов
4. Оптимизируйте промпты для лучших результатов

