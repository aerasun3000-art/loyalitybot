# 📖 Как использовать файл `create_psychology_services.sql`

## 🎯 Назначение

Этот SQL скрипт добавляет 20 популярных услуг психологов и коучей в базу данных для конкретного партнера.

---

## 📋 Пошаговая инструкция

### Шаг 1: Найдите chat_id партнера-психолога

Выполните этот запрос в Supabase SQL Editor, чтобы найти партнера:

```sql
-- Поиск партнеров-психологов
SELECT 
  chat_id,
  name,
  company_name,
  business_type,
  city,
  district,
  status
FROM partners
WHERE business_type = 'mindfulness_coaching'
   OR company_name ILIKE '%психолог%'
   OR company_name ILIKE '%псих%'
   OR company_name ILIKE '%coach%'
   OR company_name ILIKE '%therapy%'
ORDER BY created_at DESC;
```

**Или найдите по имени/компании:**

```sql
-- Поиск по имени или названию компании
SELECT 
  chat_id,
  name,
  company_name,
  business_type
FROM partners
WHERE name ILIKE '%ИМЯ%'
   OR company_name ILIKE '%НАЗВАНИЕ%';
```

**Или посмотрите все партнеры:**

```sql
-- Все партнеры
SELECT 
  chat_id,
  name,
  company_name,
  business_type
FROM partners
ORDER BY created_at DESC
LIMIT 20;
```

---

### Шаг 2: Откройте файл `create_psychology_services.sql`

Откройте файл в любом текстовом редакторе (VS Code, Notepad++, Sublime Text и т.д.)

---

### Шаг 3: Замените `YOUR_PARTNER_CHAT_ID`

Найдите все вхождения `'YOUR_PARTNER_CHAT_ID'` и замените на реальный `chat_id` партнера.

**Пример:**
- Было: `('YOUR_PARTNER_CHAT_ID', 'Индивидуальная психотерапия', ...)`
- Стало: `('123456789', 'Индивидуальная психотерапия', ...)`

**Совет:** Используйте функцию "Найти и заменить" (Ctrl+H / Cmd+H) для замены всех вхождений сразу.

---

### Шаг 4: Выполните скрипт в Supabase

1. Откройте **Supabase Dashboard** → ваш проект
2. Перейдите в **SQL Editor** (левое меню)
3. Скопируйте весь содержимое файла `create_psychology_services.sql`
4. Вставьте в SQL Editor
5. Нажмите **Run** (или Ctrl+Enter)

---

### Шаг 5: Проверьте результат

После выполнения скрипт автоматически покажет все созданные услуги. 

**Или выполните проверку вручную:**

```sql
-- Проверка созданных услуг
SELECT 
  id,
  title,
  price_points,
  category,
  approval_status,
  is_active,
  created_at
FROM services
WHERE partner_chat_id = 'ВАШ_CHAT_ID'
  AND category = 'mindfulness_coaching'
ORDER BY price_points;
```

---

## 🔄 Использование для других партнеров

Этот файл можно использовать как **шаблон** для других партнеров-психологов:

1. Создайте копию файла (например, `create_psychology_services_partner2.sql`)
2. Замените `YOUR_PARTNER_CHAT_ID` на chat_id нового партнера
3. При необходимости измените цены или описания услуг
4. Выполните скрипт

---

## ✏️ Настройка услуг под ваши нужды

### Изменить цены

Найдите строку с услугой и измените значение `price_points`:

```sql
-- Было:
('123456789', 'Индивидуальная психотерапия', '...', 125, ...)

-- Стало (например, $150):
('123456789', 'Индивидуальная психотерапия', '...', 150, ...)
```

### Добавить/удалить услуги

- **Добавить:** Скопируйте одну из строк `INSERT` и измените данные
- **Удалить:** Просто удалите ненужные строки из списка `VALUES`

### Изменить описания

Отредактируйте текст в поле `description`:

```sql
('123456789', 'Индивидуальная психотерапия', 'ВАШЕ ОПИСАНИЕ', 125, ...)
```

---

## ⚠️ Важные замечания

1. **Проверьте chat_id** перед выполнением — неправильный ID создаст услуги не тому партнеру
2. **Убедитесь, что партнер существует** в таблице `partners`
3. **Все услуги создаются со статусом `Approved`** — они сразу будут видны клиентам
4. **Категория:** Все услуги создаются с категорией `mindfulness_coaching` (🧠 Ментальное здоровье)

---

## 🐛 Решение проблем

### Ошибка: "foreign key constraint"

**Проблема:** Партнер с таким `chat_id` не существует в таблице `partners`

**Решение:** 
1. Проверьте, что партнер зарегистрирован
2. Убедитесь, что используете правильный `chat_id` (строка, не число)

### Ошибка: "duplicate key"

**Проблема:** Услуги с такими названиями уже существуют

**Решение:** 
1. Удалите старые услуги или измените названия
2. Или используйте `ON CONFLICT DO NOTHING` в INSERT

### Услуги не отображаются на сайте

**Проверьте:**
1. `approval_status = 'Approved'`
2. `is_active = true`
3. `category = 'mindfulness_coaching'`
4. Партнер имеет статус `Approved` в таблице `partners`

---

## 📊 Что создается

После выполнения скрипта будет создано **20 услуг**:

- 9 услуг психологической терапии
- 5 услуг коучинга
- 6 дополнительных услуг (консультации, пакеты)

Все услуги готовы к использованию и сразу видны клиентам.

---

## 💡 Полезные SQL запросы

### Посмотреть все услуги партнера

```sql
SELECT 
  title,
  price_points,
  is_active
FROM services
WHERE partner_chat_id = 'ВАШ_CHAT_ID'
ORDER BY price_points;
```

### Удалить все услуги партнера (осторожно!)

```sql
DELETE FROM services
WHERE partner_chat_id = 'ВАШ_CHAT_ID'
  AND category = 'mindfulness_coaching';
```

### Деактивировать услугу

```sql
UPDATE services
SET is_active = false
WHERE id = 'ID_УСЛУГИ';
```

---

*Готово! Теперь вы можете использовать этот файл для быстрого добавления услуг психологов и коучей.*


