# 🔧 Исправление доступа к partner_applications

## Проблема

Бот пишет: **"Для начала работы нажмите ссылку на регистрацию Партнера."**

Это происходит потому, что после выполнения `fix_security_policies.sql` была удалена политика **"Allow public select"** для таблицы `partner_applications`. Бот использует `anon` ключ и не может прочитать таблицу, чтобы проверить, зарегистрирован ли пользователь как партнер.

## Решение

Выполните SQL скрипт `restore_bot_permissions.sql` в Supabase SQL Editor. Этот скрипт восстановит политику SELECT для таблицы `partner_applications`.

### Шаги:

1. **Откройте Supabase Dashboard**
   - Перейдите на https://supabase.com/dashboard
   - Выберите ваш проект

2. **Откройте SQL Editor**
   - В левом меню нажмите "SQL Editor"
   - Нажмите "New query"

3. **Выполните скрипт**
   - Откройте файл `restore_bot_permissions.sql`
   - Скопируйте весь его содержимое
   - Вставьте в SQL Editor
   - Нажмите "Run" или `Ctrl+Enter`

4. **Проверьте результат**
   - После выполнения скрипта проверьте, что политики созданы:
   ```sql
   SELECT tablename, policyname, roles 
   FROM pg_policies 
   WHERE tablename = 'partner_applications';
   ```
   
   Должны быть видны:
   - `Allow public select` (для чтения через anon ключ)
   - `Allow public insert` (для регистрации партнеров)
   - `Service role can manage applications` (для полного доступа через service_role)

5. **Перезапустите бота** (если еще не перезапускали)
   ```bash
   flyctl apps restart loyalitybot-partner
   ```

6. **Проверьте работу**
   - Откройте партнерский бот в Telegram
   - Отправьте `/start`
   - Если пользователь зарегистрирован как партнер со статусом "Approved", должно открыться главное меню
   - Если статус "Pending", должно показать сообщение о рассмотрении заявки

## Альтернативное решение (более безопасное)

Если вы хотите использовать более безопасный подход, переключите бот на `service_role` ключ:

1. **Получите service_role ключ:**
   - Supabase Dashboard → Settings → API
   - Скопируйте `service_role` ключ (НЕ anon!)

2. **Обновите переменную окружения:**
   ```bash
   flyctl secrets set SUPABASE_KEY="your_service_role_key" -a loyalitybot-partner
   ```

3. **Перезапустите бота:**
   ```bash
   flyctl apps restart loyalitybot-partner
   ```

При использовании `service_role` ключа политики SELECT для `anon` роли не нужны, так как `service_role` автоматически обходит RLS.

## Проверка текущих политик

Чтобы проверить, какие политики сейчас активны для `partner_applications`:

```sql
SELECT 
    policyname,
    cmd,
    roles,
    qual,
    with_check
FROM pg_policies
WHERE tablename = 'partner_applications'
ORDER BY cmd, policyname;
```

## Что делает скрипт restore_bot_permissions.sql

1. Восстанавливает политику **"Allow public select"** для `partner_applications` - это критично для работы бота с `anon` ключом
2. Проверяет и создает политику **"Allow public insert"** - для регистрации новых партнеров
3. Восстанавливает политики для других таблиц (`messages`, `news`, `partner_network`, и т.д.)
4. Создает политики для `service_role` для всех таблиц

После выполнения скрипта бот сможет читать таблицу `partner_applications` и проверять, зарегистрирован ли пользователь как партнер.
