# 🔍 Правила отладки для этого проекта

## ⚠️ ВАЖНО: При проблемах с данными

### Если проблема связана с:
- Отображением данных из базы
- Неправильными значениями
- Отсутствующими полями
- Несоответствием данных

### ВСЕГДА ПЕРВЫМ ДЕЛОМ:
1. ✅ **Попросить пользователя проверить базу данных**
2. ✅ **Запросить SQL запрос для проверки структуры таблиц**
3. ✅ **Запросить примеры данных из таблицы**

### НЕ ДЕЛАТЬ:
- ❌ Предполагать структуру базы данных
- ❌ Пытаться исправить код без проверки данных
- ❌ Гадать какие поля есть в таблице

---

## 📋 Стандартные SQL запросы для проверки:

### 1. Проверка структуры таблицы:
```sql
SELECT 
  column_name, 
  data_type, 
  is_nullable
FROM information_schema.columns 
WHERE table_name = 'НАЗВАНИЕ_ТАБЛИЦЫ'
ORDER BY ordinal_position;
```

### 2. Проверка данных:
```sql
SELECT *
FROM НАЗВАНИЕ_ТАБЛИЦЫ
LIMIT 10;
```

### 3. Проверка конкретных полей:
```sql
SELECT 
  id,
  поле1,
  поле2,
  поле3
FROM НАЗВАНИЕ_ТАБЛИЦЫ
WHERE условие
LIMIT 20;
```

---

## 🎯 Примеры ситуаций когда нужно проверять БД:

1. **Проблемы с отображением иконок/эмодзи**
   - Проверить: есть ли поле `icon` в таблице?
   - Что в нем хранится?

2. **Неправильные названия/тексты**
   - Проверить: какие данные в поле `title`/`name`?
   - Есть ли опечатки?

3. **Отсутствующие данные**
   - Проверить: есть ли записи в таблице?
   - Правильные ли условия фильтрации?

4. **Ошибки "column does not exist"**
   - Проверить: структуру таблицы
   - Какие поля реально существуют?

---

## 📝 Чек-лист перед исправлением кода:

- [ ] Запросил структуру таблицы из БД
- [ ] Получил примеры данных из таблицы
- [ ] Проверил какие поля реально существуют
- [ ] Убедился что понимаю проблему
- [ ] Только после этого исправляю код

---

**Дата создания:** 2025-11-05  
**Причина:** Проблема с иконками - код пытался обращаться к несуществующим полям `name` и `icon` в таблице `services`

