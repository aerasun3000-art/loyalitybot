# 🔍 Анализ периодических ошибок 401 Unauthorized

## Проблема
Периодически возникает ошибка `401 Unauthorized` при подключении к Telegram API, которая пропадает после обновления токена, но появляется снова через некоторое время.

## Возможные причины

### 1. ⚠️ Rate Limiting от Telegram API
**Причина:** Telegram ограничивает количество запросов в секунду. При превышении лимита может временно возвращаться 401.

**Признаки:**
- Ошибка возникает в периоды высокой активности
- Ошибка пропадает через несколько минут
- В логах видны множественные запросы подряд

**Решение:**
- Добавить задержки между запросами
- Использовать очередь запросов
- Реализовать exponential backoff при ошибках

### 2. 🔄 Несколько экземпляров бота с одним токеном
**Причина:** Если запущено несколько процессов бота с одним токеном, Telegram может временно блокировать доступ.

**Признаки:**
- В системе запущено несколько процессов `bot.py`
- Ошибка возникает после перезапуска или деплоя

**Решение:**
```bash
# Проверить количество процессов
ps aux | grep "bot.py" | grep -v grep

# Остановить все процессы перед запуском
pkill -f "bot.py"
sleep 2
# Затем запустить один экземпляр
```

### 3. 🌐 Проблемы с сетью/прокси
**Причина:** Временные сбои сети или проблемы с прокси могут вызывать 401 ошибки.

**Признаки:**
- Ошибка возникает случайно
- Связана с проблемами сети
- Пропадает сама по себе

**Решение:**
- Добавить retry логику с exponential backoff
- Проверить настройки прокси (если используется)
- Мониторить стабильность сети

### 4. 🔐 Проблемы с кэшированием токена
**Причина:** Токен может быть закэширован в переменных окружения или где-то еще используется старый токен.

**Признаки:**
- После обновления токена проблема временно решается
- В коде есть несколько мест использования токена

**Решение:**
- Проверить все места использования токена в коде
- Убедиться, что используется один источник токена
- Перезагружать переменные окружения при перезапуске

### 5. 📚 Проблемы с библиотекой pyTelegramBotAPI
**Причина:** Старая версия библиотеки или известные баги могут вызывать периодические 401 ошибки.

**Признаки:**
- Проблема возникает на определенной версии библиотеки
- После обновления библиотеки проблема решается

**Решение:**
```bash
# Обновить библиотеку
pip install --upgrade pytelegrambotapi

# Или зафиксировать конкретную версию
pip install pytelegrambotapi==4.14.0
```

### 6. 🔗 Конфликт между Webhook и Polling
**Причина:** Если где-то настроен webhook для бота, а код использует polling, может возникать конфликт.

**Признаки:**
- Ошибка возникает после настройки webhook
- В логах есть упоминания webhook

**Решение:**
```python
# Проверить и удалить webhook перед использованием polling
import requests
requests.post(f"https://api.telegram.org/bot{PARTNER_TOKEN}/deleteWebhook")
```

### 7. 🕐 Проблемы с временем сервера
**Причина:** Если время на сервере сильно отличается от времени Telegram, токены могут не работать.

**Признаки:**
- Ошибка возникает на определенных серверах
- Время на сервере не синхронизировано

**Решение:**
```bash
# Проверить время
date

# Синхронизировать время (Linux)
sudo ntpdate -s time.nist.gov
# или
sudo timedatectl set-ntp true
```

## Рекомендуемые исправления

### 1. Улучшить обработку ошибок с exponential backoff

```python
import time
import random

def run_bot_with_retry():
    logger.info("=== Партнёрский бот запущен ===")
    retry_count = 0
    max_retries = 10
    base_delay = 5
    
    while True:
        try:
            # Сбрасываем счетчик при успешном подключении
            retry_count = 0
            bot.polling(none_stop=True, interval=1, timeout=20)
        except KeyboardInterrupt:
            logger.info("Бот остановлен пользователем (KeyboardInterrupt)")
            break
        except Exception as e:
            error_msg = str(e)
            
            # Проверяем тип ошибки
            if "401" in error_msg or "Unauthorized" in error_msg:
                logger.error(f"Ошибка авторизации (401): {e}")
                
                # Проверяем токен перед повторной попыткой
                try:
                    bot_info = bot.get_me()
                    logger.info(f"Токен валиден, бот: @{bot_info.username}")
                except Exception as token_error:
                    logger.critical(f"Токен невалиден! Проверьте TOKEN_PARTNER")
                    # Увеличиваем задержку при проблемах с токеном
                    delay = base_delay * (2 ** min(retry_count, 5))
                    time.sleep(delay)
                    retry_count += 1
                    if retry_count >= max_retries:
                        logger.critical("Превышено максимальное количество попыток. Остановка бота.")
                        break
                    continue
            
            log_exception(logger, e, "Ошибка соединения с Telegram API")
            
            # Exponential backoff с jitter
            delay = base_delay * (2 ** min(retry_count, 5)) + random.uniform(0, 1)
            logger.warning(f"Переподключение через {delay:.1f} секунд... (попытка {retry_count + 1}/{max_retries})")
            time.sleep(delay)
            retry_count += 1
            
            if retry_count >= max_retries:
                logger.critical("Превышено максимальное количество попыток. Остановка бота.")
                break
```

### 2. Добавить проверку токена при старте

```python
# После инициализации бота
try:
    bot_info = bot.get_me()
    logger.info(f"✅ Бот успешно подключен: @{bot_info.username} (ID: {bot_info.id})")
except Exception as e:
    logger.critical(f"❌ Не удалось подключиться к Telegram API: {e}")
    logger.critical("Проверьте токен TOKEN_PARTNER в переменных окружения")
    raise
```

### 3. Проверить и удалить webhook перед polling

```python
# Перед запуском polling
try:
    import requests
    webhook_info = requests.get(f"https://api.telegram.org/bot{PARTNER_TOKEN}/getWebhookInfo").json()
    if webhook_info.get('result', {}).get('url'):
        logger.warning(f"Обнаружен активный webhook: {webhook_info['result']['url']}")
        delete_result = requests.post(f"https://api.telegram.org/bot{PARTNER_TOKEN}/deleteWebhook").json()
        if delete_result.get('ok'):
            logger.info("✅ Webhook удален, переходим на polling")
        else:
            logger.warning("⚠️ Не удалось удалить webhook")
except Exception as e:
    logger.warning(f"Не удалось проверить webhook: {e}")
```

### 4. Мониторинг и логирование

```python
# Добавить метрики для отслеживания ошибок
error_count_401 = 0
last_401_time = None

# В обработчике ошибок
if "401" in error_msg or "Unauthorized" in error_msg:
    error_count_401 += 1
    last_401_time = time.time()
    
    # Логировать каждую 10-ю ошибку
    if error_count_401 % 10 == 0:
        logger.warning(f"⚠️ Зафиксировано {error_count_401} ошибок 401")
        if last_401_time:
            time_since_last = time.time() - last_401_time
            logger.info(f"Последняя ошибка была {time_since_last:.0f} секунд назад")
```

## Диагностика

### Проверка текущего состояния

1. **Проверить количество процессов:**
```bash
ps aux | grep "bot.py" | grep -v grep | wc -l
```

2. **Проверить токен:**
```bash
python3 -c "
import os
from dotenv import load_dotenv
load_dotenv()
token = os.getenv('TOKEN_PARTNER')
if token:
    print(f'Токен найден: {token[:10]}...{token[-5:]}')
    import requests
    r = requests.get(f'https://api.telegram.org/bot{token}/getMe')
    print(f'Статус: {r.status_code}')
    print(f'Ответ: {r.json()}')
else:
    print('Токен не найден')
"
```

3. **Проверить webhook:**
```bash
python3 -c "
import os
import requests
from dotenv import load_dotenv
load_dotenv()
token = os.getenv('TOKEN_PARTNER')
r = requests.get(f'https://api.telegram.org/bot{token}/getWebhookInfo')
print(r.json())
"
```

4. **Проверить логи на паттерны:**
```bash
grep "401\|Unauthorized" bot_output.log | tail -20
```

## Рекомендации

1. **Немедленно:** Добавить проверку токена при старте и улучшить обработку ошибок
2. **Краткосрочно:** Реализовать exponential backoff и мониторинг ошибок
3. **Долгосрочно:** Рассмотреть переход на webhook вместо polling для большей стабильности

## Следующие шаги

1. Применить улучшенную обработку ошибок из раздела "Рекомендуемые исправления"
2. Добавить проверку токена при старте
3. Настроить мониторинг ошибок 401
4. Провести диагностику текущего состояния




