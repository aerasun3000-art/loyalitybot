# 🔧 Исправление "uvicorn: command not found" на Render

## 🔍 Проблема

В логах:
- ✅ Build successful - uvicorn установлен
- ❌ `bash: line 1: uvicorn: command not found` при запуске

**Причина:** Render использует виртуальное окружение, но uvicorn не в PATH при запуске.

---

## ✅ Решение: Использовать полный путь или активировать venv

### Вариант 1: Полный путь к uvicorn (рекомендуется)

Измените Start Command на:

```
/opt/render/project/src/.venv/bin/uvicorn secure_api:app --host 0.0.0.0 --port $PORT
```

---

### Вариант 2: Активировать venv и запустить

```
source /opt/render/project/src/.venv/bin/activate && uvicorn secure_api:app --host 0.0.0.0 --port $PORT
```

---

### Вариант 3: Python из venv с модулем

```
/opt/render/project/src/.venv/bin/python -m uvicorn secure_api:app --host 0.0.0.0 --port $PORT
```

---

## 🔧 Как исправить

### Шаг 1: Измените Start Command

1. Render Dashboard → **loyalitybot** → **Settings**
2. Найдите **"Start Command"**
3. Измените на **Вариант 1** (полный путь):
   ```
   /opt/render/project/src/.venv/bin/uvicorn secure_api:app --host 0.0.0.0 --port $PORT
   ```
4. Сохраните изменения

---

### Шаг 2: Дождитесь деплоя

Render автоматически перезадеплоит после сохранения.

---

## ✅ После исправления

После успешного деплоя:

1. **Проверьте Health Check:**
   ```bash
   curl https://loyalitybot.onrender.com/health
   ```
   Должен вернуть: `{"status":"ok"}`

2. **Проверьте Swagger UI:**
   ```
   https://loyalitybot.onrender.com/docs
   ```

---

## 📋 Почему это работает

Render создаёт виртуальное окружение в `/opt/render/project/src/.venv/`, и все пакеты устанавливаются туда. Используя полный путь к uvicorn из этого venv, мы гарантируем что команда найдётся.

---

*Используйте полный путь к uvicorn - это должно решить проблему!*


