# 🚀 Быстрый старт MLM системы

**ВАЖНО:** Следуйте инструкциям по порядку!

---

## ✅ ШАГ 1: Выполнить SQL миграцию

### Что делать:

1. Откройте **Supabase Dashboard** → **SQL Editor**

2. Откройте файл `supabase_mlm_partner_revenue_share.sql` в редакторе

3. **Скопируйте ВЕСЬ SQL код** из файла (только SQL, не Python!)

4. Вставьте в SQL Editor Supabase

5. Нажмите **Run** или **Execute**

### ⚠️ ВАЖНО:

- ❌ **НЕ копируйте** файл `check_mlm_database.py` (это Python скрипт!)
- ✅ **Копируйте ТОЛЬКО** файл `supabase_mlm_partner_revenue_share.sql` (это SQL!)

---

## ✅ ШАГ 2: Проверить базу данных

### Что делать:

1. Откройте терминал

2. Перейдите в папку проекта:
   ```bash
   cd /Users/ghbi/Downloads/loyalitybot
   ```

3. Запустите Python скрипт проверки:
   ```bash
   python3 check_mlm_database.py
   ```

### Что проверит скрипт:

- ✅ Существование всех таблиц
- ✅ Существование всех колонок
- ✅ Работу SQL функций
- ✅ Правильность расчета PV

---

## 📋 Чеклист выполнения

- [ ] **ШАГ 1:** SQL миграция выполнена в Supabase SQL Editor
- [ ] **ШАГ 2:** Python скрипт проверки запущен и показал успех
- [ ] Все таблицы созданы
- [ ] Все функции работают
- [ ] PV расчет корректен

---

## ❌ Если возникла ошибка

### Ошибка: "syntax error at or near #!/"

**Причина:** Вы скопировали Python файл вместо SQL файла

**Решение:**
1. Откройте файл `supabase_mlm_partner_revenue_share.sql` (НЕ `check_mlm_database.py`)
2. Скопируйте только SQL код
3. Вставьте в SQL Editor

### Ошибка: "table already exists"

**Причина:** Миграция уже была выполнена

**Решение:**
1. Это нормально, можно пропустить
2. Или удалить таблицы и выполнить заново

### Ошибка: "function does not exist"

**Причина:** Функции не были созданы

**Решение:**
1. Проверьте, что весь SQL код скопирован
2. Выполните миграцию полностью
3. Проверьте логи ошибок в Supabase

---

## 🔍 Как отличить файлы

### SQL файл (`supabase_mlm_partner_revenue_share.sql`):
```sql
-- Это SQL код
CREATE TABLE IF NOT EXISTS partner_network (
    id SERIAL PRIMARY KEY,
    ...
);
```

### Python файл (`check_mlm_database.py`):
```python
#!/usr/bin/env python3
# Это Python код
import os
from supabase import create_client
```

---

## 📞 Нужна помощь?

1. Проверьте, что вы используете правильный файл
2. Убедитесь, что SQL миграция выполнена полностью
3. Запустите скрипт проверки для диагностики

---

**Дата:** Ноябрь 2025  
**Версия:** 1.0

