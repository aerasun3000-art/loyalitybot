# 🔧 Исправление ошибки 404 при открытии Web App через бота

## ❌ Проблема

Ошибка `404: DEPLOYMENT_NOT_FOUND` при попытке открыть приложение через бота.

**Причина:** Бот использует неправильный или не установленный URL фронтенда.

---

## ✅ Решение

### Шаг 1: Узнайте правильный URL Netlify

Из скриншота видно, что ваш проект Netlify:
- **URL:** `https://gentle-biscochitos-13ff77.netlify.app`

### Шаг 2: Обновите переменную окружения FRONTEND_URL

#### Вариант A: Если бот запущен локально

Обновите файл `.env` в корне проекта:

```bash
FRONTEND_URL=https://gentle-biscochitos-13ff77.netlify.app
```

Затем перезапустите бота.

#### Вариант B: Если бот задеплоен на Fly.io

Установите секрет через Fly.io CLI:

```bash
flyctl secrets set FRONTEND_URL=https://gentle-biscochitos-13ff77.netlify.app --app loyalitybot-client
```

Или через Dashboard:
1. Откройте https://fly.io/apps/loyalitybot-client
2. Settings → Secrets
3. Добавьте: `FRONTEND_URL` = `https://gentle-biscochitos-13ff77.netlify.app`
4. Перезапустите бота

#### Вариант C: Если бот на другом сервисе (Render, Railway)

Установите переменную окружения `FRONTEND_URL` в настройках сервиса.

### Шаг 3: Перезапустите бота

После установки переменной окружения:

```bash
# Локально
pkill -f client_handler.py
python3 client_handler.py

# Или на Fly.io
flyctl apps restart loyalitybot-client
```

### Шаг 4: Проверьте логи бота

Проверьте, что бот использует правильный URL:

```bash
# Локально
grep "BASE_DOMAIN\|web_app_url" /tmp/client_bot.log | tail -5

# Или на Fly.io
flyctl logs --app loyalitybot-client | grep "BASE_DOMAIN\|web_app_url"
```

Должен появиться URL: `https://gentle-biscochitos-13ff77.netlify.app`

---

## 🔍 Диагностика

### Проверка 1: Какой URL использует бот сейчас?

В коде `client_handler.py` (строки 64-67):

```python
BASE_DOMAIN = os.environ.get('FRONTEND_URL')
if not BASE_DOMAIN:
    logger.warning("FRONTEND_URL не установлен в переменных окружения!")
    BASE_DOMAIN = 'https://your-frontend-domain.com'  # ← Это заглушка!
```

Если вы видите в логах предупреждение `FRONTEND_URL не установлен`, значит переменная не установлена.

### Проверка 2: Проверьте текущее значение

```bash
# Локально
grep FRONTEND_URL .env

# На Fly.io
flyctl secrets list --app loyalitybot-client
```

### Проверка 3: Проверьте URL в Telegram

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите и удерживайте кнопку "🚀 Открыть приложение"
4. В некоторых версиях Telegram можно увидеть URL

---

## 🚀 Быстрое исправление

### Для Fly.io (рекомендуется):

```bash
# Установите секрет
flyctl secrets set FRONTEND_URL=https://gentle-biscochitos-13ff77.netlify.app --app loyalitybot-client

# Перезапустите бота
flyctl apps restart loyalitybot-client

# Проверьте логи
flyctl logs --app loyalitybot-client
```

### Для локального запуска:

```bash
# Добавьте в .env
echo "FRONTEND_URL=https://gentle-biscochitos-13ff77.netlify.app" >> .env

# Перезапустите бота
pkill -f client_handler.py
python3 client_handler.py
```

---

## ⚠️ Важно

1. **URL должен быть HTTPS** - Telegram требует HTTPS для Web Apps
2. **URL должен быть доступен** - проверьте, что сайт открывается в браузере
3. **После изменения переменной окружения нужно перезапустить бота**

---

## 📝 Чеклист

- [ ] Узнали правильный URL Netlify (`gentle-biscochitos-13ff77.netlify.app`)
- [ ] Установили переменную `FRONTEND_URL` в настройках бота
- [ ] Перезапустили бота
- [ ] Проверили логи - нет предупреждения о FRONTEND_URL
- [ ] Протестировали открытие приложения через бота

---

## 🎯 После исправления

1. Откройте бота в Telegram
2. Отправьте `/start`
3. Нажмите "🚀 Открыть приложение"
4. Приложение должно открыться без ошибки 404

---

**Версия:** 1.0  
**Последнее обновление:** Декабрь 2024




