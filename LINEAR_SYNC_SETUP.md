# 🔄 Настройка синхронизации с Linear.app

## 📋 Быстрый старт

### 1. Получение API ключа Linear

1. Зайдите в [Linear.app](https://linear.app)
2. Нажмите на **ваш аватар/имя** в левом нижнем углу
3. Выберите **"Security & Access"** (или "Settings" → "Security & Access")
4. Найдите раздел **"Personal API keys"**
5. Нажмите **"+ Create API key"** или **"Create key"**
6. Дайте ключу название (например, "MVP Sync")
7. Скопируйте ключ (он начинается с `lin_api_...`)

⚠️ **Важно:** Ключ показывается только один раз! Сохраните его.

💡 **Примечание:** Если вы на странице "API" (как на скриншоте), там написано "View your personal API keys from your **security & access settings**" — это значит, что нужно перейти в настройки безопасности, а не создавать ключ на этой странице.

### 2. Установка зависимостей

```bash
pip install requests
```

Или добавьте в `requirements.txt`:
```
requests>=2.31.0
```

### 3. Настройка переменной окружения

**macOS/Linux:**
```bash
export LINEAR_API_KEY='lin_api_ваш_ключ_здесь'
```

**Windows (PowerShell):**
```powershell
$env:LINEAR_API_KEY='lin_api_ваш_ключ_здесь'
```

**Windows (CMD):**
```cmd
set LINEAR_API_KEY=lin_api_ваш_ключ_здесь
```

**Для постоянного сохранения (macOS/Linux):**
Добавьте в `~/.zshrc` или `~/.bashrc`:
```bash
export LINEAR_API_KEY='lin_api_ваш_ключ_здесь'
```

### 4. Запуск синхронизации

```bash
python3 linear_sync.py
```

Скрипт:
- ✅ Парсит задачи из `MVP_TASKS_LINEAR.md`
- ✅ Создает проекты для категорий (ENGINE, MARKET)
- ✅ Создает все 10 задач в Linear
- ✅ Назначает приоритеты (ENGINE - выше приоритет)

---

## 📊 Что будет создано в Linear

### Проекты:
- **ENGINE** (База данных и основные боты)
- **MARKET** (Подготовка к продажам и контент)

### Задачи:
**ENGINE (5 задач):**
1. Настройка базовой схемы БД и миграции
2. Разработка клиентского бота (основной функционал)
3. Разработка партнерского бота (основной функционал)
4. Разработка админского бота (модерация)
5. Система лояльности (4 уровня и транзакции)

**MARKET (5 задач):**
6. Контент для онбординга партнеров
7. Промо-материалы и шаблоны акций
8. Демо-материалы для продаж
9. Базовый контент (новости и переводы)
10. Настройка Instagram Outreach процесса

---

## 🔧 Дополнительные настройки

### Использование конкретной команды

Если у вас несколько команд в Linear, можно указать ключ команды:

```python
sync = LinearSync(api_key=api_key)
sync.sync_tasks(team_key="ENG")  # Использовать команду с ключом "ENG"
```

### Без создания проектов

Если проекты уже созданы вручную:

```python
sync.sync_tasks(create_projects=False)
```

### Изменение приоритетов

В коде можно изменить приоритеты задач:

```python
# В методе sync_tasks()
priority = 2 if "ENGINE" in category else 3
# 1 = Urgent, 2 = High, 3 = Medium, 4 = Low
```

---

## 🐛 Решение проблем

### Ошибка: "No teams found"
- Убедитесь, что API ключ правильный
- Проверьте, что у вас есть доступ к команде в Linear

### Ошибка: "Team with key 'XXX' not found"
- Проверьте ключ команды в Linear (Settings → Teams)
- Или не указывайте team_key, скрипт использует первую доступную команду

### Ошибка: "Failed to create issue"
- Проверьте права доступа API ключа
- Убедитесь, что команда активна в Linear

### Задачи создаются, но без проектов
- Проверьте, что у вас есть права на создание проектов
- Или создайте проекты вручную в Linear и запустите скрипт снова

---

## 📝 Ручной импорт (альтернатива)

Если скрипт не работает, можно импортировать задачи вручную:

1. Откройте `MVP_TASKS_LINEAR.md`
2. В Linear создайте проекты ENGINE и MARKET
3. Для каждой задачи:
   - Нажмите "Create issue"
   - Скопируйте заголовок и описание
   - Назначьте проект
   - Установите приоритет

---

## 🔄 Повторная синхронизация

Скрипт создает задачи заново при каждом запуске. Если нужно обновить существующие задачи, используйте Linear UI или создайте отдельный скрипт для обновления.

---

## 📚 Полезные ссылки

- [Linear API Documentation](https://developers.linear.app/docs/graphql/working-with-the-graphql-api)
- [Linear GraphQL Explorer](https://linear.app/settings/api)
- [Personal API Keys](https://linear.app/settings/api/personal-api-keys)

