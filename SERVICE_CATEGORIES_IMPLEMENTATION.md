# 📋 Реализация категорий услуг с фильтрацией

## ✅ Что было реализовано:

### 1. Добавлен шаг выбора категории при создании услуги
- Теперь процесс создания услуги состоит из **4 шагов** (вместо 3)
- Шаг 4: Выбор категории из списка (17 категорий)

### 2. Добавлено поле `category` в таблицу `services`
- SQL миграция: `supabase_add_category_to_services.sql`
- Поле хранит тег категории (например: `manicure`, `hairstyle`, `massage`)

### 3. Обновлён фронтенд для фильтрации по категориям
- При клике на иконку услуги на главной странице открывается список услуг этой категории
- Страница Services фильтрует услуги по параметру `?category=...` из URL

## 🔧 Что нужно сделать:

### Шаг 1: Применить SQL миграцию в Supabase

1. Откройте **Supabase Dashboard** → ваш проект
2. Перейдите в **SQL Editor**
3. Скопируйте содержимое файла `supabase_add_category_to_services.sql`
4. Выполните SQL запрос

**Или выполните команду:**
```sql
ALTER TABLE services 
ADD COLUMN IF NOT EXISTS category TEXT;

CREATE INDEX IF NOT EXISTS idx_services_category ON services(category);
CREATE INDEX IF NOT EXISTS idx_services_category_approval ON services(category, approval_status) 
WHERE approval_status = 'Approved' AND is_active = true;
```

### Шаг 2: Перезапустить бот

```bash
# Остановить бот
kill $(ps aux | grep 'python.*bot.py' | grep -v grep | awk '{print $2}')

# Запустить снова
nohup python3 bot.py > bot_output.log 2>&1 &
```

### Шаг 3: Обновить фронтенд на Vercel

```bash
cd frontend
npm run build
# Затем задеплоить на Vercel или через git push
```

## 📝 Доступные категории услуг:

| Иконка | Тег | Название |
|--------|-----|----------|
| 💅 | `manicure` | Маникюр |
| 💇‍♀️ | `hairstyle` | Прически |
| 💆‍♀️ | `massage` | Массаж |
| 🧴 | `cosmetologist` | Косметолог |
| ✨ | `eyebrows` | Брови |
| 👁️ | `eyelashes` | Ресницы |
| 💫 | `laser` | Лазерная эпиляция |
| 💄 | `makeup` | Визажист |
| 🌸 | `skincare` | Уход за кожей |
| 🧹 | `cleaning` | Уборка |
| 🔧 | `repair` | Ремонт |
| 🚗 | `delivery` | Доставка |
| 🏃‍♀️ | `fitness` | Фитнес |
| 🛁 | `spa` | SPA |
| 🧘‍♀️ | `yoga` | Йога |
| 🥗 | `nutrition` | Питание |
| 🧠 | `psychology` | Психолог |

## 🎯 Как это работает:

### При создании услуги партнёром:
1. Партнёр нажимает "🛠️ Услуги" → "➕ Добавить новую услугу"
2. Вводит название (шаг 1)
3. Вводит описание (шаг 2)
4. Вводит стоимость в баллах (шаг 3)
5. **Выбирает категорию из списка** (шаг 4) ← **НОВОЕ**
6. Услуга сохраняется с полем `category`

### При клике на иконку на главной странице:
1. Пользователь видит сетку услуг (4x2)
2. Кликает на иконку (например, 💅 Маникюр)
3. Открывается страница `/services?category=manicure`
4. Показываются только услуги категории "Маникюр"

### При просмотре всех услуг:
1. Пользователь открывает страницу `/services`
2. Если в URL есть `?category=...`, фильтруются услуги по категории
3. Если параметра нет, показываются все услуги

## 🔄 Обновление существующих услуг:

Если у вас уже есть услуги в базе данных без категории:

```sql
-- Обновить категорию на основе названия (пример для маникюра)
UPDATE services 
SET category = 'manicure' 
WHERE title ILIKE '%маникюр%' OR title ILIKE '%ногт%'
AND category IS NULL;

-- Аналогично для других категорий
UPDATE services 
SET category = 'hairstyle' 
WHERE title ILIKE '%прическ%' OR title ILIKE '%волос%' OR title ILIKE '%стрижк%'
AND category IS NULL;

UPDATE services 
SET category = 'massage' 
WHERE title ILIKE '%массаж%'
AND category IS NULL;

-- И так далее для всех категорий...
```

## ✅ Проверка работы:

1. **Создайте новую услугу через партнёрский бот:**
   - Убедитесь, что появился шаг 4 с выбором категории
   - Выберите категорию и сохраните услугу

2. **Проверьте в базе данных:**
   ```sql
   SELECT id, title, category, approval_status 
   FROM services 
   ORDER BY created_at DESC 
   LIMIT 5;
   ```
   - Должно быть поле `category` с выбранным значением

3. **Проверьте на фронтенде:**
   - Откройте главную страницу
   - Кликните на иконку услуги
   - Должна открыться страница с отфильтрованными услугами

## 📝 Файлы изменены:

1. ✅ `bot.py` - добавлен шаг выбора категории
2. ✅ `supabase_manager.py` - поддержка поля category при сохранении
3. ✅ `frontend/src/pages/Home.jsx` - фильтрация при клике на иконку
4. ✅ `frontend/src/pages/Services.jsx` - чтение параметра category из URL
5. ✅ `frontend/src/services/supabase.js` - фильтрация по category в запросе
6. ✅ `supabase_add_category_to_services.sql` - SQL миграция

## 🚨 Важно:

- **Обязательно примените SQL миграцию** перед использованием!
- Существующие услуги без категории будут работать, но не будут фильтроваться по иконкам
- При создании новой услуги **обязательно** нужно выбрать категорию









