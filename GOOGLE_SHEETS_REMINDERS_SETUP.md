# 📋 Настройка автоматических напоминаний в Google Sheets

## 🎯 Что делает этот скрипт?

Автоматически отслеживает статусы партнеров в Google Sheets и отправляет напоминания:
- ⏰ Когда нужно сделать follow-up (через 48 часов и 7 дней)
- 📞 Напоминания о запланированных созвонах
- 📊 Создание задач для менеджеров
- 📧 Отправка уведомлений в Email и/или Telegram

---

## 📝 ШАГ 1: Подготовка Google Sheets

### 1.1. Убедитесь, что у вас есть таблица со следующей структурой:

| № | Дата | Имя | Instagram | ... | Статус | Дата контакта | ... | Последний контакт | Комментарий |
|---|------|-----|-----------|-----|--------|---------------|-----|-------------------|-------------|
| 1 | ... | ... | ...       | ... | SENT   | 2025-01-15    | ... | 2025-01-15       | ...         |

**Важные колонки:**
- **Колонка C** (3): Имя партнера
- **Колонка D** (4): Instagram
- **Колонка I** (9): Статус (SENT, FOLLOW_UP_1, CLOSED, etc.)
- **Колонка J** (10): Дата контакта
- **Колонка O** (15): Последний контакт

### 1.2. Название листа должно быть: `Partners Tracking`

Если у вас другое название, измените в скрипте:
```javascript
SHEET_NAME: 'Partners Tracking',  // ← Измените здесь
```

---

## 🔧 ШАГ 2: Установка скрипта

### 2.1. Откройте Apps Script

1. Откройте вашу Google Sheets таблицу
2. Нажмите `Extensions` → `Apps Script`
3. Откроется редактор скриптов

### 2.2. Вставьте код

1. Удалите весь существующий код (если есть)
2. Скопируйте содержимое файла `google_sheets_reminders.gs`
3. Вставьте в редактор
4. Нажмите `Save` (💾)

---

## ⚙️ ШАГ 3: Настройка конфигурации

Найдите в начале файла раздел `КОНФИГУРАЦИЯ` и настройте под себя:

### 3.1. Базовые настройки

```javascript
const CONFIG = {
  SHEET_NAME: 'Partners Tracking',  // Название вашего листа
  TASKS_SHEET_NAME: 'Tasks',        // Лист для задач (создается автоматически)
  
  // Если ваши колонки в другом порядке, измените номера:
  COLUMNS: {
    NAME: 3,              // Колонка C
    INSTAGRAM: 4,         // Колонка D
    STATUS: 9,            // Колонка I
    LAST_CONTACT: 15,     // Колонка O
    // ...
  },
  
  // Время для напоминаний
  FOLLOW_UP_DELAYS: {
    FIRST_REMINDER: 48,    // Первое напоминание через 48 часов
    SECOND_REMINDER: 168   // Второе напоминание через 7 дней
  },
};
```

### 3.2. Настройка Email уведомлений

```javascript
EMAIL: {
  ENABLED: true,                              // Включить/выключить
  TO: 'your-email@example.com',               // Ваш email
  SUBJECT: '📋 Напоминание: Follow-up с партнерами'
},
```

**Важно:** Email будет отправляться с адреса вашего Google аккаунта.

### 3.3. Настройка Telegram уведомлений (опционально)

#### Шаг 3.3.1: Создайте Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям, создайте бота
4. Скопируйте **токен** бота (например: `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

#### Шаг 3.3.2: Узнайте ваш Chat ID

1. Откройте [@userinfobot](https://t.me/userinfobot) в Telegram
2. Нажмите `/start`
3. Скопируйте ваш **ID** (например: `123456789`)

#### Шаг 3.3.3: Настройте в скрипте

```javascript
TELEGRAM: {
  ENABLED: true,                            // Включить Telegram
  BOT_TOKEN: '123456789:ABCdef...',         // Токен бота
  CHAT_ID: '123456789'                      // Ваш Chat ID
},
```

---

## 🚀 ШАГ 4: Запуск и настройка триггеров

### 4.1. Первый запуск (тестовый)

1. В редакторе Apps Script выберите функцию `manualCheck`
2. Нажмите кнопку **▶ Run** (выполнить)
3. При первом запуске нужно будет **разрешить доступ**:
   - Нажмите `Review permissions`
   - Выберите ваш Google аккаунт
   - Нажмите `Advanced` → `Go to [project name] (unsafe)`
   - Нажмите `Allow`

### 4.2. Создание автоматических триггеров

1. В редакторе Apps Script выберите функцию `setupTriggers`
2. Нажмите **▶ Run**
3. Триггеры созданы! ✅

**Что создается:**
- ✅ Проверка каждые **6 часов**
- ✅ Ежедневная проверка в **9:00 UTC**

### 4.3. Проверка триггеров

1. В редакторе Apps Script нажмите на иконку ⏰ (часы) слева
2. Вы увидите список активных триггеров
3. Можете редактировать или удалять их

---

## 📊 ШАГ 5: Проверка работы

### 5.1. Проверка логов

1. В редакторе Apps Script нажмите `View` → `Logs`
2. Вы увидите записи о выполнении скрипта:
   - ✅ Успешные операции
   - ❌ Ошибки
   - ℹ️ Информационные сообщения

### 5.2. Проверка листа Tasks

После первого запуска появится новый лист `Tasks` с задачами для follow-up.

**Структура листа Tasks:**
| Дата создания | Тип | Имя партнера | Instagram | Задача | Статус | Новый статус |
|---------------|-----|--------------|-----------|--------|--------|--------------|

### 5.3. Проверка Email/Telegram

- Проверьте почту (может быть в спаме)
- Проверьте Telegram (если настроен)

---

## 🎛️ Дополнительные функции

### Экспорт задач в CSV

```javascript
// Выберите функцию exportTasksToCSV и запустите
// Создаст CSV файл в Google Drive
```

### Ручная проверка

```javascript
// Выберите функцию manualCheck и запустите
// Проверит партнеров прямо сейчас
```

---

## 🔍 Логика работы

### Когда отправляются напоминания?

1. **Первое напоминание:**
   - Статус партнера: `SENT`
   - Прошло ≥ 48 часов с последнего контакта
   - Новый статус: `FOLLOW_UP_1`

2. **Второе напоминание:**
   - Статус партнера: `FOLLOW_UP_1`
   - Прошло ≥ 7 дней с последнего контакта
   - Новый статус: `FOLLOW_UP_2`

3. **Напоминание о созвоне:**
   - Статус партнера: `CALL_SCHEDULED`
   - Дата созвона = сегодня
   - Отправляется уведомление

### Какие статусы игнорируются?

- `CLOSED` - сделка закрыта
- `NOT_INTERESTED` - не интересно
- `GHOSTED` - не отвечает (финальный статус)

---

## 🐛 Устранение проблем

### Проблема: Скрипт не запускается

**Решение:**
1. Проверьте логи (View → Logs)
2. Убедитесь, что разрешения предоставлены
3. Проверьте название листа (`SHEET_NAME`)

### Проблема: Напоминания не отправляются

**Решение:**
1. Проверьте, что `EMAIL.ENABLED = true` или `TELEGRAM.ENABLED = true`
2. Для Telegram: проверьте токен и Chat ID
3. Проверьте логи на ошибки

### Проблема: Неправильные колонки

**Решение:**
1. Проверьте номера колонок в `COLUMNS`
2. Убедитесь, что данные в правильных колонках
3. Первая строка = заголовки (пропускается)

### Проблема: Telegram не работает

**Решение:**
1. Проверьте токен бота (должен начинаться с цифр и двоеточия)
2. Проверьте Chat ID (только цифры)
3. Убедитесь, что вы отправили `/start` боту хотя бы раз

---

## 📈 Настройка времени напоминаний

### Изменить задержки

```javascript
FOLLOW_UP_DELAYS: {
  FIRST_REMINDER: 24,    // Через 24 часа вместо 48
  SECOND_REMINDER: 120   // Через 5 дней вместо 7
},
```

### Изменить время проверки

1. Удалите старые триггеры (⏰ → Delete)
2. Измените код в `setupTriggers()`:
   ```javascript
   // Например, проверка каждые 3 часа:
   .everyHours(3)
   
   // Или в определенное время:
   .atHour(10)  // 10:00 UTC
   ```

---

## 🔐 Безопасность

- ✅ Скрипт работает только с вашей таблицей
- ✅ Токены Telegram хранятся только в скрипте (не публично)
- ✅ Email отправляется с вашего Google аккаунта

**Рекомендации:**
- Не делитесь файлом скрипта с токенами
- Используйте отдельного бота для уведомлений (не основной)
- Регулярно проверяйте логи на подозрительную активность

---

## 📞 Поддержка

Если что-то не работает:

1. **Проверьте логи** в Apps Script (View → Logs)
2. **Проверьте конфигурацию** (правильные номера колонок, названия листов)
3. **Тестируйте пошагово**: сначала `manualCheck()`, потом триггеры

---

## ✅ Чек-лист настройки

- [ ] Google Sheets таблица создана
- [ ] Структура колонок правильная
- [ ] Скрипт установлен в Apps Script
- [ ] Конфигурация настроена
- [ ] Email или Telegram настроен
- [ ] Первый запуск выполнен (разрешения предоставлены)
- [ ] Триггеры созданы
- [ ] Логи проверены
- [ ] Тестовое напоминание получено

---

**Готово!** 🎉 Теперь напоминания будут отправляться автоматически!
