# 🔍 Как проверить URL приложения

## Способ 1: Проверить в коде бота ✅

URL хранится в переменной `BASE_DOMAIN` в файле `client_handler.py`:

```python
BASE_DOMAIN = os.environ.get('FRONTEND_URL', 'https://frontend-42sxe56ot-alekseis-projects-3b6bffb8.vercel.app')
```

**Текущий URL:** `https://frontend-42sxe56ot-alekseis-projects-3b6bffb8.vercel.app`

## Способ 2: Проверить в логах бота 📋

Когда бот отправляет кнопку "🚀 Открыть приложение", он логирует URL.

**Выполните в терминале:**
```bash
grep "web_app_url\|BASE_DOMAIN" /tmp/client_bot.log | tail -5
```

Или просто смотрите логи в реальном времени:
```bash
tail -f /tmp/client_bot.log
```

Когда нажмёте `/start` в боте, в логах появится строка с URL.

## Способ 3: Проверить в Telegram (визуально) 👀

1. Откройте бота `@mindbeaty` в Telegram
2. Нажмите `/start`
3. Посмотрите на кнопку "🚀 Открыть приложение"
4. **Нажмите и удерживайте кнопку** (long press)
5. В некоторых версиях Telegram можно увидеть URL

**Или:**
- Нажмите на кнопку
- Приложение откроется
- В адресной строке браузера (если открылось в браузере) будет виден URL

## Способ 4: Проверить в браузере 🌐

Откройте URL напрямую в браузере:

```
https://frontend-42sxe56ot-alekseis-projects-3b6bffb8.vercel.app
```

**Что должно произойти:**
- Страница загрузится
- Появится интерфейс приложения
- Если открыто не из Telegram, может быть сообщение об авторизации

## Способ 5: Проверить в консоли приложения 🔧

Если приложение уже открыто в Telegram:

1. Откройте DevTools (если доступны)
2. В консоли выполните:
```javascript
console.log(window.location.href)
```

**Или:**
- В коде приложения уже есть логирование URL
- Откройте консоль браузера (если возможно)
- Найдите строки с `🔍 Debug - Window location:`

## Способ 6: Проверить через Python скрипт 🐍

Создайте временный скрипт:

```python
import sys
sys.path.append('/Users/ghbi/Downloads/loyalitybot')
from client_handler import BASE_DOMAIN
print(f"Текущий URL: {BASE_DOMAIN}")
```

**Выполните:**
```bash
cd /Users/ghbi/Downloads/loyalitybot
python3 -c "import sys; sys.path.append('.'); from client_handler import BASE_DOMAIN; print(f'URL: {BASE_DOMAIN}')"
```

## Способ 7: Проверить через curl 🌐

Проверьте, что URL доступен:

```bash
curl -I https://frontend-42sxe56ot-alekseis-projects-3b6bffb8.vercel.app
```

**Ожидаемый результат:**
```
HTTP/2 200
```

## ✅ Самый простой способ

**Просто откройте бота в Telegram и нажмите кнопку "🚀 Открыть приложение"**

Если приложение открывается и работает - URL правильный! ✅

Если не открывается или показывает ошибку - URL неправильный или приложение не задеплоено. ❌

## 🔍 Что проверить дополнительно

1. **Версия в URL:** В URL должны быть параметры кэш-бастинга (`?v=...&nocache=1...`)
2. **HTTPS:** URL должен начинаться с `https://`
3. **Домен:** Должен быть `vercel.app` или ваш кастомный домен

## 📝 Текущий статус

- ✅ URL в коде: `https://frontend-42sxe56ot-alekseis-projects-3b6bffb8.vercel.app`
- ✅ Бот перезапущен с новым URL
- ✅ Фронтенд задеплоен на этот URL
- ✅ Готово к тестированию

