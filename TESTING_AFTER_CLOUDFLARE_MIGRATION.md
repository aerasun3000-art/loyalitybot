# 🧪 Тестирование после миграции на Cloudflare

**Дата:** 2026-01-18  
**Статус:** После миграции на Cloudflare webhooks  
**Цель:** Протестировать все фичи, которые могли отвалиться после миграции

---

## 📋 Обзор

После перехода на webhooks и сервер Cloudflare система стала работать стабильнее, но многие настроенные фичи могли отвалиться. Необходимо протестировать все функции заново.

---

## 🚀 Быстрый старт

### 1. Очистка старых задач и создание новых

```bash
# Сначала посмотреть что будет сделано (dry-run)
python3 cleanup_and_recreate_linear_tasks.py --dry-run

# Только удалить старые задачи
python3 cleanup_and_recreate_linear_tasks.py --cleanup-only

# Только создать новые задачи
python3 cleanup_and_recreate_linear_tasks.py --create-only

# Полная очистка и создание (интерактивно)
python3 cleanup_and_recreate_linear_tasks.py

# С указанием команды
python3 cleanup_and_recreate_linear_tasks.py --team ENG
```

### 2. Проверка API ключа Linear

Убедитесь, что в `.env` файле есть:
```bash
LINEAR_API_KEY=lin_api_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```

---

## 📊 Создаваемые задачи

Скрипт создаст **13 задач** для тестирования:

### Критические (P0) - 3 задачи
1. **TEST-CF-001**: Клиентский бот - регистрация через /start
2. **TEST-CF-002**: Партнерский бот - регистрация и главное меню
3. **TEST-CF-003**: Webhook валидация и секретный токен

### Важные (P1) - 4 задачи
4. **TEST-CF-004**: Клиентский бот - баланс и NPS
5. **TEST-CF-005**: Партнерский бот - меню и навигация
6. **TEST-CF-006**: Интеграция с Supabase
7. **TEST-CF-007**: Интеграция с Telegram API

### Обычные (P2) - 6 задач
8. **TEST-CF-008**: Обработка ошибок и логирование
9. **TEST-CF-009**: Производительность и задержки
10. **TEST-CF-010**: Переменные окружения и секреты
11. **TEST-CF-011**: Реферальные ссылки и партнеры
12. **TEST-CF-012**: Админ-бот (если реализован)
13. **TEST-CF-013**: REST API Worker

---

## 🗑️ Удаляемые задачи

Скрипт автоматически найдет и предложит удалить задачи, содержащие:

- `TC-` - Старые тест-кейсы (до миграции)
- `PREFLIGHT-` - Pre-flight проверки (устарели)
- `Миграция на Cloudflare` - Задачи миграции (уже выполнены)
- `CLOUDFLARE_MIGRATION` - Задачи миграции
- `Fly.io` - Задачи связанные со старой инфраструктурой
- `long polling` - Старый метод работы ботов
- `TEST-` - Старые тесты (но НЕ `TEST-CF-`)

**Исключение:** Задачи с `TEST-CF-` НЕ удаляются (это новые задачи).

---

## 🔍 Что тестировать

### 1. Клиентский бот (client-webhook)

**Основные функции:**
- ✅ Регистрация через `/start`
- ✅ Регистрация через реферальную ссылку `/start partner_123`
- ✅ Приветственное сообщение с кнопками
- ✅ Начисление welcome bonus
- ✅ Callback "Мой баланс"
- ✅ NPS рейтинг (если реализован)

**Worker:** `cloudflare/workers/client-webhook`  
**Handlers:** `cloudflare/handlers/client.js`

**Проверка:**
```bash
# Проверить webhook URL
curl -s "https://api.telegram.org/bot<TOKEN_CLIENT>/getWebhookInfo" | python3 -m json.tool

# Проверить логи
cd cloudflare/workers/client-webhook
wrangler tail
```

---

### 2. Партнерский бот (partner-webhook)

**Основные функции:**
- ✅ Регистрация через `/start`
- ✅ Главное меню для одобренных партнеров
- ✅ Адаптивное меню для Influencer (без Операций и Контента)
- ✅ Все кнопки меню (Операции, Контент, Аналитика, Revenue Share, Ещё)
- ✅ Callback queries для навигации

**Worker:** `cloudflare/workers/partner-webhook`  
**Handlers:** `cloudflare/handlers/partner.js`

**Проверка:**
```bash
# Проверить webhook URL
curl -s "https://api.telegram.org/bot<TOKEN_PARTNER>/getWebhookInfo" | python3 -m json.tool

# Проверить логи
cd cloudflare/workers/partner-webhook
wrangler tail
```

---

### 3. Админ-бот (admin-webhook)

**Статус:** TODO - проверить реализацию

**Если реализован:**
- ✅ Команды админа
- ✅ Одобрение/отклонение заявок партнеров
- ✅ Создание новостей

**Worker:** `cloudflare/workers/admin-webhook` (если есть)

---

### 4. REST API Worker

**Статус:** TODO - проверить реализацию

**Если реализован:**
- ✅ Все эндпоинты доступны
- ✅ Интеграция с фронтендом
- ✅ CORS настроен правильно

**Worker:** `cloudflare/workers/api`

---

## 🔐 Безопасность

### Webhook Secret Token

**Проверка:**
- Webhook должен валидировать secret token (если настроен)
- Запросы без токена должны обрабатываться (если токен не настроен)
- CORS preflight запросы должны работать

**Настройка:**
```bash
cd cloudflare/workers/client-webhook
wrangler secret put WEBHOOK_SECRET_TOKEN
```

---

## 📊 Интеграции

### Supabase

**Проверка:**
- Подключение к Supabase работает
- Чтение данных (users, partners)
- Запись данных (создание пользователей, транзакций)
- Обработка ошибок БД

**Utils:** `cloudflare/utils/supabase.js`

### Telegram API

**Проверка:**
- Отправка сообщений работает
- Отправка сообщений с клавиатурами работает
- Callback queries обрабатываются
- Ошибки API обрабатываются

**Utils:** `cloudflare/utils/telegram.js`

---

## 🐛 Обработка ошибок

**Что проверить:**
- Ошибки логируются в Cloudflare Dashboard
- Telegram получает 200 OK (чтобы не retry)
- Пользователь видит понятные сообщения об ошибках

**Логи:**
```bash
# Просмотр логов в реальном времени
cd cloudflare/workers/client-webhook
wrangler tail

# Или через Cloudflare Dashboard
# Workers → Select Worker → Logs
```

---

## ⚡ Производительность

**Метрики:**
- Время ответа webhook < 1 секунды
- Нет таймаутов
- Быстрая работа Edge network

**Проверка:**
```bash
# Измерить время ответа
time curl -X POST "https://loyalitybot-client-webhook.aerasun3000.workers.dev" \
  -H "Content-Type: application/json" \
  -d '{"update_id": 1, "message": {...}}'
```

**Метрики в Cloudflare Dashboard:**
- Workers → Select Worker → Analytics

---

## 🔧 Конфигурация

### Переменные окружения

**Проверка секретов:**
```bash
# Список секретов
cd cloudflare/workers/client-webhook
wrangler secret list

# Проверка значений (не показываются, но можно проверить наличие)
```

**Необходимые секреты:**
- `TOKEN_CLIENT` / `TOKEN_PARTNER` / `TOKEN_ADMIN`
- `SUPABASE_URL`
- `SUPABASE_KEY`
- `FRONTEND_URL` (должен быть Cloudflare Pages)
- `WELCOME_BONUS_AMOUNT`
- `WEBHOOK_SECRET_TOKEN` (опционально)

---

## 📝 Чеклист тестирования

### Pre-flight проверки

- [ ] Все webhook URLs настроены в Telegram
- [ ] Все секреты настроены в Cloudflare Workers
- [ ] FRONTEND_URL указывает на Cloudflare Pages
- [ ] Логи доступны в Cloudflare Dashboard

### Критические тесты

- [ ] TEST-CF-001: Клиентский бот - регистрация
- [ ] TEST-CF-002: Партнерский бот - регистрация и меню
- [ ] TEST-CF-003: Webhook валидация

### Важные тесты

- [ ] TEST-CF-004: Баланс и NPS
- [ ] TEST-CF-005: Меню и навигация
- [ ] TEST-CF-006: Интеграция с Supabase
- [ ] TEST-CF-007: Интеграция с Telegram API

### Обычные тесты

- [ ] TEST-CF-008: Обработка ошибок
- [ ] TEST-CF-009: Производительность
- [ ] TEST-CF-010: Переменные окружения
- [ ] TEST-CF-011: Реферальные ссылки
- [ ] TEST-CF-012: Админ-бот (если есть)
- [ ] TEST-CF-013: REST API (если есть)

---

## 🚨 Известные проблемы

### После миграции могли отвалиться:

1. **Реферальные ссылки** - Проверить сохранение `referral_source`
2. **NPS система** - Проверить отправку и сохранение рейтингов
3. **QR-коды** - Проверить генерацию и отправку QR-кодов
4. **Транзакции** - Проверить начисление/списание баллов
5. **Уведомления** - Проверить отправку уведомлений клиентам
6. **Аналитика** - Проверить работу дашбордов
7. **Revenue Share** - Проверить MLM систему

---

## 📚 Полезные ссылки

- [Cloudflare Workers Docs](https://developers.cloudflare.com/workers/)
- [Cloudflare Dashboard](https://dash.cloudflare.com/)
- [Linear App](https://linear.app/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

## 🎯 Следующие шаги

1. Запустить скрипт для создания задач в Linear
2. Пройти все тесты по порядку
3. Исправить найденные проблемы
4. Обновить задачи в Linear с результатами
5. Создать отчет о тестировании

---

**Документ создан:** 2026-01-18  
**Версия:** 1.0
