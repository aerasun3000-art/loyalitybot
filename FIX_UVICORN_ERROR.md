# 🔧 Исправление ошибки "uvicorn: command not found"

## 🔍 Проблема

В логах видно:
- ✅ Build successful - uvicorn установлен
- ❌ `bash: line 1: uvicorn: command not found` при запуске

**Причина:** uvicorn установлен, но не доступен в PATH при запуске на Render.

---

## ✅ Решение

Используйте `python -m uvicorn` вместо просто `uvicorn`.

---

## 🔧 Как исправить

### Шаг 1: Измените Start Command

1. В Render Dashboard → ваш сервис **"loyalitybot"**
2. Перейдите в **Settings**
3. Найдите раздел **"Start Command"**
4. Измените на:
   ```
   python -m uvicorn secure_api:app --host 0.0.0.0 --port $PORT
   ```
5. Сохраните изменения

---

### Шаг 2: Перезадеплойте

После сохранения Render автоматически перезадеплоит сервис.

Или нажмите **"Manual Deploy"** → **"Deploy latest commit"**.

---

## ✅ После исправления

После успешного деплоя:

1. **Проверьте Health Check:**
   ```bash
   curl https://loyalitybot.onrender.com/health
   ```
   Должен вернуть: `{"status":"ok"}`

2. **Проверьте Swagger UI:**
   ```
   https://loyalitybot.onrender.com/docs
   ```

---

## 📋 Разница команд

**Неправильно:**
```
uvicorn secure_api:app --host 0.0.0.0 --port $PORT
```

**Правильно:**
```
python -m uvicorn secure_api:app --host 0.0.0.0 --port $PORT
```

`python -m uvicorn` гарантирует что uvicorn будет найден через Python модуль.

---

*Измените Start Command на `python -m uvicorn` и всё заработает!*


