# 💰 План интеграции TON COIN для взаиморасчетов

**Дата:** Декабрь 2025  
**Статус:** Предложение к реализации  
**Приоритет:** Высокий

---

## 📋 ОБЗОР РЕШЕНИЯ

### Цель
Интегрировать блокчейн TON (The Open Network) для выплат Revenue Share партнерам, заменив или дополнив существующую систему банковских переводов.

### Преимущества
- ✅ **Мгновенные выплаты** (5-10 секунд вместо 1-5 дней)
- ✅ **Минимальные комиссии** (~$0.01-0.05 вместо 2-5%)
- ✅ **Прозрачность** всех транзакций в блокчейне
- ✅ **Глобальная доступность** без ограничений географии
- ✅ **Интеграция с Telegram Wallet** для удобства партнеров

---

## 🏗️ АРХИТЕКТУРА РЕШЕНИЯ

### Компоненты системы

```
┌─────────────────────────────────────────┐
│   Partner Revenue Share Calculation     │
│   (calculate_monthly_revenue_share.py)  │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   TON Payment Service                   │
│   (ton_payment_service.py)              │
│   - Отправка TON транзакций             │
│   - Получение курса TON/USD             │
│   - Batch выплаты                       │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Telegram Wallet API / TON SDK         │
│   - pytonlib или ton-python             │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   TON Blockchain                        │
└──────────────┬──────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────┐
│   Telegram Wallet (партнеры)            │
│   - Получение выплат                    │
│   - Автоматические уведомления          │
└─────────────────────────────────────────┘
```

---

## 📊 ИЗМЕНЕНИЯ В БАЗЕ ДАННЫХ

### 1. Добавление полей в таблицу `partners`

```sql
-- Добавляем поддержку TON кошельков
ALTER TABLE partners 
ADD COLUMN IF NOT EXISTS ton_wallet_address VARCHAR(48),
ADD COLUMN IF NOT EXISTS payment_method VARCHAR(20) DEFAULT 'bank' 
    CHECK (payment_method IN ('bank', 'ton', 'both'));

-- Индексы для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_partners_payment_method 
    ON partners(payment_method) 
    WHERE payment_method = 'ton' OR payment_method = 'both';
```

### 2. Создание таблицы для TON транзакций

```sql
-- Таблица для хранения TON транзакций
CREATE TABLE IF NOT EXISTS ton_payments (
    id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
    partner_chat_id TEXT NOT NULL REFERENCES partners(chat_id) ON DELETE CASCADE,
    revenue_share_id INTEGER REFERENCES partner_revenue_share(id) ON DELETE SET NULL,
    
    -- Суммы
    amount_nano BIGINT NOT NULL,  -- Сумма в нанотонах (1 TON = 1e9 nanoTON)
    amount_usd NUMERIC(10,2) NOT NULL,  -- Эквивалент в USD на момент выплаты
    exchange_rate NUMERIC(12,6) NOT NULL,  -- Курс TON/USD на момент выплаты
    
    -- Блокчейн данные
    ton_tx_hash VARCHAR(64) UNIQUE,  -- Hash транзакции в блокчейне
    ton_tx_lt BIGINT,  -- Logical time транзакции
    ton_block_seqno INTEGER,  -- Номер блока
    
    -- Статусы
    status VARCHAR(20) NOT NULL DEFAULT 'pending' 
        CHECK (status IN ('pending', 'sent', 'confirmed', 'failed')),
    
    -- Метаданные
    comment TEXT,  -- Комментарий к транзакции
    error_message TEXT,  -- Сообщение об ошибке (если failed)
    
    -- Временные метки
    created_at TIMESTAMP DEFAULT NOW(),
    sent_at TIMESTAMP,  -- Когда транзакция отправлена
    confirmed_at TIMESTAMP,  -- Когда транзакция подтверждена в блокчейне
    
    -- Откат (если нужно)
    retry_count INTEGER DEFAULT 0,
    last_retry_at TIMESTAMP
);

-- Индексы
CREATE INDEX IF NOT EXISTS idx_ton_payments_partner 
    ON ton_payments(partner_chat_id);
CREATE INDEX IF NOT EXISTS idx_ton_payments_status 
    ON ton_payments(status);
CREATE INDEX IF NOT EXISTS idx_ton_payments_hash 
    ON ton_payments(ton_tx_hash);
CREATE INDEX IF NOT EXISTS idx_ton_payments_revenue_share 
    ON ton_payments(revenue_share_id);
```

### 3. Обновление таблицы `partner_revenue_share`

```sql
-- Добавляем поля для TON выплат
ALTER TABLE partner_revenue_share
ADD COLUMN IF NOT EXISTS payment_method VARCHAR(20) DEFAULT 'bank',
ADD COLUMN IF NOT EXISTS ton_tx_hash VARCHAR(64),
ADD COLUMN IF NOT EXISTS ton_payment_id UUID REFERENCES ton_payments(id);
```

---

## 🐍 PYTHON КОМПОНЕНТЫ

### 1. TON Payment Service (`ton_payment_service.py`)

**Зависимости:**
- `pytonlib` или `ton-python` - для работы с TON блокчейном
- `requests` - для получения курса валют

**Функционал:**
- Получение курса TON/USD
- Отправка TON транзакций
- Проверка статуса транзакций
- Batch выплаты
- Обработка ошибок и retry логика

### 2. Интеграция с Revenue Share (`calculate_monthly_revenue_share.py`)

**Изменения:**
- После одобрения выплаты проверять `payment_method` партнера
- Для TON выплат: конвертировать USD → TON и отправлять через `TONPaymentService`
- Обновлять статус в `partner_revenue_share` и `ton_payments`

### 3. Команды в боте (`bot.py`)

**Новые команды:**
- `/setup_wallet` - настройка TON кошелька
- `/my_wallet` - просмотр привязанного кошелька
- `/change_wallet` - изменение кошелька
- `/payment_method` - выбор метода выплат (bank/ton/both)

---

## 🔧 ТЕХНИЧЕСКАЯ РЕАЛИЗАЦИЯ

### Фаза 1: Базовая инфраструктура (1-2 недели)

#### 1.1. Установка зависимостей

Добавить в `requirements.txt`:
```txt
pytonlib>=0.1.0  # или ton-python
aiohttp>=3.9.0  # для async запросов (если используем async)
```

#### 1.2. Создание SQL миграций

Файл: `supabase_ton_integration.sql`
- Добавление полей в `partners`
- Создание таблицы `ton_payments`
- Обновление `partner_revenue_share`
- Индексы и constraints

#### 1.3. Базовая структура `ton_payment_service.py`

```python
class TONPaymentService:
    def __init__(self, wallet_address, wallet_seed, network='mainnet'):
        """Инициализация TON Payment Service"""
        pass
    
    async def get_ton_price(self) -> float:
        """Получить текущий курс TON/USD"""
        pass
    
    async def send_payment(
        self,
        recipient_wallet: str,
        amount_nano: int,
        comment: str = None
    ) -> dict:
        """Отправить TON транзакцию"""
        pass
    
    async def check_transaction_status(self, tx_hash: str) -> dict:
        """Проверить статус транзакции в блокчейне"""
        pass
```

### Фаза 2: Интеграция с Revenue Share (1 неделя)

#### 2.1. Модификация `calculate_monthly_revenue_share.py`

Добавить функцию:
```python
async def process_ton_payments(
    period_start: date,
    period_end: date
):
    """Обработка TON выплат для одобренных Revenue Share"""
    pass
```

#### 2.2. Обновление статусов

После успешной отправки TON:
- Обновить `partner_revenue_share.status` → 'paid'
- Обновить `partner_revenue_share.ton_tx_hash`
- Записать в `ton_payments` со статусом 'sent'

### Фаза 3: Telegram Bot интеграция (3-5 дней)

#### 3.1. Команда `/setup_wallet`

```python
@bot.message_handler(commands=['setup_wallet'])
def setup_ton_wallet(message):
    """Настройка TON кошелька для партнера"""
    # Инструкция по созданию кошелька
    # Запрос адреса кошелька
    # Валидация адреса
    # Сохранение в БД
```

#### 3.2. Команда `/payment_method`

```python
@bot.message_handler(commands=['payment_method'])
def set_payment_method(message):
    """Выбор метода выплат: bank/ton/both"""
    # Inline клавиатура с вариантами
    # Обновление payment_method в БД
```

### Фаза 4: Автоматизация и мониторинг (1 неделя)

#### 4.1. Cron задача для автоматических выплат

Добавить в расписание:
- После расчета Revenue Share (1-го числа месяца)
- Проверка статуса pending транзакций
- Автоматическая отправка TON выплат
- Retry для failed транзакций

#### 4.2. Мониторинг транзакций

- Периодическая проверка статуса 'sent' транзакций
- Обновление до 'confirmed' после подтверждения в блокчейне
- Уведомления партнерам о получении выплаты

---

## 🔐 БЕЗОПАСНОСТЬ

### Хранение ключей

**ВНИМАНИЕ:** Критически важно безопасное хранение seed-фразы кошелька!

```python
# Использовать переменные окружения
TON_WALLET_ADDRESS = os.getenv('TON_WALLET_ADDRESS')
TON_WALLET_SEED = os.getenv('TON_WALLET_SEED')  # Хранить в секретах!

# ИЛИ использовать сервис для управления ключами:
# - Hardware Security Module (HSM)
# - Cloud Key Management Service (KMS)
# - Vault от HashiCorp
```

### Валидация адресов

```python
def validate_ton_address(address: str) -> bool:
    """Валидация TON адреса"""
    # Проверка формата: EQ... (base64) или 0:... (hex)
    # Проверка checksum
    pass
```

### Rate limiting

- Ограничение количества запросов к TON API
- Задержки между транзакциями
- Batch обработка для экономии комиссий

---

## 📈 МЕТРИКИ И МОНИТОРИНГ

### Ключевые метрики

1. **Скорость выплат**
   - Время от approval до confirmed: цель < 1 минуты
   - Процент транзакций, подтвержденных за < 5 минут: цель > 95%

2. **Надежность**
   - Процент успешных транзакций: цель > 99%
   - Количество retry: отслеживать и минимизировать

3. **Экономия**
   - Сравнение комиссий: TON vs Bank
   - Общая экономия в месяц

4. **Принятие**
   - Количество партнеров с настроенным TON кошельком
   - Процент выплат через TON

---

## 🚀 ПОЭТАПНЫЙ ПЛАН ВНЕДРЕНИЯ

### Этап 1: MVP (2 недели) ⚠️ ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ
- [ ] SQL миграции для БД
- [ ] Базовая версия `ton_payment_service.py`
- [ ] Команда `/setup_wallet` в боте
- [ ] Ручная отправка тестовых выплат

### Этап 2: Интеграция (1 неделя) ⚠️ ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ
- [ ] Интеграция с `calculate_monthly_revenue_share.py`
- [ ] Автоматическая конвертация USD → TON
- [ ] Обновление статусов в БД
- [ ] Тестирование на тестовой сети

### Этап 3: Автоматизация (1 неделя) ⚠️ ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ
- [ ] Cron задача для автоматических выплат
- [ ] Мониторинг статуса транзакций
- [ ] Retry логика для failed транзакций
- [ ] Уведомления партнерам

### Этап 4: Оптимизация (1-2 недели) ⚠️ ТРЕБУЕТ ПОДТВЕРЖДЕНИЯ
- [ ] Batch транзакции (экономия комиссий)
- [ ] Дашборд для мониторинга TON выплат
- [ ] Автоматические отчеты
- [ ] Документация для партнеров

---

## ⚠️ РИСКИ И МИТИГАЦИЯ

| Риск | Вероятность | Митигация |
|------|-------------|-----------|
| Волатильность курса TON | Средняя | Конвертация в момент выплаты, фиксация курса на 5 минут |
| Технические проблемы TON сети | Низкая | Retry логика, fallback на банковские переводы |
| Ошибки при отправке | Низкая | Валидация адресов, проверка баланса кошелька |
| Нежелание партнеров использовать TON | Средняя | Обучение, демонстрация выгод, гибридная система |

---

## 💰 ЭКОНОМИЧЕСКИЙ ЭФФЕКТ

### Расчет экономии (пример для $33,085.80/год выплат)

**Банковские переводы:**
- Комиссии (3%): $992.57
- Валютные потери (1.5%): $496.29
- **Итого: $1,488.86/год**

**TON Coin:**
- Комиссии сети (~50 транзакций × $0.03): $1.50
- **Итого: $1.50/год**

**Экономия: $1,487.36/год на одного партнера**

**При 100 партнерах: $148,736/год экономии**

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

### Перед началом реализации необходимо:

1. ✅ **Подтверждение архитектуры** - согласовать предложенное решение
2. ⏳ **Выбор TON SDK** - решить между `pytonlib` и `ton-python`
3. ⏳ **Настройка TON кошелька** - создать основной кошелек для выплат
4. ⏳ **Получение API ключей** - для получения курса TON/USD (например, CoinGecko, Binance)
5. ⏳ **Тестирование на testnet** - перед запуском в mainnet

### Файлы для создания/изменения:

1. **Новые файлы:**
   - `ton_payment_service.py` - сервис для работы с TON
   - `supabase_ton_integration.sql` - SQL миграции
   - `TON_WALLET_SETUP_GUIDE.md` - инструкция для партнеров

2. **Изменения в существующих файлах:**
   - `requirements.txt` - добавить зависимости TON
   - `calculate_monthly_revenue_share.py` - интеграция TON выплат
   - `bot.py` - команды для настройки кошелька
   - `.env.example` - переменные окружения для TON

---

## ❓ ВОПРОСЫ ДЛЯ ОБСУЖДЕНИЯ

1. **Выбор SDK:** Какой использовать - `pytonlib` или `ton-python`?
2. **Управление ключами:** Где хранить seed-фразу кошелька? (HSM, KMS, env variables)
3. **Гибридность:** Всегда предлагать выбор между банком и TON, или по умолчанию TON?
4. **Минимальная сумма:** Устанавливать ли минимальную сумму для TON выплат?
5. **Тестирование:** Использовать testnet для начального тестирования?
6. **Курс валют:** Какой API использовать для получения TON/USD? (CoinGecko, Binance, другой?)

---

**Готов начать реализацию после вашего подтверждения!** 🚀

