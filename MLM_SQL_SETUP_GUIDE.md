# 📋 Инструкция по выполнению SQL миграции для MLM системы

## 🎯 Способ 1: Через Supabase Dashboard (Рекомендуется)

### Шаг 1: Откройте Supabase Dashboard
1. Перейдите на [https://supabase.com](https://supabase.com)
2. Войдите в свой аккаунт
3. Выберите ваш проект

### Шаг 2: Откройте SQL Editor
1. В левом меню найдите **"SQL Editor"** (или **"SQL"**)
2. Нажмите на него

### Шаг 3: Создайте новый запрос
1. Нажмите кнопку **"New query"** (или **"+"**)
2. Скопируйте **весь** содержимое файла `supabase_mlm_referral_system.sql`
3. Вставьте в редактор SQL

### Шаг 4: Выполните запрос
1. Нажмите кнопку **"Run"** (или `Ctrl+Enter` / `Cmd+Enter`)
2. Дождитесь выполнения (обычно несколько секунд)
3. Проверьте результат - должно быть сообщение об успешном выполнении

### Шаг 5: Проверка
Выполните этот запрос для проверки:

```sql
-- Проверка создания таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('referral_tree', 'referral_rewards');

-- Должно вернуть 2 строки: referral_tree и referral_rewards
```

---

## 🎯 Способ 2: Через Supabase CLI

Если у вас установлен Supabase CLI:

```bash
# 1. Убедитесь, что вы в директории проекта
cd /Users/ghbi/Downloads/loyalitybot

# 2. Выполните SQL файл
supabase db execute --file supabase_mlm_referral_system.sql

# Или если нужно подключиться к удалённой БД:
supabase db execute --file supabase_mlm_referral_system.sql --project-id YOUR_PROJECT_ID
```

---

## 🎯 Способ 3: Через psql (PostgreSQL CLI)

Если у вас есть доступ к базе данных через psql:

```bash
# 1. Получите connection string из Supabase Dashboard
# Settings → Database → Connection string → URI

# 2. Выполните SQL файл
psql "postgresql://postgres:[YOUR-PASSWORD]@[YOUR-HOST]:5432/postgres" -f supabase_mlm_referral_system.sql
```

---

## ⚠️ Важные замечания

### Проверка перед выполнением
Убедитесь, что:
- ✅ У вас есть права администратора в Supabase проекте
- ✅ Вы работаете с правильной базой данных (не production, если это тест)
- ✅ У вас есть резервная копия базы данных (на всякий случай)

### Что создаётся
Миграция создаёт:
- ✅ 2 новые таблицы: `referral_tree`, `referral_rewards`
- ✅ 6 новых колонок в таблице `users`
- ✅ Индексы для оптимизации запросов
- ✅ Триггеры для автоматического обновления статистики
- ✅ Функции для генерации кодов и обновления статистики
- ✅ RLS политики для безопасности

### Если возникли ошибки

**Ошибка: "relation already exists"**
- Таблицы уже существуют - это нормально, если миграция уже выполнялась
- Можно пропустить или удалить существующие таблицы (осторожно!)

**Ошибка: "column already exists"**
- Колонки уже добавлены в таблицу `users`
- Можно пропустить эти строки или использовать `IF NOT EXISTS` (уже добавлено в SQL)

**Ошибка: "permission denied"**
- У вас нет прав администратора
- Обратитесь к администратору проекта

---

## ✅ Проверка после выполнения

Выполните эти запросы для проверки:

```sql
-- 1. Проверка таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('referral_tree', 'referral_rewards');

-- 2. Проверка колонок в users
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'users' 
AND column_name IN ('referral_code', 'referred_by_chat_id', 'total_referrals', 'active_referrals', 'total_referral_earnings', 'referral_level');

-- 3. Проверка индексов
SELECT indexname 
FROM pg_indexes 
WHERE tablename IN ('referral_tree', 'referral_rewards', 'users')
AND indexname LIKE '%referral%';

-- 4. Проверка функций
SELECT routine_name 
FROM information_schema.routines 
WHERE routine_schema = 'public' 
AND routine_name IN ('generate_referral_code', 'update_referral_stats', 'update_referral_level');
```

Все запросы должны вернуть результаты.

---

## 🚀 После выполнения миграции

1. **Перезапустите боты** (если они запущены):
   ```bash
   ./restart_bots.sh
   ```

2. **Протестируйте реферальную систему**:
   - Зарегистрируйте тестового пользователя
   - Используйте команду `/referral` в боте
   - Попробуйте зарегистрировать другого пользователя по реферальной ссылке

3. **Проверьте логи** на наличие ошибок

---

## 📝 Примечания

- Миграция использует `IF NOT EXISTS` и `IF EXISTS`, поэтому её можно выполнять несколько раз безопасно
- Все существующие данные сохранятся
- Новые пользователи автоматически получат реферальные коды при первом использовании команды `/referral`

