# 💱 КОНВЕРТАЦИЯ ПРОМО-БАЛЛОВ В БАЛЛЫ ЛОЯЛЬНОСТИ

## 📋 ОПИСАНИЕ

Система позволяет участникам лидерборда, которые не получили призы, конвертировать свои промо-баллы (баллы лидерборда) в обычные баллы системы лояльности по специальному курсу в конце промо-периода.

---

## 🎯 КАК ЭТО РАБОТАЕТ

### Условия конвертации:

1. ✅ Период лидерборда завершён и призы распределены
2. ✅ Участник **не получил приз** (не в топ-10 или получил приз типа 'none')
3. ✅ У участника есть баллы лидерборда (> 0)
4. ✅ Конвертация включена для периода (`points_conversion_enabled = true`)
5. ✅ Баллы ещё не были конвертированы

### Курс конвертации:

- **По умолчанию:** 10% (10 баллов лидерборда = 1 балл лояльности)
- **Настраивается:** Для каждого периода можно установить свой курс
- **Хранится:** В поле `points_conversion_rate` таблицы `leaderboard_periods`

**Пример:**
- Баллы лидерборда: 1000
- Курс: 10%
- Получено баллов: 100

---

## 📊 СТРУКТУРА БД

### Обновления таблицы `leaderboard_periods`:

```sql
ALTER TABLE leaderboard_periods 
ADD COLUMN points_conversion_rate NUMERIC(5, 2) DEFAULT 10.0;

ALTER TABLE leaderboard_periods 
ADD COLUMN points_conversion_enabled BOOLEAN DEFAULT true;
```

### Обновления таблицы `leaderboard_rankings`:

```sql
ALTER TABLE leaderboard_rankings 
ADD COLUMN points_converted BOOLEAN DEFAULT false;

ALTER TABLE leaderboard_rankings 
ADD COLUMN points_converted_amount NUMERIC(10, 2) DEFAULT 0;

ALTER TABLE leaderboard_rankings 
ADD COLUMN points_converted_at TIMESTAMP;
```

---

## 🔧 ФУНКЦИИ

### 1. SQL функция `convert_leaderboard_points_to_loyalty_points()`

**Параметры:**
- `period_id_param` - ID периода
- `client_chat_id_param` - Chat ID клиента

**Возвращает:**
```json
{
  "success": true,
  "leaderboard_points": 1000.0,
  "conversion_rate": 10.0,
  "loyalty_points": 100.0,
  "message": "Конвертировано 1000.00 баллов лидерборда в 100.00 баллов лояльности (курс: 10.00%)"
}
```

**Проверки:**
- Период существует и завершён
- Конвертация разрешена
- Участник не получил приз
- Баллы ещё не конвертированы
- Есть баллы для конвертации

### 2. Python функция `convert_leaderboard_points_to_loyalty()`

**Расположение:** `supabase_manager.py`

**Что делает:**
1. Вызывает SQL функцию конвертации
2. Начисляет баллы на счёт пользователя
3. Записывает транзакцию
4. Обновляет баланс

### 3. Python функция `get_completed_periods_for_user()`

**Расположение:** `supabase_manager.py`

**Что делает:**
- Получает все завершённые периоды, где пользователь участвовал
- Определяет, может ли пользователь конвертировать баллы
- Возвращает список периодов с информацией о конвертации

---

## 💬 КОМАНДЫ ДЛЯ КЛИЕНТОВ

### Команда `/convert_points`

**Алиасы:** `/конвертировать`, `/convert`

**Что делает:**
1. Показывает список доступных периодов для конвертации
2. Если один период - сразу конвертирует
3. Если несколько - показывает список для выбора

**Пример использования:**
```
/convert_points
```

**Ответ:**
```
📊 Выберите период для конвертации:

• Ноябрь 2025
  Баллы: 1000.00 → 100.00 (курс: 10%)

[🔄 Ноябрь 2025 (100 баллов)]
```

---

## 🔔 АВТОМАТИЧЕСКИЕ УВЕДОМЛЕНИЯ

После завершения периода и распределения призов, участники, которые не получили призы, автоматически получают уведомление:

```
🎉 Период лидерборда завершён!

📊 Период: Ноябрь 2025
🎯 Ваши баллы: 1000.00

💱 Конвертация баллов

Вы можете конвертировать свои баллы лидерборда 
в обычные баллы системы лояльности!

💰 Вы получите: 100.00 баллов
📈 Курс: 10%

💡 Как конвертировать:
• Используйте команду /convert_points
• Или откройте меню спецвозможностей
```

---

## 🎨 ИНТЕГРАЦИЯ С МЕНЮ СПЕЦВОЗМОЖНОСТЕЙ

В меню "⭐ Мои спецвозможности" автоматически появляется кнопка:

```
[💱 Конвертировать баллы (2)]
```

Где число в скобках - количество доступных периодов для конвертации.

Кнопка появляется только если есть периоды, где можно конвертировать баллы.

---

## ⚙️ НАСТРОЙКА КУРСА КОНВЕРТАЦИИ

### При создании периода:

```python
period_data = {
    'period_type': 'monthly',
    'period_name': 'Ноябрь 2025',
    'start_date': '2025-11-01',
    'end_date': '2025-11-30',
    'points_conversion_rate': 10.0,  # 10%
    'points_conversion_enabled': True,
    'prizes_config': {...}
}
```

### Изменение курса для существующего периода:

```sql
UPDATE leaderboard_periods
SET points_conversion_rate = 15.0  -- 15%
WHERE id = 1;
```

### Отключение конвертации:

```sql
UPDATE leaderboard_periods
SET points_conversion_enabled = false
WHERE id = 1;
```

---

## 📝 ПРИМЕРЫ СЦЕНАРИЕВ

### Сценарий 1: Участник не в топ-10

1. Период завершён
2. Участник набрал 500 баллов, занял 15 место
3. Получает уведомление о возможности конвертации
4. Использует `/convert_points`
5. Конвертирует 500 баллов → 50 баллов (курс 10%)

### Сценарий 2: Участник получил приз

1. Период завершён
2. Участник занял 3 место, получил приз
3. **Не получает** уведомление о конвертации
4. Конвертация недоступна

### Сценарий 3: Несколько периодов

1. Участник участвовал в 2 периодах
2. Оба завершены, призы не получены
3. Использует `/convert_points`
4. Видит список из 2 периодов
5. Выбирает нужный период для конвертации

---

## ✅ ЧЕКЛИСТ ТЕСТИРОВАНИЯ

- [ ] SQL миграция выполнена (`add_points_conversion.sql`)
- [ ] Период создан с курсом конвертации
- [ ] Период завершён и призы распределены
- [ ] Участник не получил приз
- [ ] Участник получил уведомление о конвертации
- [ ] Команда `/convert_points` работает
- [ ] Конвертация выполняется корректно
- [ ] Баллы начисляются на счёт
- [ ] Транзакция записывается
- [ ] Повторная конвертация невозможна
- [ ] Участники с призами не могут конвертировать
- [ ] Кнопка в меню спецвозможностей появляется

---

## 🚀 УСТАНОВКА

1. **Выполнить SQL миграцию:**
```bash
psql -d your_database -f add_points_conversion.sql
```

2. **При создании периода указать курс:**
```python
points_conversion_rate = 10.0  # 10%
points_conversion_enabled = True
```

3. **Готово!** Система автоматически:
   - Отправит уведомления после распределения призов
   - Позволит конвертировать баллы через команду
   - Покажет кнопку в меню спецвозможностей

---

**Готово к использованию! 🎉**






