# 📋 Как найти и проверить логи

## 🔍 Где находятся логи

### 1. Логи бота (client_handler.py)

**Путь:** `/tmp/client_bot.log`

**Проверка:**
```bash
# Последние 50 строк
tail -50 /tmp/client_bot.log

# Последние 100 строк
tail -100 /tmp/client_bot.log

# Следить за логами в реальном времени
tail -f /tmp/client_bot.log

# Поиск по ключевым словам
grep "contact_" /tmp/client_bot.log
grep "ERROR" /tmp/client_bot.log
grep "handle_new_user_start" /tmp/client_bot.log
```

### 2. Логи Vercel (фронтенд)

**Проверка через команду:**
```bash
cd frontend
npx vercel logs frontend-ec5v951w0-alekseis-projects-3b6bffb8.vercel.app
```

**Или через веб-интерфейс:**
1. Откройте https://vercel.com
2. Войдите в аккаунт
3. Выберите проект `frontend`
4. Перейдите в раздел "Deployments"
5. Выберите последний деплой
6. Нажмите "View Function Logs"

### 3. Логи в консоли браузера (для фронтенда)

**В Telegram Web App:**
1. Откройте приложение в Telegram
2. Нажмите на три точки (меню)
3. Выберите "Open in Browser" (если доступно)
4. Откройте DevTools (F12 или Cmd+Option+I)
5. Перейдите в раздел "Console"
6. Проверьте логи с префиксом `🚀🚀🚀` или `🔍 Debug`

**Или используйте удалённую отладку:**
- Для Android: chrome://inspect
- Для iOS: Safari → Develop → [ваше устройство]

## 📝 Что искать в логах

### В логах бота (`/tmp/client_bot.log`)

**При нажатии "Написать специалисту" должны быть:**
```
handle_new_user_start
contact_
contact_specialist
```

**Пример успешного лога:**
```
INFO - Клиент {chat_id} отправил сообщение специалисту {partner_chat_id}
INFO - Message saved: ID={message_id}
```

**Пример ошибки:**
```
ERROR - Ошибка обработки запроса
ERROR - Специалист не найден в системе
```

### В логах фронтенда (консоль браузера)

**Должны быть логи:**
```
🚀🚀🚀🚀🚀 handleContactSpecialist CALLED - DIAGNOSTIC MODE
🔍 Debug - Function version: v9-diagnostic
🔍 Debug - Bot username: mindbeaty
📱📱📱 Opening bot with contact data: {...}
🔗🔗🔗 Bot link: https://t.me/mindbeaty?start=contact_...
```

## 🔧 Полезные команды

### Проверка статуса бота
```bash
ps aux | grep client_handler
```

### Проверка размера лог файла
```bash
ls -lh /tmp/client_bot.log
```

### Очистка старых логов
```bash
# Создать резервную копию
cp /tmp/client_bot.log /tmp/client_bot.log.backup

# Очистить лог
> /tmp/client_bot.log
```

### Поиск ошибок в логах
```bash
grep -i "error\|exception\|traceback" /tmp/client_bot.log | tail -20
```

## 📋 Чеклист проверки логов

1. **Проверьте логи бота:**
   ```bash
   tail -50 /tmp/client_bot.log
   ```

2. **Проверьте, что бот запущен:**
   ```bash
   ps aux | grep client_handler
   ```

3. **Проверьте логи Vercel:**
   ```bash
   cd frontend
   npx vercel logs
   ```

4. **Проверьте консоль браузера:**
   - Откройте DevTools (F12)
   - Перейдите в Console
   - Проверьте логи с префиксом `🚀` или `🔍`

## 🎯 Что делать дальше

1. **Откройте приложение в Telegram**
2. **Нажмите "Написать специалисту"**
3. **Проверьте логи бота:**
   ```bash
   tail -50 /tmp/client_bot.log
   ```
4. **Сообщите, что видно в логах**
