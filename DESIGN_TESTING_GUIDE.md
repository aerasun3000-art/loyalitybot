# 🎨 Руководство по тестированию нового дизайна

## 📋 План действий

### Шаг 1: Сохранение текущего состояния

Перед началом работы с новым дизайном необходимо сохранить все текущие изменения:

```bash
# Проверить статус
git status

# Закоммитить текущие изменения (если нужно)
git add .
git commit -m "Save current state before design experiment"

# ИЛИ сохранить во временное хранилище (stash)
git stash push -m "Current state before design experiment"
```

### Шаг 2: Создание ветки для нового дизайна

```bash
# Создать и переключиться на новую ветку
git checkout -b design-experiment

# Или с более описательным названием
git checkout -b design-v2-sakura-redesign
```

### Шаг 3: Работа над новым дизайном

Теперь можно безопасно вносить изменения в:
- `frontend/src/styles/index.css` - глобальные стили
- `frontend/tailwind.config.js` - конфигурация Tailwind
- `frontend/src/pages/*.jsx` - страницы
- `frontend/src/components/*.jsx` - компоненты

### Шаг 4: Тестирование

```bash
# Запустить локальный сервер для тестирования
cd frontend
npm run dev
```

### Шаг 5: Варианты действий после тестирования

#### ✅ Если новый дизайн успешен:

```bash
# Закоммитить изменения
git add .
git commit -m "New design implementation"

# Переключиться на main
git checkout main

# Слить изменения из ветки дизайна
git merge design-experiment

# Отправить на сервер
git push origin main

# Удалить ветку (опционально)
git branch -d design-experiment
```

#### ❌ Если нужно откатиться:

```bash
# Просто переключиться обратно на main
git checkout main

# Все изменения останутся в ветке design-experiment
# Можно вернуться к ним позже или удалить:
git branch -D design-experiment
```

#### 🔄 Если нужно вернуться к старому состоянию:

```bash
# Переключиться на main
git checkout main

# Если использовали stash, восстановить:
git stash pop

# Или посмотреть список stash:
git stash list
```

## 🛠️ Быстрые команды

### Создать ветку и начать работу:
```bash
git checkout -b design-experiment && echo "Ветка создана, можно начинать работу"
```

### Вернуться к main (откат):
```bash
git checkout main
```

### Посмотреть различия между ветками:
```bash
git diff main..design-experiment
```

### Сохранить текущую работу и переключиться:
```bash
git stash && git checkout main
```

### Вернуться к работе над дизайном:
```bash
git checkout design-experiment && git stash pop
```

## 📝 Рекомендации

1. **Регулярно коммитить** изменения в ветке дизайна для сохранения прогресса
2. **Тестировать локально** перед слиянием с main
3. **Документировать изменения** - что именно изменилось в дизайне
4. **Создавать бэкапы** важных файлов перед большими изменениями

## 🎯 Структура файлов для изменения дизайна

Основные файлы для работы с дизайном:

- `frontend/src/styles/index.css` - глобальные CSS стили и переменные
- `frontend/tailwind.config.js` - конфигурация цветов и темы
- `frontend/src/pages/Home.jsx` - главная страница
- `frontend/src/pages/Services.jsx` - страница услуг
- `frontend/src/pages/Promotions.jsx` - страница акций
- `frontend/src/pages/Profile.jsx` - страница профиля
- `frontend/src/components/*.jsx` - переиспользуемые компоненты

## ⚠️ Важно

- **НЕ коммитить** изменения напрямую в `main` во время экспериментов
- **НЕ удалять** ветку дизайна до полного тестирования
- **Сохранять** текущее состояние перед началом работы





