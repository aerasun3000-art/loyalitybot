# Альтернативный способ подключения Git к Cloudflare Pages

**Проблема:** Проект создан через CLI (`wrangler pages deploy`), поэтому секция "Builds & deployments" отсутствует в Settings.

---

## ✅ РЕШЕНИЕ: Пересоздать проект через Dashboard с Git

Так как проект создан через CLI, нужно создать новый проект через Dashboard с Git интеграцией.

### ⚠️ ВАЖНО: Сохраните текущий URL!

Текущий URL проекта: `https://loyalitybot-frontend.pages.dev`

После пересоздания URL останется тем же, если вы используете то же имя проекта.

---

## 📋 ШАГ 1: Откройте создание нового проекта

1. Откройте https://dash.cloudflare.com/1e573db7d6be24be4e467cc02a9b8524/pages
2. Нажмите кнопку **"Create a project"** или **"Add project"**
3. Выберите **"Connect to Git"** (не "Upload assets")

---

## 📋 ШАГ 2: Подключите GitHub

1. Выберите **GitHub** как провайдер
2. Авторизуйтесь через GitHub (если еще не авторизованы)
3. Предоставьте доступ Cloudflare к репозиториям
4. Выберите репозиторий: **`aerasun3000-art/loyalitybot`**

---

## 📋 ШАГ 3: Настройте Build settings

После выбора репозитория откроется форма настройки:

### Заполните параметры:

| Параметр | Значение |
|----------|----------|
| **Project name** | `loyalitybot-frontend` (то же имя!) |
| **Production branch** | `main` |
| **Root directory** | `frontend` |
| **Build command** | `npm run build` |
| **Build output directory** | `dist` |
| **Node.js version** | `18` или `18.x` |

### ⚠️ ВАЖНО: Имя проекта должно быть `loyalitybot-frontend`!

Это важно, чтобы URL остался тем же.

---

## 📋 ШАГ 4: Настройте переменные окружения

После настройки Build settings, перед сохранением:

1. Найдите раздел **"Environment variables"** (может быть на этой же странице или в Settings после создания)
2. Добавьте переменные:

#### Production environment:

| Key | Value |
|-----|-------|
| `VITE_SUPABASE_URL` | `https://gynpvfchojnyoirosysj.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imd5bnB2ZmNob2pueW9pcm9zeXNqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjA2NTExMzksImV4cCI6MjA3NjIyNzEzOX0.Lw-DG19z7ZNoiu1k0jLO8A7SmylhHPfA596qg0a88qk` |
| `VITE_API_URL` | `https://loyalitybot-api.aerasun3000.workers.dev` |
| `VITE_APP_NAME` | `LoyalityBot` |
| `VITE_APP_VERSION` | `0.1.0` |

---

## 📋 ШАГ 5: Сохраните и удалите старый проект

### 5.1: Создайте новый проект

Нажмите **"Save and deploy"** или **"Create project"**

Cloudflare автоматически:
- ✅ Создаст проект с Git интеграцией
- ✅ Запустит первый билд
- ✅ Задеплоит фронтенд

### 5.2: Удалите старый проект (опционально)

После успешного деплоя нового проекта:

1. Перейдите в **Pages** → выберите старый проект `loyalitybot-frontend`
2. Перейдите в **Settings** → в самом низу страницы
3. Найдите секцию **"Delete project"**
4. Нажмите **"Delete"** и подтвердите

⚠️ **ВАЖНО:** Удаляйте старый проект ТОЛЬКО после того, как новый проект успешно задеплоен!

---

## ✅ ПРОВЕРКА

После создания нового проекта:

1. Перейдите в **Pages** → `loyalitybot-frontend`
2. Перейдите в **Settings**
3. Теперь должна появиться секция **"Builds & deployments"**!
4. Проверьте, что Git подключен (должен быть виден репозиторий)

---

## 🔄 Альтернатива: Оставить старый проект

Если не хотите пересоздавать проект:

1. Оставьте текущий проект как есть
2. Используйте CLI для деплоя:
   ```bash
   cd frontend
   npm run build
   wrangler pages deploy dist --project-name=loyalitybot-frontend
   ```
3. Но тогда не будет автоматических билдов из Git

---

## 📝 Примечания

- **URL останется тем же**, если используете то же имя проекта
- **Переменные окружения** нужно будет настроить заново в новом проекте
- **FRONTEND_URL в боте** не нужно менять (URL останется тем же)
- **Старый проект** можно удалить после успешного деплоя нового

---

**Дата:** 2026-01-18
