# 📋 Быстрая проверка логов

## 🔍 Где находятся логи

### 1. Логи бота (самый важный)

**Путь:** `/tmp/client_bot.log`

**Быстрая проверка:**
```bash
tail -50 /tmp/client_bot.log
```

**Мониторинг в реальном времени:**
```bash
tail -f /tmp/client_bot.log
```
*(Нажмите Ctrl+C для остановки)*

### 2. Проверка статуса бота

```bash
ps aux | grep client_handler
```

Должен быть **только 1 процесс**!

## 📝 Что искать в логах

### При нажатии "Написать специалисту" должны появиться:

**В логах бота:**
```
handle_new_user_start
contact_
contact_specialist
```

**Пример успешного лога:**
```
INFO - Клиент {chat_id} отправил сообщение специалисту {partner_chat_id}
INFO - Message saved: ID={message_id}
```

### Если в логах НЕТ `contact_`:
- ❌ Функция не вызывается
- ❌ Ссылка на бота не открывается
- ❌ Параметр не передаётся

## 🎯 Что проверить СЕЙЧАС

### 1. Откройте терминал и выполните:
```bash
tail -f /tmp/client_bot.log
```

### 2. В Telegram:
1. Откройте приложение
2. Нажмите "Написать специалисту"

### 3. Смотрите, что появляется в логах:

**Если появляется `contact_`:**
- ✅ Функция работает, бот получает запрос
- 🔍 Проверьте, что бот обрабатывает запрос правильно

**Если НЕ появляется `contact_`:**
- ❌ Функция не вызывается или ссылка не открывается
- 🔍 Проверьте, появляется ли alert в браузере

## ✅ Текущий статус

- ✅ Бот запущен (1 экземпляр)
- ✅ Код содержит `mindbeaty` (проверено в локальном билде)
- ✅ Фронтенд задеплоен на Vercel
- ❓ Нужно проверить, вызывается ли функция

## 📋 Следующие шаги

1. **Запустите мониторинг логов:**
   ```bash
   tail -f /tmp/client_bot.log
   ```

2. **Откройте приложение в Telegram**

3. **Нажмите "Написать специалисту"**

4. **Сообщите, что появилось в логах**

