# 🚀 Быстрый запуск локального сервера

## Проблема: Не открывается localhost

### Решение 1: Остановить старый процесс и запустить заново

```bash
# 1. Перейти в папку frontend
cd /Users/alekseysanzheev/Desktop/loyalitybot/frontend

# 2. Остановить процесс на порту 3000 (если занят)
lsof -ti:3000 | xargs kill -9 2>/dev/null

# 3. Запустить dev сервер
npm run dev
```

### Решение 2: Использовать другой порт

Если порт 3000 занят, можно изменить в `vite.config.js`:

```javascript
server: {
  port: 5173,  // или любой другой свободный порт
  host: true
}
```

### Решение 3: Проверить зависимости

```bash
cd /Users/alekseysanzheev/Desktop/loyalitybot/frontend

# Переустановить зависимости
rm -rf node_modules package-lock.json
npm install

# Запустить
npm run dev
```

---

## ✅ После запуска

Dev сервер должен быть доступен по адресу:
- **http://localhost:3000** (или порт, указанный в vite.config.js)
- **http://127.0.0.1:3000**

---

## 🔍 Проверка

### 1. Проверить, что сервер запущен:
```bash
lsof -ti:3000
```
Если выводит число (PID процесса) - сервер работает ✅

### 2. Проверить в браузере:
Откройте: `http://localhost:3000`

### 3. Проверить логи:
В терминале, где запущен `npm run dev`, должны быть логи:
```
  VITE v5.0.8  ready in XXX ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

---

## 🐛 Частые проблемы

### Проблема: "Port 3000 is already in use"
**Решение:**
```bash
# Найти и остановить процесс
lsof -ti:3000 | xargs kill -9
# Или использовать другой порт
```

### Проблема: "Cannot find module"
**Решение:**
```bash
npm install
```

### Проблема: "EADDRINUSE: address already in use"
**Решение:**
```bash
# Найти процесс
lsof -i :3000
# Остановить его
kill -9 <PID>
```

### Проблема: Страница не загружается
**Решение:**
1. Проверить, что сервер запущен
2. Проверить URL в браузере
3. Проверить консоль браузера на ошибки (F12)
4. Проверить логи в терминале

---

## 📝 Команды для быстрого запуска

```bash
# Полный перезапуск
cd /Users/alekseysanzheev/Desktop/loyalitybot/frontend
lsof -ti:3000 | xargs kill -9 2>/dev/null
npm run dev
```

---

## 🌐 Тестирование формы регистрации

После запуска сервера откройте:
- **Форма регистрации:** http://localhost:3000/partner/apply
- **Главная страница:** http://localhost:3000/

---

**Дата:** 2025-01-XX









