# 🔍 Задание для проверки базы данных

## Цель: Понять почему иконки услуг отображаются как эмодзи

## Задание 1: Проверить структуру таблицы `services`

### SQL запрос:
```sql
SELECT 
  column_name, 
  data_type, 
  is_nullable
FROM information_schema.columns 
WHERE table_name = 'services'
ORDER BY ordinal_position;
```

**Что ищем:**
- Есть ли поле `icon`?
- Какой тип данных у полей `title`, `name`, `icon`?

---

## Задание 2: Проверить данные в таблице `services`

### SQL запрос:
```sql
SELECT 
  id,
  title,
  name,
  icon,
  approval_status,
  is_active
FROM services
WHERE approval_status = 'Approved' 
  AND is_active = true
LIMIT 20;
```

**Что проверить:**
1. **Поле `title`** - какие названия услуг?
   - Примеры: "Маникюр", "Прическа", "Массаж" и т.д.
   
2. **Поле `icon`** - что там хранится?
   - Есть ли эмодзи? (💅, ✂️, 💆 и т.д.)
   - Или строки типа "manicure", "hairstyle"?
   - Или NULL?

3. **Поле `name`** - если есть, что там?

---

## Задание 3: Найти услуги с эмодзи в поле `icon`

### SQL запрос:
```sql
SELECT 
  id,
  title,
  icon,
  CASE 
    WHEN icon ~ '[^\x00-\x7F]' THEN 'Contains non-ASCII'
    ELSE 'ASCII only'
  END as icon_type
FROM services
WHERE approval_status = 'Approved' 
  AND is_active = true
  AND icon IS NOT NULL;
```

**Что ищем:**
- Есть ли услуги где `icon` содержит не-ASCII символы (эмодзи)?

---

## Задание 4: Проверить примеры услуг

### SQL запрос:
```sql
SELECT 
  title,
  icon,
  LENGTH(icon) as icon_length,
  icon ~ '[^\x00-\x7F]' as has_emoji
FROM services
WHERE approval_status = 'Approved' 
  AND is_active = true
ORDER BY created_at DESC
LIMIT 10;
```

---

## Что нужно прислать:

1. **Структура таблицы** - какие поля есть
2. **Примеры данных** - скриншот или текст первых 5-10 записей
3. **Ответы на вопросы:**
   - Есть ли поле `icon` в таблице `services`?
   - Что в нем хранится? (эмодзи, строки, NULL)
   - Какие названия услуг в поле `title`?

---

## Возможные варианты проблемы:

### Вариант A: Поле `icon` содержит эмодзи
**Решение:** Нужно очистить поле `icon` или игнорировать его полностью

### Вариант B: Поле `icon` содержит строки типа "💅" 
**Решение:** Нужно заменить на правильные строки типа "manicure"

### Вариант C: Поле `icon` NULL или пустое
**Решение:** Тогда проблема в функции `getServiceIcon()` - она не находит совпадение

### Вариант D: Поле `title` содержит нестандартные названия
**Решение:** Нужно расширить функцию `getServiceIcon()` для распознавания

---

## Дополнительно: Проверить функцию `getServiceIcon`

Если в базе есть услуги с названиями, которые не распознаются, нужно добавить их в `serviceIcons.js`.

**Пример:** Если есть услуга "Наращивание ногтей", но функция ищет только "маникюр", она не найдет совпадение.

---

## После проверки:

Пришлите результаты, и я:
1. Найду точную причину проблемы
2. Предложу конкретное решение
3. Дам SQL запрос для исправления данных (если нужно)

