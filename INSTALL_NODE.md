# 📦 Установка Node.js на macOS

## Вариант 1: Официальный установщик (РЕКОМЕНДУЕТСЯ)

1. Откройте в браузере: **https://nodejs.org/**
2. Нажмите кнопку **"Download Node.js (LTS)"** - это стабильная версия
3. Скачайте файл `.pkg` для macOS
4. Запустите установщик и следуйте инструкциям
5. **Перезапустите терминал** (закройте и откройте заново)
6. Проверьте установку:
   ```bash
   node --version
   npm --version
   ```

## Вариант 2: Через Homebrew

### Шаг 1: Установите Homebrew (если еще не установлен)

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### Шаг 2: Установите Node.js

```bash
brew install node
```

### Шаг 3: Проверьте установку

```bash
node --version
npm --version
```

## После установки Node.js

1. Перейдите в папку frontend:
   ```bash
   cd /Users/verasanzheeva/Downloads/loyalitybot/frontend
   ```

2. Установите зависимости:
   ```bash
   npm install
   ```

3. Запустите dev сервер:
   ```bash
   npm run dev
   ```

4. Откройте в браузере:
   - **Список акций:** http://localhost:5173/promotions
   - **Главная:** http://localhost:5173/

---

## ⚠️ Если после установки команды не работают

1. **Закройте и откройте терминал заново** - это обновит PATH
2. Или выполните:
   ```bash
   source ~/.zshrc
   ```

## ✅ Проверка

После установки выполните:
```bash
node --version  # Должно показать версию, например: v20.10.0
npm --version   # Должно показать версию, например: 10.2.3
```

Если обе команды работают - Node.js установлен успешно! 🎉

