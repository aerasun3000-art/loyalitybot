# 📅 Интеграция с Google Calendar - Руководство по настройке

## 📋 Обзор

Интеграция с Google Calendar позволяет автоматически создавать события для созвонов с потенциальными партнерами, синхронизировать статусы и отправлять напоминания.

## ✅ Что реализовано

1. ✅ Автоматическое создание событий в Google Calendar
2. ✅ Планирование созвонов через админ-бота
3. ✅ Быстрое планирование (сегодня, завтра, через 3 дня)
4. ✅ Хранение ссылок на видеозвонки
5. ✅ Синхронизация статусов между системой и календарем
6. ✅ Просмотр предстоящих созвонов

---

## 🔧 Шаг 1: Настройка Google Cloud Project

### 1.1. Создание проекта

1. Откройте [Google Cloud Console](https://console.cloud.google.com)
2. Нажмите на выпадающий список проектов вверху
3. Нажмите "New Project"
4. Введите название проекта (например: `loyaltybot-calendar`)
5. Нажмите "Create"

### 1.2. Включение Google Calendar API

1. В меню слева выберите "APIs & Services" → "Library"
2. В поиске введите "Google Calendar API"
3. Нажмите на результат
4. Нажмите кнопку "Enable"

---

## 🔑 Шаг 2: Создание Service Account

### 2.1. Создание Service Account

1. В меню выберите "APIs & Services" → "Credentials"
2. Нажмите "Create Credentials" → "Service Account"
3. Введите:
   - **Service account name**: `calendar-service`
   - **Service account ID**: автоматически заполнится
   - **Description**: `Service account for LoyaltyBot calendar integration`
4. Нажмите "Create and Continue"
5. В разделе "Grant this service account access to project":
   - Роль: выберите "Editor" (или создайте кастомную роль с правами на Calendar)
   - Нажмите "Continue"
6. Нажмите "Done"

### 2.2. Создание ключа

1. Найдите созданный Service Account в списке
2. Нажмите на него
3. Перейдите на вкладку "Keys"
4. Нажмите "Add Key" → "Create new key"
5. Выберите формат "JSON"
6. Нажмите "Create"
7. **ВАЖНО**: JSON файл автоматически скачается. Сохраните его в безопасном месте!

---

## 📧 Шаг 3: Настройка календаря

### 3.1. Создание календаря (опционально)

Рекомендуется создать отдельный календарь для встреч с партнерами:

1. Откройте [Google Calendar](https://calendar.google.com)
2. Слева нажмите "+" рядом с "Other calendars"
3. Выберите "Create new calendar"
4. Введите:
   - **Name**: `LoyaltyBot - Partner Calls`
   - **Description**: `Созвоны с потенциальными партнерами`
5. Нажмите "Create calendar"

### 3.2. Предоставление доступа Service Account

1. В настройках календаря (найдите календарь в списке слева → "Settings and sharing")
2. Прокрутите вниз до раздела "Share with specific people"
3. Нажмите "Add people"
4. Введите email вашего Service Account (найдите его в JSON файле в поле `client_email`)
   - Формат: `calendar-service@your-project-id.iam.gserviceaccount.com`
5. Выберите права доступа: **"Make changes to events"**
6. Нажмите "Send"

**Альтернативный способ** (для персонального календаря):
1. Скопируйте email Service Account из JSON файла
2. В настройках вашего основного календаря добавьте этот email с правами "Make changes to events"

### 3.3. Получение Calendar ID

1. В настройках календаря найдите раздел "Integrate calendar"
2. Скопируйте **Calendar ID**:
   - Для кастомного календаря: формат `xxxxx@group.calendar.google.com`
   - Для персонального: это будет ваш email адрес

---

## 📁 Шаг 4: Настройка проекта

### 4.1. Установка зависимостей

```bash
pip install google-api-python-client google-auth-httplib2 google-auth-oauthlib
```

### 4.2. Размещение файла credentials

1. Поместите скачанный JSON файл в безопасное место:
   ```bash
   mkdir -p /path/to/secure/directory
   mv ~/Downloads/your-project-id-xxxxx.json /path/to/secure/directory/
   ```

2. Установите правильные права доступа:
   ```bash
   chmod 600 /path/to/secure/directory/your-project-id-xxxxx.json
   ```

### 4.3. Настройка переменных окружения

Откройте файл `.

` и добавьте:

```env
# Google Calendar Integration
GOOGLE_CALENDAR_CREDENTIALS_PATH=/path/to/secure/directory/your-project-id-xxxxx.json
GOOGLE_CALENDAR_ID=your-calendar-id@group.calendar.google.com
```

**Пример:**
```env
GOOGLE_CALENDAR_CREDENTIALS_PATH=/home/user/secure/calendar-service-key.json
GOOGLE_CALENDAR_ID=loyaltybot-calls@group.calendar.google.com
```

---

## 🗄️ Шаг 5: Выполнение SQL миграции

Выполните SQL миграцию для добавления полей календаря в базу данных:

1. Откройте Supabase Dashboard
2. Перейдите в SQL Editor
3. Откройте файл `supabase_calendar_integration.sql`
4. Скопируйте весь содержимое
5. Вставьте в SQL Editor
6. Нажмите "Run" (или Ctrl+Enter)

Проверка:
```sql
SELECT calendar_event_id, meeting_link, call_scheduled_date 
FROM instagram_outreach 
WHERE outreach_status = 'CALL_SCHEDULED' 
LIMIT 1;
```

---

## ✅ Шаг 6: Проверка работы

### 6.1. Перезапуск админ-бота

```bash
pkill -f admin_bot.py
nohup python3 admin_bot.py > admin_bot_output.log 2>&1 &
```

### 6.2. Тестирование

1. Откройте админ-бота в Telegram
2. Отправьте: `/admin`
3. Выберите: `📱 Instagram Outreach`
4. Найдите контакт со статусом `INTERESTED`
5. Нажмите кнопку планирования (если доступна)
6. Или используйте быстрые опции планирования

---

## 🎯 Использование

### Планирование созвона

#### Через админ-бота:

1. **Быстрое планирование:**
   - Откройте меню Outreach
   - Найдите контакт
   - Выберите одно из быстрых времен:
     - Сегодня 14:00
     - Завтра 10:00
     - Завтра 15:00
     - Через 3 дня 14:00

2. **Ручной ввод времени:**
   - Выберите "✏️ Ввести вручную"
   - Введите дату и время в формате: `DD.MM.YYYY HH:MM`
   - Выберите длительность (30/45/60 минут)

### Просмотр предстоящих созвонов

1. В меню Outreach нажмите: `📅 Предстоящие созвоны`
2. Увидите список всех запланированных встреч с:
   - Именем партнера
   - Временем встречи
   - Ссылкой на видеозвонок
   - Ссылкой на событие в календаре

---

## 🔍 Устранение неполадок

### Проблема: "Calendar Manager недоступен"

**Решение:**
1. Проверьте, что пути к файлу credentials правильные:
   ```bash
   cat $GOOGLE_CALENDAR_CREDENTIALS_PATH
   ```

2. Проверьте, что файл существует и доступен:
   ```bash
   ls -la /path/to/credentials.json
   ```

3. Проверьте логи бота:
   ```bash
   tail -50 admin_bot_output.log | grep -i calendar
   ```

### Проблема: "Permission denied" при создании события

**Решение:**
1. Убедитесь, что Service Account добавлен в календарь с правами "Make changes to events"
2. Проверьте правильность Calendar ID
3. Убедитесь, что календарь существует

### Проблема: События не создаются

**Решение:**
1. Проверьте, что Google Calendar API включен в проекте
2. Проверьте логи:
   ```bash
   tail -100 admin_bot_output.log | grep -i "calendar\|error"
   ```

3. Проверьте формат JSON файла credentials

---

## 📚 Дополнительная информация

### Структура данных в базе

Новые поля в таблице `instagram_outreach`:
- `calendar_event_id` - ID события в Google Calendar
- `meeting_link` - Ссылка на видеозвонок
- `meeting_duration` - Длительность встречи в минутах
- `meeting_type` - Тип встречи (call, video_call, in_person, other)
- `reminder_sent` - Отправлено ли напоминание
- `calendar_synced_at` - Время последней синхронизации

### Автоматические напоминания

События в Google Calendar автоматически настроены на напоминания:
- Email за 24 часа до встречи
- Popup за 30 минут до встречи

Можно настроить дополнительные напоминания в настройках календаря.

---

## 🎉 Готово!

Интеграция с Google Calendar настроена и готова к использованию!

**Следующие шаги:**
1. Протестируйте планирование первого созвона
2. Проверьте, что события создаются в календаре
3. Убедитесь, что ссылки на встречи работают
4. Настройте автоматические напоминания (опционально)

---

*Последнее обновление: Декабрь 2025*












