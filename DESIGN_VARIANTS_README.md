# 🎨 Руководство по вариантам дизайна

## 📋 Обзор

Реализована система тестирования нескольких вариантов дизайна в стиле **Modern Flat Design / Card-based UI** с возможностью переключения между вариантами.

## 🎯 Доступные варианты

### 1. **Sakura** (по умолчанию)
- Оригинальный дизайн с Sakura палитрой
- Розовые акценты
- Градиенты и фоновые изображения
- Полная функциональность

### 2. **Orange** (Modern Flat Design)
- Яркий оранжевый акцентный цвет
- Чистый белый фон
- Карточный дизайн с тенями
- Минималистичный стиль

### 3. **Minimal** (Ultra Minimal)
- Серый акцентный цвет
- Максимально минималистичный дизайн
- Тонкие границы вместо теней
- Компактные элементы

## 🚀 Как использовать

### Переключение вариантов

В правом верхнем углу главной страницы появился переключатель вариантов дизайна. Просто нажмите на нужный вариант:
- **Sakura** - оригинальный дизайн
- **Orange** - оранжевый Modern Flat Design
- **Minimal** - минималистичный вариант

### Тестирование

1. Откройте приложение
2. Нажмите на кнопку варианта в правом верхнем углу
3. Страница автоматически переключится на выбранный вариант
4. Выбор сохраняется в localStorage

## 📁 Структура файлов

```
frontend/src/
├── store/
│   └── designVariantStore.js          # Хранилище выбранного варианта
├── components/
│   ├── DesignVariantSwitcher.jsx      # Переключатель вариантов
│   ├── cards/
│   │   ├── ServiceCategoryCard.jsx   # Карточка категории услуги
│   │   ├── PromotionCard.jsx          # Карточка акции
│   │   └── SalonCard.jsx             # Карточка салона
│   └── HomeVariants/
│       └── HomeFlatDesign.jsx        # Вариант главной страницы
└── pages/
    └── Home.jsx                       # Главная страница (с переключением)
```

## 🎨 Особенности каждого варианта

### Orange Variant
- **Цвета:**
  - Акцент: `orange-500` (#F97316)
  - Фон: белый
  - Текст: темно-серый
  
- **Элементы:**
  - Крупные карточки с тенями
  - Округлые углы (rounded-2xl)
  - Яркие кнопки
  - Поисковая строка с оранжевым акцентом

### Minimal Variant
- **Цвета:**
  - Акцент: `gray-800`
  - Фон: белый
  - Текст: серый
  
- **Элементы:**
  - Компактные карточки с границами
  - Меньшие отступы
  - Тонкие границы вместо теней
  - Минималистичные кнопки

### Sakura Variant
- **Цвета:**
  - Акцент: `sakura-accent` (#6D3C52)
  - Фон: с градиентами и изображениями
  - Текст: темный
  
- **Элементы:**
  - Полнофункциональный оригинальный дизайн
  - Градиенты и эффекты
  - Фоновые изображения

## 🔧 Технические детали

### Хранение состояния
Вариант дизайна сохраняется в `localStorage` через Zustand persist middleware.

### Условный рендеринг
В `Home.jsx` добавлена проверка:
```javascript
if (variant === 'orange' || variant === 'minimal') {
  return <HomeFlatDesign ... />
}
```

### Адаптивные стили
Каждый компонент карточки использует функцию `getVariantStyles()` для получения стилей в зависимости от выбранного варианта.

## 📝 Добавление нового варианта

1. Добавьте новый вариант в `designVariantStore.js`
2. Обновите `DesignVariantSwitcher.jsx` с новой кнопкой
3. Добавьте стили в `getVariantStyles()` каждого компонента карточки
4. Обновите условие в `Home.jsx` при необходимости

## 🎯 Следующие шаги

- [ ] Добавить варианты для других страниц (Services, Promotions)
- [ ] Создать варианты навигации
- [ ] Добавить анимации переключения
- [ ] Создать A/B тестирование вариантов

## ⚠️ Важно

- Все изменения находятся в ветке `design-experiment`
- Можно легко откатиться к оригинальному дизайну через Git
- Варианты не влияют на функциональность, только на визуальное представление

