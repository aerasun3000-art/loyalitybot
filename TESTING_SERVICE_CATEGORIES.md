# 🧪 План тестирования категорий услуг

## 📋 Порядок тестирования

### Этап 1: Подготовка базы данных
**Где:** Supabase Dashboard → SQL Editor

1. ✅ Выполнить SQL скрипт `setup_service_categories.sql`
   ```sql
   -- Проверить, что поля добавлены
   SELECT column_name, data_type 
   FROM information_schema.columns 
   WHERE table_name = 'partner_applications' 
   AND column_name = 'business_type';
   ```

2. ✅ Проверить индексы
   ```sql
   SELECT indexname, indexdef 
   FROM pg_indexes 
   WHERE tablename = 'partner_applications' 
   AND indexname LIKE '%business_type%';
   ```

---

### Этап 2: Локальное тестирование (localhost)

#### 2.1. Запуск фронтенда локально
```bash
cd frontend
npm run dev
```

#### 2.2. Тестирование формы регистрации партнера
**URL:** `http://localhost:5173/partner/apply` (или другой порт)

**Проверки:**
- [ ] Форма открывается без ошибок
- [ ] Поле "Категория услуг" отображается
- [ ] В выпадающем списке видны все 12 категорий
- [ ] Категории отображаются с эмодзи и названиями (RU/EN)
- [ ] Валидация работает (нельзя отправить без выбора категории)
- [ ] При отправке формы `business_type` сохраняется в БД

**Проверка в Supabase:**
```sql
SELECT chat_id, name, company_name, business_type, city, district 
FROM partner_applications 
ORDER BY created_at DESC 
LIMIT 1;
```

#### 2.3. Тестирование главной страницы
**URL:** `http://localhost:5173/`

**Проверки:**
- [ ] Главная страница загружается без ошибок
- [ ] Отображаются 8 плиток услуг (4x2 сетка)
- [ ] Эмодзи категорий отображаются корректно
- [ ] Названия категорий обрезаются, если длинные
- [ ] При клике на категорию открывается страница услуг

#### 2.4. Тестирование страницы услуг
**URL:** `http://localhost:5173/services`

**Проверки:**
- [ ] Услуги загружаются
- [ ] Эмодзи услуг определяются по `business_type` партнера
- [ ] Фильтрация по категориям работает (если реализована)

---

### Этап 3: Тестирование на деплое (Vercel/другой хостинг)

#### 3.1. Деплой фронтенда
```bash
cd frontend
npm run build
# Деплой на Vercel или другой хостинг
```

#### 3.2. Проверка после деплоя
**URL:** `https://ваш-домен.vercel.app/partner/apply`

**Проверки:**
- [ ] Форма работает так же, как на localhost
- [ ] Все категории загружаются
- [ ] Данные сохраняются в Supabase
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах Vercel

**Проверка главной страницы:**
- [ ] `https://ваш-домен.vercel.app/`
- [ ] Категории отображаются корректно
- [ ] Нет ошибок загрузки

---

### Этап 4: Тестирование через Telegram бота

#### 4.1. Подготовка
- Убедиться, что бот настроен на правильный URL формы
- Проверить, что `PARTNER_URL` в `bot.py` указывает на деплой

#### 4.2. Тестирование регистрации через бота
1. Открыть партнерский бот в Telegram
2. Нажать кнопку регистрации (открывает веб-форму)
3. Заполнить форму:
   - Имя
   - Телефон
   - Название компании
   - **Выбрать категорию услуг** ← НОВОЕ
   - Выбрать город
   - Выбрать район
4. Отправить форму

**Проверки:**
- [ ] Форма открывается в Telegram WebView
- [ ] Все поля отображаются корректно
- [ ] Категория услуг выбирается
- [ ] Форма отправляется успешно
- [ ] Показывается сообщение об успехе

#### 4.3. Проверка в админ-панели
**Где:** Админский бот или админ-дашборд

**Проверки:**
- [ ] Новая заявка видна в списке заявок
- [ ] В заявке отображается `business_type`
- [ ] При одобрении партнера `business_type` копируется в таблицу `partners`

**SQL проверка:**
```sql
-- Проверить заявку
SELECT * FROM partner_applications 
WHERE chat_id = 'YOUR_TEST_CHAT_ID';

-- После одобрения проверить партнера
SELECT * FROM partners 
WHERE chat_id = 'YOUR_TEST_CHAT_ID';
```

#### 4.4. Тестирование отображения услуг
1. Одобрить тестового партнера через админ-панель
2. Создать услугу для этого партнера
3. Открыть клиентский бот
4. Перейти на главную страницу

**Проверки:**
- [ ] Услуга отображается на главной странице
- [ ] Эмодзи услуги соответствует категории партнера (`business_type`)
- [ ] Название услуги отображается корректно

---

## 🔍 Дополнительные проверки

### Проверка данных в Supabase

#### Проверить структуру таблиц:
```sql
-- partner_applications
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_name = 'partner_applications'
ORDER BY ordinal_position;

-- partners
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_name = 'partners'
ORDER BY ordinal_position;
```

#### Проверить существующие заявки:
```sql
SELECT 
  chat_id,
  name,
  company_name,
  business_type,
  city,
  district,
  status,
  created_at
FROM partner_applications
ORDER BY created_at DESC
LIMIT 10;
```

#### Проверить партнеров:
```sql
SELECT 
  chat_id,
  name,
  company_name,
  business_type,
  city,
  district
FROM partners
ORDER BY chat_id
LIMIT 10;
```

---

## 🐛 Возможные проблемы и решения

### Проблема 1: Поле `business_type` не сохраняется
**Решение:**
- Проверить, что SQL скрипт выполнен
- Проверить RLS policies в Supabase
- Проверить консоль браузера на ошибки

### Проблема 2: Категории не отображаются в форме
**Решение:**
- Проверить импорт `getAllServiceCategories` в `PartnerApply.jsx`
- Проверить консоль браузера на ошибки
- Убедиться, что `serviceCategories` правильно экспортируется

### Проблема 3: Эмодзи не отображаются на главной
**Решение:**
- Проверить, что `business_type` копируется при одобрении партнера
- Проверить функцию `getCategoryByCode()`
- Проверить, что `partner.business_type` загружается в `getApprovedServices()`

### Проблема 4: Форма не открывается в Telegram
**Решение:**
- Проверить URL в `bot.py`
- Проверить настройки Telegram WebView
- Проверить CORS настройки на сервере

---

## ✅ Чек-лист финальной проверки

- [ ] SQL скрипт выполнен успешно
- [ ] Форма регистрации работает локально
- [ ] Форма регистрации работает на деплое
- [ ] Форма регистрации работает через Telegram
- [ ] Категории отображаются на главной странице
- [ ] Эмодзи услуг определяются по `business_type`
- [ ] Данные сохраняются в `partner_applications`
- [ ] Данные копируются в `partners` при одобрении
- [ ] Нет ошибок в консоли браузера
- [ ] Нет ошибок в логах сервера

---

## 📝 Примечания

1. **Тестирование на разных устройствах:**
   - Мобильный телефон (iOS/Android)
   - Планшет
   - Десктоп

2. **Тестирование на разных языках:**
   - Русский язык
   - Английский язык

3. **Тестирование с разными данными:**
   - Все 12 категорий
   - Разные города и районы
   - Онлайн-услуги (город "Все")

---

**Дата создания:** 2025-01-XX  
**Версия:** 1.0









