# 🚀 СИСТЕМА ГОТОВА К БЕТА-ТЕСТИРОВАНИЮ

## ✅ Выполненные проверки

Все основные компоненты системы проверены и работают:

- ✅ Все SQL миграции выполнены
- ✅ Все таблицы созданы (9 таблиц)
- ✅ Все функции работают
- ✅ Переменные окружения настроены
- ✅ Боты готовы к запуску

## 📋 Финальные шаги для запуска бета

### Шаг 1: Инициализация данных (SQL)

Выполните в Supabase SQL Editor:

```sql
-- Файл: init_beta_data.sql
-- Создаст первый период лидерборда и добавит промо-материалы
```

**Или вручную через админ-бот:**
1. `/start` → "🏆 Лидерборд" → "➕ Создать период"

### Шаг 2: Запуск всех ботов

**Автоматический запуск:**
```bash
./start_beta.sh
```

**Или вручную:**
```bash
source venv/bin/activate

# Клиентский бот
nohup python3 client_handler.py > client_handler_output.log 2>&1 &

# Партнёрский бот
nohup python3 bot.py > bot_output.log 2>&1 &

# Админский бот
nohup python3 admin_bot.py > admin_bot_output.log 2>&1 &
```

### Шаг 3: Проверка работы

**Тестирование системы:**
```bash
python3 test_beta_flow.py
```

**Проверка статуса ботов:**
```bash
ps aux | grep -E "(client_handler|bot\.py|admin_bot)" | grep python
```

## 🎯 Основные функции для тестирования

### 1. Реферальная программа
- Команда: `/referral`
- Функции: Реферальные ссылки, QR-коды, статистика, достижения

### 2. Промоутеры и UGC
- Статус промоутера: Автоматически при NPS = 10
- Команда: `/promoter` - статистика промоутера
- Команда: `/ugc_add <ссылка> <платформа>` - добавление контента
- Команда: `/ugc` - инструкция по добавлению

### 3. Лидерборд
- Команда: `/leaderboard` - просмотр рейтинга
- Управление: Через админ-бот (`/start` → "🏆 Лидерборд")

### 4. Модерация UGC (Админ)
- `/start` → "📸 Модерация UGC"
- Автоматические уведомления о новом контенте
- Одобрение/отклонение с начислением баллов

## 📊 Мониторинг

**Логи:**
```bash
# Клиентский бот
tail -f client_handler_output.log

# Партнёрский бот
tail -f bot_output.log

# Админский бот
tail -f admin_bot_output.log
```

**Статус в БД:**
```sql
-- Промоутеры
SELECT COUNT(*) FROM promoters;

-- UGC контент
SELECT status, COUNT(*) FROM ugc_content GROUP BY status;

-- Лидерборд
SELECT * FROM leaderboard_periods WHERE status = 'active';

-- Рефералы
SELECT COUNT(*) FROM referral_tree;
```

## 🔄 Автоматизация

**Автозапуск ботов (cron):**
```bash
# Добавьте в crontab (crontab -e)
@reboot cd /Users/ghbi/Downloads/loyalitybot && ./start_beta.sh
```

**Автоактивация периодов лидерборда:**
```sql
-- Настройте cron для вызова функции activate_upcoming_periods()
-- Или вызывайте вручную через админ-бот
```

## ⚠️ Известные ограничения

1. **QR-декодирование**: Требует libzbar для arm64 (опционально)
   - Партнёры могут вводить CLIENT_ID вручную
   
2. **Первый период**: Создаётся вручную через админ-бот или SQL

3. **Промо-материалы**: Добавляются через SQL или будут через админ-интерфейс

## 📝 Чеклист перед запуском бета

- [x] SQL миграции выполнены
- [x] Все таблицы созданы
- [x] Все функции работают
- [ ] Первый период лидерборда создан (`init_beta_data.sql`)
- [ ] Промо-материалы добавлены (`init_beta_data.sql`)
- [ ] Все боты запущены (`./start_beta.sh`)
- [ ] Тесты пройдены (`python3 test_beta_flow.py`)

## 🎉 Готово к тестированию!

Система полностью готова к бета-тестированию. Все основные функции реализованы и проверены.

**Следующие шаги:**
1. Выполните `init_beta_data.sql`
2. Запустите боты через `./start_beta.sh`
3. Начните тестирование с реальными пользователями

---

**Версия:** Beta 1.0  
**Дата:** 2025-11-18  
**Статус:** ✅ Готово

