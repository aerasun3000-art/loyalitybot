# 🔔 Sentry Интеграция — Мониторинг Ошибок

## ✅ Что сделано

Sentry успешно интегрирован во все компоненты системы для отслеживания ошибок в реальном времени.

### Интегрированные компоненты:

1. **bot.py** (Partner Bot) — telebot с pyTelegramBotAPI
2. **admin_bot.py** (Admin Bot) — aiogram
3. **client_handler.py** (Client Bot) — telebot
4. **secure_api.py** (FastAPI) — FastAPI с специальной интеграцией

---

## 📦 Что установлено

### requirements.txt
```
sentry-sdk[fastapi]>=2.0.0
```

Включает:
- Базовый SDK для Python
- Интеграцию с FastAPI
- Автоматический трекинг ошибок

---

## 🔧 Конфигурация

### .env файл (добавьте эти переменные):

```bash
# Sentry DSN для мониторинга ошибок
SENTRY_DSN=https://bcb0ae7907d2c03b4be2507334a93db9@o4510368013877248.ingest.us.sentry.io/4510368037470208

# Окружение (production, staging, development)
SENTRY_ENVIRONMENT=production

# Версия приложения (опционально)
APP_VERSION=1.0.0
```

---

## 🚀 Установка и запуск

### 1. Установите зависимости

```bash
pip install -r requirements.txt
```

### 2. Добавьте SENTRY_DSN в .env

Скопируйте из `env.example.txt` или добавьте вручную:

```bash
echo "SENTRY_DSN=https://bcb0ae7907d2c03b4be2507334a93db9@o4510368013877248.ingest.us.sentry.io/4510368037470208" >> .env
echo "SENTRY_ENVIRONMENT=production" >> .env
```

### 3. Протестируйте интеграцию

```bash
python3 test_sentry.py
```

Вы должны увидеть:
```
✅ Sentry инициализирован
📤 Отправка тестовых событий в Sentry...
1️⃣ Отправка тестового сообщения (info)...
2️⃣ Отправка предупреждения (warning)...
3️⃣ Отправка тестовой ошибки (error)...
4️⃣ Отправка ошибки с дополнительным контекстом...
✅ Все тестовые события отправлены!
```

### 4. Проверьте в Sentry Dashboard

1. Откройте https://sentry.io/
2. Войдите в свой проект `loyalitybot-bots`
3. Через 10-30 секунд вы увидите тестовые события

---

## 📊 Что отслеживается

### Автоматически:
- ❌ Все необработанные исключения
- ❌ Критические ошибки (logger.error, logger.critical)
- 📊 Производительность (10% запросов)
- 🔍 Контекст ошибок (стек трейс, переменные)

### FastAPI дополнительно:
- 🌐 HTTP запросы и ответы
- ⏱️ Время выполнения эндпоинтов
- 📈 Метрики производительности API

### Фильтрация:
- Отправляются только ошибки уровня **error** и **fatal**
- Info и warning не загружают Sentry (экономия квоты)

---

## 🎯 Примеры использования

### В коде бота (автоматически):

```python
try:
    result = sm.execute_transaction(...)
except Exception as e:
    # Ошибка автоматически отправится в Sentry
    log_exception(logger, e, "Ошибка выполнения транзакции")
```

### Ручная отправка (если нужно):

```python
import sentry_sdk

# Отправить сообщение
sentry_sdk.capture_message("Важное событие", level="warning")

# Отправить ошибку с контекстом
with sentry_sdk.push_scope() as scope:
    scope.set_tag("bot", "partner")
    scope.set_context("transaction", {"amount": 1500, "client": "123"})
    sentry_sdk.capture_exception(error)
```

---

## 📈 Настройки производительности

### Текущие параметры:

```python
traces_sample_rate=0.1  # 10% транзакций
profiles_sample_rate=0.1  # 10% профилирования
```

**Почему 10%?**
- Снижает нагрузку на Sentry
- Экономит квоту бесплатного плана (5000 событий/месяц)
- 10% достаточно для выявления проблем

**Можно увеличить до 1.0 (100%)** на staging для полного мониторинга.

---

## 🔐 Безопасность

### Что Sentry НЕ видит:
- ❌ Токены ботов (фильтруются автоматически)
- ❌ API ключи Supabase (фильтруются)
- ❌ Пароли и секреты
- ❌ Персональные данные пользователей (по умолчанию)

### Фильтры данных:
Sentry автоматически скрывает:
- `TOKEN`, `PASSWORD`, `SECRET`, `API_KEY`
- Заголовки `Authorization`, `Cookie`
- Чувствительные параметры запросов

---

## 📬 Уведомления

### Настройка алертов в Sentry:

1. Откройте проект в Sentry
2. Settings → Alerts → Create Alert
3. Настройте условия:
   - **New Issue** — новая ошибка
   - **Error Rate** — больше 10 ошибок в минуту
   - **Spike in Errors** — резкий рост ошибок

4. Выберите канал уведомлений:
   - Email
   - Telegram (через интеграцию)
   - Slack
   - Discord

---

## 🧪 Тестирование

### Запустите тест:
```bash
python3 test_sentry.py
```

### Проверьте в Sentry:
1. Issues → должны появиться 4 события
2. Performance → транзакции (если включено)
3. Filters → по тегам и контексту

---

## 🆘 Troubleshooting

### Ошибки не появляются в Sentry?

1. **Проверьте DSN в .env:**
   ```bash
   grep SENTRY_DSN .env
   ```

2. **Проверьте, что Sentry инициализирован:**
   Запустите бота и найдите в логах:
   ```
   ✅ Sentry инициализирован для partner_bot
   ```

3. **Проверьте квоту:**
   - Бесплатный план: 5000 событий/месяц
   - Проверьте в Sentry → Settings → Subscription

4. **Проверьте интернет-соединение:**
   ```bash
   curl https://o4510368013877248.ingest.us.sentry.io/
   ```

### Слишком много событий?

Увеличьте фильтрацию в коде:

```python
before_send=lambda event, hint: event if event.get('level') == 'fatal' else None
```

Отправлять только критические ошибки.

---

## 📊 Метрики и статистика

### Что можно отслеживать:

1. **Stability** (стабильность):
   - Crash-free sessions
   - Error rate

2. **Performance** (производительность):
   - Response time
   - Throughput (запросов/сек)

3. **Business metrics** (бизнес-метрики):
   - Количество транзакций
   - Активные пользователи
   - Популярные команды

### Пример кастомных метрик:

```python
# В коде транзакций
with sentry_sdk.start_transaction(op="transaction", name="accrual"):
    result = sm.execute_transaction(...)
    sentry_sdk.set_measurement("transaction_amount", amount)
```

---

## 🔄 Следующие шаги

### 1. Настройте алерты
- Уведомления в Telegram при критических ошибках
- Email при новых типах ошибок

### 2. Интегрируйте React фронтенд
- Создайте проект `loyalitybot-frontend` в Sentry
- Установите `@sentry/react`
- Отслеживайте ошибки в браузере

### 3. Добавьте Prometheus (опционально)
- Для детальной статистики производительности
- Визуализация через Grafana

---

## 📚 Дополнительные ресурсы

- [Sentry Python SDK](https://docs.sentry.io/platforms/python/)
- [FastAPI Integration](https://docs.sentry.io/platforms/python/integrations/fastapi/)
- [Best Practices](https://docs.sentry.io/product/sentry-basics/integrate-backend/)

---

## ✅ Checklist

- [x] Установлен `sentry-sdk[fastapi]`
- [x] Добавлен SENTRY_DSN в .env
- [x] Интегрирован в bot.py
- [x] Интегрирован в admin_bot.py
- [x] Интегрирован в client_handler.py
- [x] Интегрирован в secure_api.py
- [x] Создан тестовый скрипт
- [ ] Запущен тест (выполните: `python3 test_sentry.py`)
- [ ] Настроены алерты в Sentry
- [ ] Добавлена интеграция фронтенда

---

**Поздравляем! 🎉 Sentry успешно интегрирован в ваш проект.**



