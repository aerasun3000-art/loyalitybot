# 🔍 Детальная диагностика проблемы с взломом

**Дата:** 2026-01-18  
**Проблема:** Новые пользователи все еще видят "OWNED BY @MISHADOX" после восстановления

---

## 🚨 Симптомы

Новые пользователи видят сообщение "OWNED BY @MISHADOX" вместо правильного приветствия.

---

## 🔍 Диагностика

### Шаг 1: Проверить логи Cloudflare Worker (КРИТИЧНО!)

**Запустите логи в реальном времени:**
```bash
cd cloudflare/workers/client-webhook
wrangler tail
```

**Что делать:**
1. Запустите `wrangler tail` в терминале
2. Попросите **НОВОГО** пользователя отправить `/start`
3. Смотрите логи в реальном времени

**Что искать:**

#### Если Worker получает запросы:
```
[handleStart] New user - FRONTEND_URL from env: ...
[handleStart] New user - Using URL: ...
```

#### Если Worker НЕ получает запросы:
- ❌ Нет никаких логов при отправке `/start`
- Это означает, что запросы НЕ доходят до Cloudflare Worker!

---

### Шаг 2: Проверить webhook

```bash
cd /Users/ghbi/Downloads/loyalitybot
TOKEN=$(grep "^TOKEN_CLIENT=" .env | cut -d'=' -f2 | tr -d '"' | tr -d "'" | tr -d ' ')
curl -s "https://api.telegram.org/bot${TOKEN}/getWebhookInfo" | python3 -m json.tool
```

**Что проверить:**
- `url` - должен быть `https://loyalitybot-client-webhook.aerasun3000.workers.dev`
- `pending_update_count` - должно быть 0 (или небольшое число)
- `last_error_message` - не должно быть ошибок

---

### Шаг 3: Проверить, нет ли других ботов/сервисов

**Проблема:** Возможно, есть другой сервис, который перехватывает запросы.

**Проверка через @BotFather:**
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Проверьте список ботов:
   - Есть ли дубликаты?
   - Все ли боты на вашем аккаунте?
4. Выберите клиентского бота (mindbeatybot)
5. Выберите "API Token"
6. Проверьте webhook:
   - Должен быть: `https://loyalitybot-client-webhook.aerasun3000.workers.dev`

---

### Шаг 4: Проверить, работает ли Worker вообще

**Тестовый запрос к Worker:**
```bash
curl -X POST https://loyalitybot-client-webhook.aerasun3000.workers.dev \
  -H "Content-Type: application/json" \
  -d '{
    "update_id": 999999999,
    "message": {
      "message_id": 1,
      "from": {
        "id": 123456789,
        "is_bot": false,
        "first_name": "Test"
      },
      "chat": {
        "id": 123456789,
        "type": "private"
      },
      "date": '$(date +%s)',
      "text": "/start"
    }
  }'
```

**Ожидаемый результат:**
- Если Worker работает, вернется JSON ответ (200 OK)
- Если нет - ошибка соединения или 401 (из-за Secret Token)

---

### Шаг 5: Проверить Secret Token

**Проблема:** Если Secret Token установлен в webhook, но Worker не может его проверить, запросы могут отклоняться.

**Проверка:**
1. Проверьте, что Secret Token в webhook совпадает с секретом в Cloudflare:
   ```bash
   # Secret Token сохранен в:
   cat /tmp/webhook_secret_token.txt
   ```

2. Проверьте валидацию в Worker:
   - Откройте `cloudflare/workers/client-webhook/index.js`
   - Убедитесь, что валидация настроена правильно

3. Временно отключите Secret Token для проверки:
   ```bash
   TOKEN=$(grep "^TOKEN_CLIENT=" .env | cut -d'=' -f2 | tr -d '"' | tr -d "'" | tr -d ' ')
   curl -X POST "https://api.telegram.org/bot${TOKEN}/setWebhook" \
     -H "Content-Type: application/json" \
     -d '{
       "url": "https://loyalitybot-client-webhook.aerasun3000.workers.dev",
       "drop_pending_updates": true
     }'
   ```

---

### Шаг 6: Проверить, нет ли другого токена

**Проблема:** Возможно, взломанный токен все еще используется где-то.

**Проверка:**
1. Отзовите ВСЕ токены бота через @BotFather
2. Создайте НОВЫЙ токен
3. Обновите токен везде:
   - `.env` файл
   - Cloudflare секреты
   - Передеплойте Worker
   - Переустановите webhook

---

### Шаг 7: Проверить, нет ли других webhook'ов

**Проблема:** Возможно, есть другие webhook'и, которые устанавливались ранее.

**Решение:** Полностью очистить и переустановить webhook:

```bash
TOKEN=$(grep "^TOKEN_CLIENT=" .env | cut -d'=' -f2 | tr -d '"' | tr -d "'" | tr -d ' ')

# Удалить webhook полностью
curl -X POST "https://api.telegram.org/bot${TOKEN}/deleteWebhook?drop_pending_updates=true"

# Подождать
sleep 5

# Установить новый webhook
SECRET_TOKEN=$(cat /tmp/webhook_secret_token.txt)
curl -X POST "https://api.telegram.org/bot${TOKEN}/setWebhook" \
  -H "Content-Type: application/json" \
  -d "{
    \"url\": \"https://loyalitybot-client-webhook.aerasun3000.workers.dev\",
    \"secret_token\": \"${SECRET_TOKEN}\",
    \"drop_pending_updates\": true
  }"
```

---

## 🔍 Возможные причины проблемы

### 1. Worker не получает запросы

**Признаки:**
- Нет логов в `wrangler tail`
- Webhook установлен правильно

**Причины:**
- Запросы перехватываются до Worker
- Другой webhook активен
- Проблема с DNS/сетью

**Решение:**
- Проверить webhook через `getWebhookInfo`
- Переустановить webhook
- Проверить логи Cloudflare Dashboard

---

### 2. Secret Token не совпадает

**Признаки:**
- Запросы приходят, но отклоняются (401 в логах)
- Worker получает запросы, но не обрабатывает

**Причины:**
- Secret Token в webhook не совпадает с секретом в Worker
- Валидация настроена неправильно

**Решение:**
- Переустановить Secret Token
- Проверить валидацию в Worker
- Временно отключить Secret Token для проверки

---

### 3. Другой сервис перехватывает запросы

**Признаки:**
- Webhook установлен правильно
- Worker не получает запросы
- Есть другие сервисы/боты с тем же токеном

**Причины:**
- Другой сервис установил свой webhook
- Кто-то получил доступ к токену
- Есть proxy/шлюз между Telegram и Worker

**Решение:**
- Отозвать токен и создать новый
- Проверить @BotFather на дубликаты
- Проверить активные сеансы Telegram

---

### 4. Старые запросы в очереди

**Признаки:**
- `pending_update_count` > 0
- Новые пользователи видят старые сообщения

**Решение:**
- Удалить webhook с `drop_pending_updates=true`
- Установить новый webhook
- Подождать несколько минут

---

## 📋 Чеклист диагностики

- [ ] Проверить логи Worker (`wrangler tail`)
- [ ] Проверить webhook через `getWebhookInfo`
- [ ] Проверить @BotFather на дубликаты
- [ ] Проверить Secret Token
- [ ] Проверить, работает ли Worker (тестовый запрос)
- [ ] Переустановить webhook с очисткой
- [ ] Отозвать и создать новый токен (если нужно)

---

## 🚨 СРОЧНЫЕ ДЕЙСТВИЯ

1. **Запустите логи Worker:**
   ```bash
   cd cloudflare/workers/client-webhook
   wrangler tail
   ```

2. **Попросите нового пользователя отправить `/start`**

3. **Смотрите логи:**
   - Если логов НЕТ → Worker не получает запросы
   - Если логи ЕСТЬ → Смотрите, что в них записано

4. **Сообщите результаты:**
   - Получает ли Worker запросы?
   - Что записано в логах?
   - Есть ли ошибки?

---

**Дата:** 2026-01-18  
**Приоритет:** 🔴 КРИТИЧЕСКИЙ
