# 🔗 Настройка username для переписки с мастерами

## 📋 Что было сделано

1. ✅ Добавлено поле `username` в таблицы `partners` и `partner_applications`
2. ✅ Обновлена форма регистрации партнёра - добавлено поле для username
3. ✅ Username автоматически получается из Telegram (если доступен)
4. ✅ Обновлён фронтенд для использования username из базы данных
5. ✅ При нажатии "Написать специалисту" открывается переписка напрямую с мастером по ссылке `https://t.me/username`

## 🗄️ Шаг 1: Добавить поле в базу данных

Выполните SQL скрипт в Supabase SQL Editor:

```sql
-- Файл: add_partner_contact_link.sql
ALTER TABLE partners 
ADD COLUMN IF NOT EXISTS username TEXT;

ALTER TABLE partner_applications 
ADD COLUMN IF NOT EXISTS username TEXT;

CREATE INDEX IF NOT EXISTS idx_partners_username ON partners(username) WHERE username IS NOT NULL;
CREATE INDEX IF NOT EXISTS idx_partner_applications_username ON partner_applications(username) WHERE username IS NOT NULL;

COMMENT ON COLUMN partners.username IS 'Telegram username мастера/специалиста для переписки (без @)';
COMMENT ON COLUMN partner_applications.username IS 'Telegram username мастера/специалиста для переписки (без @)';
```

## ✏️ Шаг 2: Заполнить username для существующих партнёров

Для каждого партнёра нужно добавить username мастера:

```sql
UPDATE partners 
SET username = 'master_username'
WHERE chat_id = 'PARTNER_CHAT_ID';
```

Замените:
- `master_username` — username мастера (без @)
- `PARTNER_CHAT_ID` — chat_id партнёра

## 📝 Примеры

### Пример 1: Психолог с username
```sql
UPDATE partners 
SET username = 'psychologist_master'
WHERE chat_id = '469123456';
```

### Пример 2: Мастер маникюра с username
```sql
UPDATE partners 
SET username = 'nail_master_pro'
WHERE chat_id = '789456123';
```

## 🔄 Как это работает при регистрации

1. **Новая регистрация партнёра:**
   - При заполнении формы регистрации система пытается автоматически получить username из Telegram
   - Если username не получен автоматически, партнёр может ввести его вручную в поле "Telegram username мастера"
   - Username сохраняется в `partner_applications`

2. **При одобрении заявки:**
   - Администратор одобряет заявку
   - Username автоматически копируется из `partner_applications` в `partners`

3. **Использование:**
   - При нажатии "Написать специалисту" формируется ссылка `https://t.me/username`
   - Открывается переписка с мастером напрямую

## ✅ Проверка

После настройки:
1. Откройте приложение в Telegram
2. Выберите услугу партнёра
3. Нажмите "Написать специалисту"
4. Должна открыться переписка с мастером по ссылке `https://t.me/username`

## ⚠️ Важно

- Если `username` не заполнен, пользователь увидит сообщение об ошибке и предложение использовать раздел "Мои сообщения" в боте
- Username должен быть в формате: только буквы, цифры и подчеркивания (5-32 символа)
- **В базе данных username сохраняется БЕЗ символа @** (система автоматически убирает @ при вводе)
- Можно вводить `vera_yoga03` или `@vera_yoga03` - оба варианта будут сохранены как `vera_yoga03`
- Ссылка `https://t.me/username` работает всегда, если у мастера есть username в Telegram

## 📝 Примеры username

✅ Правильные форматы:
- `vera_yoga03` → сохранится как `vera_yoga03`
- `@vera_yoga03` → сохранится как `vera_yoga03` (символ @ будет автоматически убран)
- `nail_master_pro` → сохранится как `nail_master_pro`

❌ Неправильные форматы:
- `@vera yoga03` (пробел недопустим)
- `vera` (слишком короткий, минимум 5 символов)
- `vera@yoga03` (символ @ внутри username недопустим)

