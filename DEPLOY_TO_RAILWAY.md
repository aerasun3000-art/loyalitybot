# 🚀 Деплой secure_api на Railway

## 📋 Быстрый старт

### Шаг 1: Создайте аккаунт на Railway

1. Перейдите на https://railway.app
2. Нажмите **"Start a New Project"**
3. Войдите через GitHub (рекомендуется)

---

### Шаг 2: Создайте новый проект

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Выберите ваш репозиторий `loyalitybot`
4. Railway автоматически определит Python проект

---

### Шаг 3: Настройте переменные окружения

В настройках проекта Railway → **Variables** добавьте:

**Обязательные:**
```env
SUPABASE_URL=ваш_supabase_url
SUPABASE_KEY=ваш_supabase_key
SENTRY_DSN=ваш_sentry_dsn
SENTRY_ENVIRONMENT=production
```

**Для Telegram алертов:**
```env
SENTRY_ALERT_TELEGRAM_TOKEN=ваш_telegram_bot_token
SENTRY_ALERT_CHAT_ID=ваш_chat_id
SENTRY_WEBHOOK_SECRET=ваш_webhook_secret
```

**Опциональные:**
```env
APP_VERSION=1.0.0
LOG_LEVEL=INFO
```

---

### Шаг 4: Настройте деплой

Railway автоматически:
- Определит Python проект
- Установит зависимости из `requirements.txt`
- Запустит команду из `Procfile`

Если нужно настроить вручную:
- **Build Command**: (оставьте пустым, Railway определит автоматически)
- **Start Command**: `uvicorn secure_api:app --host 0.0.0.0 --port $PORT`

---

### Шаг 5: Получите публичный URL

После деплоя Railway создаст публичный URL:
```
https://your-app-name.up.railway.app
```

Ваш webhook URL будет:
```
https://your-app-name.up.railway.app/api/sentry-webhook
```

---

### Шаг 6: Настройте Sentry Webhook

1. Откройте Sentry → ваш проект → Alerts → Create Alert Rule
2. В блоке **THEN** → **Add action** → выберите **Webhook**
3. Укажите URL:
   ```
   https://your-app-name.up.railway.app/api/sentry-webhook
   ```
4. Сохраните

---

## 🔧 Альтернатива: Deploy через CLI

### Установка Railway CLI

```bash
brew install railway
```

### Деплой

```bash
# Войдите в Railway
railway login

# Инициализируйте проект
railway init

# Добавьте переменные окружения
railway variables set SUPABASE_URL=ваш_url
railway variables set SUPABASE_KEY=ваш_key
# ... и т.д.

# Задеплойте
railway up
```

---

## 📊 Проверка работы

### 1. Health Check

```bash
curl https://your-app-name.up.railway.app/health
```

Должен вернуть: `{"status":"ok"}`

### 2. Swagger UI

Откройте в браузере:
```
https://your-app-name.up.railway.app/docs
```

### 3. Тест Sentry

```bash
curl https://your-app-name.up.railway.app/sentry-debug
```

Проверьте Sentry Dashboard - должна появиться ошибка.

---

## 🔐 Безопасность

### Рекомендации:

1. **Используйте Railway Private Networking** для внутренних сервисов
2. **Настройте Rate Limiting** (уже настроено в коде)
3. **Используйте HTTPS** (Railway предоставляет автоматически)
4. **Не коммитьте .env** в Git

---

## 💰 Стоимость

Railway предоставляет:
- **$5 бесплатного кредита** каждый месяц
- Для небольшого API этого достаточно
- Платные планы от $5/месяц

---

## 🐛 Troubleshooting

### Ошибка при деплое

1. Проверьте логи в Railway Dashboard
2. Убедитесь что все переменные окружения установлены
3. Проверьте что `requirements.txt` содержит все зависимости

### API не отвечает

1. Проверьте что сервис запущен (Railway Dashboard)
2. Проверьте логи на ошибки
3. Убедитесь что порт правильный (`$PORT`)

### Webhook не работает

1. Проверьте что URL правильный и доступен
2. Проверьте логи Railway на наличие запросов от Sentry
3. Убедитесь что `SENTRY_WEBHOOK_SECRET` совпадает (если используется)

---

## 📚 Дополнительные ресурсы

- [Railway Documentation](https://docs.railway.app/)
- [Railway Pricing](https://railway.app/pricing)
- [FastAPI Deployment](https://fastapi.tiangolo.com/deployment/)

---

*После деплоя у вас будет постоянный публичный URL для webhook!*


