# 🔍 Шаги для диагностики проблемы с иконками

## Проблема по скриншоту:
Иконки отображаются как изображения/эмодзи, а не как SVG.

## Что нужно проверить в базе данных:

### 1. Проверить что в таблице services:
```sql
SELECT 
  id,
  title,
  description
FROM services
WHERE approval_status = 'Approved' 
  AND is_active = true
LIMIT 10;
```

**Вопросы:**
- Какие названия услуг в поле `title`?
- Есть ли эмодзи в названиях?

### 2. Проверить есть ли поле `icon` в таблице:
```sql
SELECT column_name 
FROM information_schema.columns 
WHERE table_name = 'services' 
  AND column_name = 'icon';
```

### 3. Если поле `icon` существует, проверить что в нем:
```sql
SELECT 
  title,
  icon,
  CASE 
    WHEN icon ~ '[^\x00-\x7F]' THEN 'Contains emoji'
    ELSE 'Text only'
  END as icon_type
FROM services
WHERE approval_status = 'Approved' 
  AND is_active = true
LIMIT 10;
```

## Возможные причины:

1. **Кэш Telegram WebView** - старый код закэширован
   - Решение: перезапустить приложение в Telegram

2. **В базе есть поле `icon` с эмодзи** - мы его не учитываем
   - Решение: игнорировать это поле полностью

3. **Названия услуг не распознаются** - `getServiceIcon()` возвращает 'default'
   - Решение: расширить функцию поиска

4. **CSS перекрывает SVG** - какой-то стиль скрывает иконки
   - Решение: проверить стили

## Что сделано:

1. ✅ Добавлен `style={{ fontSize: 0 }}` для скрытия эмодзи
2. ✅ Добавлен `style={{ display: 'block' }}` для SVG
3. ✅ Исправлена логика для `defaultServiceIcons`

## Следующие шаги:

После получения данных из БД:
- Если названия услуг нестандартные → расширить `getServiceIcon()`
- Если есть поле `icon` с эмодзи → игнорировать его полностью
- Если проблема в кэше → инструкция по очистке кэша Telegram

