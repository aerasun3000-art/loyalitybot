# ✅ Финальные шаги после деплоя

## 📋 Что проверить

### 1. Start Command правильный?

В Render → Settings → проверьте **Start Command**:
```
uvicorn secure_api:app --host 0.0.0.0 --port $PORT
```

Если там `gunicorn` - измените на `uvicorn`!

---

### 2. Дождитесь завершения деплоя

В Render Dashboard проверьте статус:
- ✅ **"Live"** - сервис работает
- ✅ **"Deployed"** - деплой завершён
- ⏳ **"In Progress"** - ещё деплоится (подождите)

---

### 3. Получите публичный URL

После успешного деплоя Render покажет:

**Ваш URL:**
```
https://loyalitybot.onrender.com
```

**Webhook URL для Sentry:**
```
https://loyalitybot.onrender.com/api/sentry-webhook
```

---

### 4. Проверьте работу API

```bash
# Health check
curl https://loyalitybot.onrender.com/health

# Должен вернуть: {"status":"ok"}
```

**Swagger UI:**
```
https://loyalitybot.onrender.com/docs
```

---

### 5. Настройте Sentry Webhook

1. Вернитесь в **Sentry** → ваш проект → **Alerts**
2. Откройте созданное **Alert Rule**
3. В блоке **THEN** найдите действие с webhook
4. Укажите URL:
   ```
   https://loyalitybot.onrender.com/api/sentry-webhook
   ```
5. Сохраните

---

### 6. Протестируйте

```bash
# Вызовите тестовую ошибку
curl https://loyalitybot.onrender.com/sentry-debug

# Проверьте:
# 1. Sentry Dashboard → Issues (должна появиться ошибка)
# 2. Telegram (должно прийти уведомление)
```

---

## 🎉 Готово!

После этих шагов:
- ✅ API работает на публичном URL
- ✅ Webhook настроен в Sentry
- ✅ Ошибки автоматически отправляются в Telegram

---

## 🐛 Если что-то не работает

### API не отвечает

1. Проверьте логи в Render Dashboard → **Logs**
2. Убедитесь что Start Command правильный
3. Проверьте что все переменные окружения добавлены

### Webhook не работает

1. Проверьте что URL правильный и доступен
2. Проверьте логи Render на запросы от Sentry
3. Убедитесь что `SENTRY_WEBHOOK_SECRET` совпадает (если используется)

---

*После проверки всех шагов всё должно работать!*


