# 🎨 Механизм смены фонов из админ-бота

## Что было реализовано:

1. **Таблица настроек в Supabase** (`app_settings`)
   - Хранит настройки приложения, включая путь к фоновому изображению
   
2. **Методы в SupabaseManager**
   - `get_app_setting()` - получить настройку
   - `set_app_setting()` - установить настройку
   - `get_background_image()` - получить текущий фон

3. **Команда в админ-боте**
   - Кнопка "🎨 Смена Фона" в главном меню
   - Меню выбора из доступных фонов
   - Автоматическое сохранение выбранного фона

4. **Обновление фронтенда**
   - Загрузка фона из базы данных при монтировании компонента
   - Функция `getBackgroundImage()` в `supabase.js`

## 📋 Шаги для запуска:

### 1. Создать таблицу в Supabase

Выполните SQL-скрипт `supabase_app_settings.sql` в Supabase SQL Editor:

```sql
-- Таблица для хранения настроек приложения
CREATE TABLE IF NOT EXISTS app_settings (
    id SERIAL PRIMARY KEY,
    setting_key TEXT UNIQUE NOT NULL,
    setting_value TEXT NOT NULL,
    description TEXT,
    updated_at TIMESTAMP DEFAULT NOW(),
    updated_by TEXT
);

-- Вставляем дефолтное значение для фона
INSERT INTO app_settings (setting_key, setting_value, description, updated_by)
VALUES ('background_image', '/bg/sakura.jpg', 'Путь к фоновому изображению главной страницы', 'system')
ON CONFLICT (setting_key) DO NOTHING;

-- Создаем индекс для быстрого поиска
CREATE INDEX IF NOT EXISTS idx_app_settings_key ON app_settings(setting_key);
```

### 2. Настроить права доступа в Supabase

Убедитесь, что таблица `app_settings` доступна для чтения через anon key:
- В Supabase Dashboard → Authentication → Policies
- Создайте политику для SELECT (чтение) для всех пользователей
- Создайте политику для UPDATE (обновление) только для сервисной роли (используется в админ-боте)

### 3. Перезапустить админ-бот

```bash
# Остановить бота (если запущен)
# Затем запустить заново
python admin_bot.py
```

### 4. Использование

1. Откройте админ-бот в Telegram
2. Нажмите `/start` или `/admin`
3. Выберите "🎨 Смена Фона"
4. Выберите нужный фон из списка
5. Фон автоматически обновится на главной странице

## 🎯 Доступные фоны:

- 🌸 Сакура (по умолчанию): `/bg/sakura.jpg`
- 🎨 Фон 2: `/bg/fon2_files/02e59953309fdb690b5421c190a7524f.jpg`
- 🎨 Фон 3: `/bg/fon3_files/e6e8a21b0775730d94fac0aeeeb0b03f.jpg`
- 🎨 Фон 6: `/bg/fon6_files/2c793e92fdcc7213bbd46848a72f59aa.jpg`

## 📝 Добавление новых фонов:

Чтобы добавить новый фон в меню выбора:

1. Положите изображение в папку `frontend/public/bg/`
2. Откройте `admin_bot.py`
3. Найдите список `backgrounds` в функции `show_background_menu()`
4. Добавьте новую строку: `("🎨 Название", "/bg/путь/к/файлу.jpg")`

## ⚠️ Важно:

- Фоновые изображения должны находиться в папке `frontend/public/bg/`
- Путь должен начинаться с `/bg/`
- После изменения фона в админ-боте, пользователям нужно обновить страницу, чтобы увидеть новый фон

