# ✅ Быстрое исправление: Алексей и 3 мастера

## 📋 Данные

**Chat ID Алексея:** `406631153`

**3 мастера:**
1. `521221007` - Роман А. (Psychologist, PR, mentoring)
2. `6205824176` - Тота (Totally.nailed)
3. `726690151` - Вера Санжеева (Yoga for queens)

## 🔧 Что нужно сделать

### Вариант 1: Готовый скрипт (РЕКОМЕНДУЕТСЯ)

1. Откройте файл **`fix_alexey_3_masters_FINAL.sql`** в SQL Editor Supabase
2. Выполните все запросы по порядку (ШАГ 1, ШАГ 2, ШАГ 3)
3. Проверьте результат (раздел "ПРОВЕРКА РЕЗУЛЬТАТА")

### Вариант 2: Выполнить запросы вручную

#### 1. Установить referred_by_chat_id

```sql
UPDATE partners
SET referred_by_chat_id = '406631153'
WHERE chat_id IN (
    '521221007',
    '6205824176',
    '726690151'
);
```

#### 2. Создать записи в partner_network (ВАЖНО!)

```sql
INSERT INTO partner_network (referrer_chat_id, referred_chat_id, level, is_active)
VALUES 
    ('406631153', '521221007', 1, true),
    ('406631153', '6205824176', 1, true),
    ('406631153', '726690151', 1, true)
ON CONFLICT (referrer_chat_id, referred_chat_id) 
DO UPDATE SET level = 1, is_active = true;
```

#### 3. (Опционально) Установить тег 'master'

```sql
UPDATE partners
SET partner_type = 'master', partner_level = 3
WHERE chat_id IN ('521221007', '6205824176', '726690151');
```

## ✅ Проверка результата

После выполнения запросов выполните:

```sql
SELECT 
    COUNT(*) as masters_linked,
    COUNT(CASE WHEN pn.id IS NOT NULL THEN 1 END) as in_network
FROM partners p
LEFT JOIN partner_network pn ON (
    pn.referred_chat_id = p.chat_id 
    AND pn.referrer_chat_id = '406631153'
)
WHERE p.referred_by_chat_id = '406631153';
```

**Ожидаемый результат:**
- `masters_linked = 3`
- `in_network = 3`

## ⚠️ Важно

1. **ШАГ 2 критично важен** - без записей в `partner_network` Revenue Share не будет работать
2. Можно выполнять несколько раз - `ON CONFLICT` предотвратит дубликаты
3. Если что-то пошло не так, выполните проверку из скрипта

