# Настройка переменных окружения для Cloudflare Pages

**Проект:** `loyalitybot-frontend`  
**URL:** https://loyalitybot-frontend.pages.dev

---

## ⚠️ ВАЖНО: Переменные окружения для Vite

В Cloudflare Pages переменные окружения для Vite должны быть настроены через **Dashboard**, а не через CLI.

Это необходимо, потому что:
- Vite использует переменные `VITE_*` **во время сборки** (build time)
- Секреты в Cloudflare доступны только во время выполнения (runtime)
- Для автоматических билдов через Git нужны переменные окружения

---

## 📋 Инструкция по настройке

### Шаг 1: Откройте Dashboard

Перейдите по ссылке:
```
https://dash.cloudflare.com/1e573db7d6be24be4e467cc02a9b8524/pages/view/loyalitybot-frontend/settings/configuration
```

Или:
1. Откройте https://dash.cloudflare.com
2. Выберите аккаунт `aerasun3000@gmail.com`
3. Перейдите в **Pages** → **loyalitybot-frontend**
4. Перейдите в **Settings** → **Configuration**

### Шаг 2: Добавьте переменные окружения

В разделе **"Environment variables"** добавьте следующие переменные:

#### Production environment:

| Key | Value |
|-----|-------|
| `VITE_SUPABASE_URL` | `https://gynpvfchojnyoirosysj.supabase.co` |
| `VITE_SUPABASE_ANON_KEY` | `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Imd5bnB2ZmNob2pueW9pcm9zeXNqIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjA2NTExMzksImV4cCI6MjA3NjIyNzEzOX0.Lw-DG19z7ZNoiu1k0jLO8A7SmylhHPfA596qg0a88qk` |
| `VITE_API_URL` | `https://loyalitybot-api.aerasun3000.workers.dev` |
| `VITE_APP_NAME` | `LoyalityBot` |
| `VITE_APP_VERSION` | `0.1.0` |

#### Preview environment (опционально):

Те же самые переменные, но можно использовать другие значения для тестирования.

---

## 🔄 После настройки переменных

### Вариант 1: Передеплой через CLI

```bash
cd frontend
npm run build
wrangler pages deploy dist --project-name=loyalitybot-frontend --commit-dirty=true --commit-message="Update with environment variables"
```

### Вариант 2: Использовать Git интеграцию (рекомендуется)

1. В Dashboard → **Settings** → **Builds & deployments**
2. Подключите GitHub репозиторий
3. Настройте:
   - **Root directory:** `frontend`
   - **Build command:** `npm run build`
   - **Build output directory:** `dist`
   - **Branch for production:** `main`

После этого каждый push в `main` будет автоматически деплоить фронтенд.

---

## ✅ Текущий статус

- ✅ Проект создан: `loyalitybot-frontend`
- ✅ Первый деплой выполнен
- ✅ Production URL: https://loyalitybot-frontend.pages.dev
- ✅ FRONTEND_URL обновлен в клиентском боте
- ⚠️ Переменные окружения нужно настроить через Dashboard (см. выше)

---

## 🔍 Проверка работы

После настройки переменных и передеплоя:

1. Откройте https://loyalitybot-frontend.pages.dev
2. Проверьте, что приложение загружается
3. Проверьте в консоли браузера, что нет ошибок с переменными окружения
4. Протестируйте через Telegram бота:
   - Откройте клиентского бота
   - Нажмите `/start`
   - Нажмите "🚀 Открыть приложение"
   - Должно открыться приложение

---

## 📝 Примечания

- Текущий деплой собран с переменными из локального `.env`, поэтому он должен работать
- Для будущих деплоев (особенно через Git) переменные должны быть в Dashboard
- Секреты (через `wrangler pages secret put`) доступны только во время выполнения (runtime), не во время сборки (build time)

---

**Дата создания:** 2026-01-18  
**Последнее обновление:** 2026-01-18
