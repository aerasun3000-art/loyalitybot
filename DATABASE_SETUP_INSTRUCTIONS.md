# 🔧 Инструкция по настройке базы данных для формы партнерства

## ⚠️ Проблема
При регистрации партнера возникает ошибка из-за отсутствия или неправильной настройки таблицы `partner_applications` в базе данных Supabase.

## ✅ Решение

### Шаг 1: Откройте SQL Editor в Supabase

1. Перейдите на https://supabase.com
2. Войдите в свой проект
3. Откройте **SQL Editor** (в левом меню)

### Шаг 2: Выполните скрипт для таблицы partner_applications

Скопируйте и выполните содержимое файла `setup_partner_applications.sql`:

```sql
-- Скопируйте весь файл setup_partner_applications.sql
-- и выполните в SQL Editor
```

**Или выполните команду:**
```bash
# Просмотрите содержимое файла
cat setup_partner_applications.sql
```

### Шаг 3: Выполните скрипт для таблицы partners (опционально)

Если нужно обновить таблицу `partners`, выполните `setup_partners_table.sql`.

### Шаг 4: Проверьте результат

После выполнения скриптов:

1. Перейдите в **Table Editor** в Supabase
2. Найдите таблицу `partner_applications`
3. Убедитесь, что у неё есть следующие поля:
   - `chat_id` (TEXT, PRIMARY KEY)
   - `name` (TEXT)
   - `phone` (TEXT)
   - `company_name` (TEXT)
   - `city` (TEXT)
   - `district` (TEXT)
   - `status` (TEXT, DEFAULT 'Pending')
   - `created_at` (TIMESTAMP)

### Шаг 5: Проверьте RLS Policies

1. Перейдите в **Authentication** → **Policies**
2. Найдите таблицу `partner_applications`
3. Убедитесь, что есть следующие policies:
   - **Allow public insert** - для создания заявок
   - **Allow public select** - для чтения заявок

## 🧪 Тестирование

После настройки базы данных:

1. Откройте форму регистрации партнера
2. Заполните все поля:
   - Имя
   - Телефон
   - Название компании
   - Город (выберите из списка: Online, New York, Los Angeles, etc.)
   - Район (автоматически установится "All")
3. Нажмите "Отправить"
4. Проверьте, что заявка создалась в таблице `partner_applications`

## 🔍 Если ошибка сохраняется

### Проверьте консоль браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку **Console**
3. Попробуйте отправить форму
4. Посмотрите на ошибку в консоли

### Возможные ошибки:

1. **"relation partner_applications does not exist"**
   - Таблица не создана → выполните `setup_partner_applications.sql`

2. **"column city does not exist"**
   - Поле не добавлено → выполните ALTER TABLE из скрипта

3. **"new row violates row-level security policy"**
   - RLS блокирует вставку → проверьте policies, выполните скрипт настройки RLS

4. **"permission denied for table partner_applications"**
   - Проблема с правами доступа → проверьте RLS policies

## 📝 Файлы для выполнения

1. `setup_partner_applications.sql` - основная настройка таблицы заявок
2. `setup_partners_table.sql` - обновление таблицы партнеров (опционально)

## ✅ После выполнения

После успешного выполнения скриптов форма регистрации партнера должна работать без ошибок!









