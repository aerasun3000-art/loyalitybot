# ✅ Чеклист для бета-запуска системы промоутеров, UGC и лидерборда

## 📋 Обязательные шаги перед запуском

### 1. ✅ База данных

- [x] Выполнена миграция `supabase_mlm_referral_system.sql`
- [x] Выполнена миграция `supabase_promoters_ugc_leaderboard.sql`
- [ ] Выполнена инициализация тестовых данных `init_beta_data.sql`

**Проверка:**
```sql
-- Проверить созданные таблицы
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN (
    'promoters', 'ugc_content', 'promo_materials',
    'leaderboard_periods', 'leaderboard_rankings',
    'leaderboard_metrics', 'prize_distributions',
    'referral_tree', 'referral_rewards'
)
ORDER BY table_name;
-- Должно вернуть 9 таблиц
```

### 2. 🤖 Запуск ботов

#### Клиентский бот
```bash
cd /Users/ghbi/Downloads/loyalitybot
source venv/bin/activate
nohup python3 client_handler.py > client_handler_output.log 2>&1 &
```

#### Партнёрский бот
```bash
cd /Users/ghbi/Downloads/loyalitybot
source venv/bin/activate
nohup python3 bot.py > bot_output.log 2>&1 &
```

#### Админский бот
```bash
cd /Users/ghbi/Downloads/loyalitybot
source venv/bin/activate
nohup python3 admin_bot.py > admin_bot_output.log 2>&1 &
```

**Проверка работы:**
```bash
ps aux | grep -E "(client_handler|bot\.py|admin_bot)" | grep python
```

### 3. ⚙️ Переменные окружения

Проверьте наличие всех необходимых переменных:

**Для клиентского бота:**
- `TOKEN_CLIENT`
- `SUPABASE_URL`
- `SUPABASE_KEY`
- `VITE_BOT_USERNAME` (для фронтенда)

**Для партнёрского бота:**
- `TOKEN_PARTNER`
- `SUPABASE_URL`
- `SUPABASE_KEY`

**Для админского бота:**
- `ADMIN_BOT_TOKEN`
- `ADMIN_CHAT_ID`
- `TOKEN_PARTNER` (для уведомлений)

**Для фронтенда:**
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_BOT_USERNAME`

### 4. 🗄️ Инициализация данных

Выполните SQL:
```sql
-- В Supabase SQL Editor
-- Выполните файл: init_beta_data.sql
```

Или вручную:

1. **Создать первый период лидерборда** (через админ-бот):
   - `/start` → "🏆 Лидерборд" → "➕ Создать период"

2. **Добавить промо-материалы** (через SQL или админ-интерфейс):
   ```sql
   -- См. init_beta_data.sql
   ```

### 5. 🧪 Тестовые сценарии

#### Сценарий 1: Создание промоутера через NPS
1. Клиент совершает покупку через партнёра
2. Партнёр начисляет баллы
3. Клиент получает запрос на оценку NPS
4. Клиент ставит оценку **10**
5. ✅ Клиент автоматически становится промоутером
6. ✅ Клиент получает промо-код

#### Сценарий 2: Добавление UGC контента
1. Промоутер использует `/promoter` → видит свой промо-код
2. Промоутер использует `/ugc_add <ссылка> <платформа>`
3. ✅ UGC контент отправлен на модерацию
4. Админ получает уведомление
5. Админ модерирует через `/start` → "📸 Модерация UGC"
6. Админ одобряет контент
7. ✅ Промоутеру начислено 100 баллов
8. ✅ Метрика добавлена в лидерборд

#### Сценарий 3: Реферальная программа
1. Клиент использует `/referral` → получает реферальную ссылку
2. Клиент делится QR-кодом реферальной ссылки
3. Друг сканирует и регистрируется
4. ✅ Клиент получает 100 баллов за регистрацию
5. ✅ Друг совершает покупку
6. ✅ Клиент получает 8% от баллов друга

#### Сценарий 4: Лидерборд
1. Админ создаёт период лидерборда
2. Клиенты зарабатывают баллы (рефералы + UGC)
3. Клиенты смотрят `/leaderboard`
4. ✅ Видят свой рейтинг и топ участников
5. В конце периода админ распределяет призы

## 📊 Мониторинг и проверка

### Проверка логов ботов

```bash
# Клиентский бот
tail -f client_handler_output.log

# Партнёрский бот
tail -f bot_output.log

# Админский бот
tail -f admin_bot_output.log
```

### Проверка работы основных функций

**1. Проверка создания промоутера:**
```sql
SELECT * FROM promoters ORDER BY joined_at DESC LIMIT 5;
```

**2. Проверка UGC контента:**
```sql
SELECT * FROM ugc_content ORDER BY submitted_at DESC LIMIT 10;
```

**3. Проверка лидерборда:**
```sql
SELECT * FROM leaderboard_periods WHERE status = 'active';
SELECT * FROM leaderboard_rankings ORDER BY total_score DESC LIMIT 10;
```

**4. Проверка рефералов:**
```sql
SELECT * FROM referral_tree ORDER BY registered_at DESC LIMIT 10;
SELECT * FROM referral_rewards ORDER BY created_at DESC LIMIT 10;
```

## 🔧 Известные ограничения

1. **QR-декодирование**: Может не работать на arm64 без установки `libzbar`
   - Решение: Партнёры могут вводить CLIENT_ID вручную
   - Или установить: `arch -arm64 brew install zbar`

2. **RLS политики**: Убедитесь, что политики настроены корректно для вашего случая использования

3. **Автоматическая активация периодов**: Функция `activate_upcoming_periods()` должна вызываться периодически (через cron или планировщик задач)

## 🚀 Готовность к запуску

### Минимальные требования

- [x] SQL миграции выполнены
- [x] Все боты запускаются без ошибок
- [ ] Первый период лидерборда создан
- [ ] Промо-материалы добавлены
- [ ] Переменные окружения настроены
- [ ] Логи проверены на ошибки

### Рекомендуемые действия

- [ ] Создать тестового промоутера
- [ ] Протестировать добавление UGC контента
- [ ] Протестировать модерацию через админ-бот
- [ ] Протестировать реферальную программу
- [ ] Протестировать лидерборд

## 📝 Следующие шаги после запуска

1. **Мониторинг**: Следить за логами и ошибками
2. **Сбор метрик**: Отслеживать количество промоутеров, UGC контента, рефералов
3. **Обратная связь**: Собирать отзывы от тестировщиков
4. **Итерации**: Вносить улучшения на основе отзывов

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи ботов
2. Проверьте логи Supabase (Postgres Logs)
3. Проверьте переменные окружения
4. Проверьте статус всех ботов

---

**Дата создания:** $(date)
**Версия:** Beta 1.0
**Статус:** 🟡 В разработке

