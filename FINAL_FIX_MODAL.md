# ✅ Финальное исправление проблемы с модальным окном

## ❌ Проблема

При нажатии "Написать специалисту" показывалось модальное окно "Открываю переписку со специалистом..." вместо открытия переписки.

## 🔍 Причина

В Telegram Web App iframe методы `tg.openTelegramLink()` и `tg.openLink()` могут показывать модальное окно вместо прямого открытия ссылки. Это ограничение Web App iframe.

## ✅ Решение

**Новый подход:** Закрываем Web App и открываем ссылку напрямую через `window.location.href`

### Логика:
1. Для Telegram ссылок (`t.me/`):
   - Закрываем Web App через `tg.close()`
   - Открываем ссылку напрямую через `window.location.href`
   - Это обходит ограничения iframe и открывает ссылку напрямую в Telegram

2. Для других ссылок:
   - Используем `tg.openLink()` (открывает в браузере)

## 📝 Изменения в коде

### `frontend/src/pages/Services.jsx`

**Было:**
```javascript
if (tg?.openTelegramLink) {
  tg.openTelegramLink(contactLink) // ❌ Показывает модальное окно
  return
}
```

**Стало:**
```javascript
if (contactLink.includes('t.me/')) {
  // Закрываем Web App и открываем ссылку напрямую
  if (tg?.close) {
    tg.close()
    setTimeout(() => {
      window.location.href = contactLink // ✅ Прямое открытие
    }, 100)
  } else {
    window.location.href = contactLink
  }
  return
}
```

## 🚀 Новый URL

**URL:** `https://frontend-lcilgto5a-alekseis-projects-3b6bffb8.vercel.app`

## ✅ Результат

- ✅ При нажатии "Написать специалисту" Web App закрывается
- ✅ Ссылка открывается напрямую в Telegram (не модальное окно)
- ✅ Пользователь попадает в переписку со специалистом
- ✅ Нет модальных окон, которые блокируют открытие

## 📋 Что проверить

1. Откройте бота в Telegram
2. Нажмите "🚀 Открыть приложение"
3. Выберите услугу
4. Нажмите "Написать специалисту"
5. **Web App должен закрыться, и должна открыться переписка со специалистом**

## 🎯 Преимущества решения

- **Обход ограничений iframe**: Закрываем Web App перед открытием ссылки
- **Прямое открытие**: Используем `window.location.href` для прямого перехода
- **Лучший UX**: Пользователь сразу попадает в переписку, без модальных окон

