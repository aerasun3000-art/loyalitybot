# 📋 Простая инструкция: Как проверить логи

## 🎯 Быстрая проверка

### Шаг 1: Откройте терминал

В Cursor или любом терминале выполните:

```bash
tail -f /tmp/client_bot.log
```

Эта команда будет показывать логи бота в реальном времени.

**Нажмите Ctrl+C для остановки.**

### Шаг 2: Откройте приложение в Telegram

1. Откройте бота `@mindbeaty` в Telegram
2. Нажмите "🚀 Открыть приложение"
3. Выберите услугу
4. **Нажмите "Написать специалисту"**

### Шаг 3: Смотрите логи

**В терминале должны появиться строки с:**
- `contact_`
- `contact_specialist`
- `handle_new_user_start`

**Если появляются:**
- ✅ Функция работает, бот получает запрос

**Если НЕ появляются:**
- ❌ Функция не вызывается или ссылка не открывается

## 🔍 Альтернативная проверка (без tail -f)

### Проверка последних логов:

```bash
tail -50 /tmp/client_bot.log
```

### Поиск упоминаний contact_:

```bash
grep "contact_" /tmp/client_bot.log
```

## 📝 Что должно быть в логах

### При успешном запросе:

```
INFO - Клиент {chat_id} запустил бота с текстом: /start contact_...
INFO - Клиент {chat_id} отправил сообщение специалисту {partner_chat_id}
INFO - Message saved: ID={message_id}
```

### При ошибке:

```
ERROR - Ошибка обработки запроса
ERROR - Специалист не найден в системе
```

## ✅ Текущий статус

- ✅ Бот запущен (1 экземпляр)
- ✅ Код содержит `mindbeaty`
- ✅ Фронтенд задеплоен
- ❓ Нужно проверить, вызывается ли функция

## 🎯 Что делать сейчас

1. **Откройте терминал**
2. **Выполните:** `tail -f /tmp/client_bot.log`
3. **В Telegram нажмите "Написать специалисту"**
4. **Сообщите, что появилось в логах**

