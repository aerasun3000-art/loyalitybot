# 👨‍💼 РУКОВОДСТВО ПО MLM КОМАНДАМ В АДМИН БОТЕ

**Дата:** Ноябрь 2025  
**Бот:** `admin_bot.py`

---

## 📋 ДОСТУПНЫЕ КОМАНДЫ

### 1. Меню MLM Revenue Share

**Доступ:** Главное меню → "💎 MLM Revenue Share"

**Функции:**
- 📊 MLM Статистика
- 💎 Установить PV
- ✅ Одобрить выплаты
- 🌐 Структура сети

---

### 2. Команда `/mlm_stats`

**Описание:** Показывает общую статистику MLM системы

**Использование:**
```
/mlm_stats
```

**Показывает:**
- Количество партнеров
- Активных Revenue Share
- Размер сети
- Средний PV
- Общая сумма выплат
- Распределение по уровням PV

---

### 3. Команда `/set_pv`

**Описание:** Устанавливает PV (Partner Value) для конкретного партнера

**Использование:**
```
/set_pv <partner_chat_id> <pv_percent>
```

**Примеры:**
```
/set_pv 123456789 8.5
/set_pv 987654321 10.0
```

**Параметры:**
- `partner_chat_id` - ID партнера в Telegram
- `pv_percent` - Процент PV (от 0 до 100)

**Примечание:** PV также можно установить для всей отрасли через базу данных (см. `PV_INDUSTRY_GUIDE.md`)

---

### 4. Команда `/mlm_partner`

**Описание:** Показывает реферальную сеть конкретного партнера

**Использование:**
```
/mlm_partner <partner_chat_id>
```

**Пример:**
```
/mlm_partner 123456789
```

**Показывает:**
- Количество партнеров на каждом уровне (1, 2, 3)
- Общее количество партнеров в сети

---

### 5. Одобрение выплат

**Доступ:** Меню MLM → "✅ Одобрить выплаты"

**Функция:** Автоматически одобряет все pending выплаты Revenue Share за текущий месяц

**Что делает:**
1. Находит все выплаты со статусом `pending` за текущий месяц
2. Переводит их в статус `approved`
3. Показывает количество одобренных выплат и общую сумму

**Примечание:** Для фактической выплаты нужно дополнительно обновить статус на `paid` в базе данных.

---

## 🎯 ПРИМЕРЫ ИСПОЛЬЗОВАНИЯ

### Пример 1: Проверка статистики

```
Админ: /mlm_stats

Бот: 📊 MLM СТАТИСТИКА
     👥 ПАРТНЕРЫ:
     ├─ Всего партнеров: 25
     ├─ Активных Revenue Share: 15
     ├─ Размер сети: 45 связей
     └─ Средний PV: 7.2%
     ...
```

### Пример 2: Установка PV

```
Админ: /set_pv 123456789 8.5

Бот: ✅ PV установлен для партнера 123456789: 8.5%
```

### Пример 3: Просмотр сети партнера

```
Админ: /mlm_partner 123456789

Бот: 🌐 СЕТЬ ПАРТНЕРА 123456789
     📊 СТАТИСТИКА:
     ├─ Уровень 1: 5 партнеров
     ├─ Уровень 2: 12 партнеров
     ├─ Уровень 3: 8 партнеров
     └─ Всего: 25 партнеров
```

---

## 🔧 РАСШИРЕННЫЕ ВОЗМОЖНОСТИ

### Установка PV для отрасли

Используйте SQL запрос в Supabase:

```sql
-- Установить PV 5% для всех партнеров отрасли "beauty"
UPDATE partners 
SET pv_percent = 5.0 
WHERE industry_type = 'beauty';
```

### Массовое одобрение выплат

Используйте SQL запрос:

```sql
-- Одобрить все pending выплаты за ноябрь 2025
UPDATE partner_revenue_share 
SET status = 'approved'
WHERE status = 'pending'
  AND period_start >= '2025-11-01'
  AND period_end <= '2025-11-30';
```

### Просмотр детальной статистики

Используйте SQL запросы:

```sql
-- Топ-10 партнеров по Revenue Share
SELECT 
    p.chat_id,
    p.name,
    p.personal_income_monthly,
    p.revenue_share_monthly,
    p.pv_percent
FROM partners p
WHERE p.is_revenue_share_active = true
ORDER BY p.revenue_share_monthly DESC
LIMIT 10;
```

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Права доступа:** Только администраторы могут использовать эти команды
2. **PV автоматическое обновление:** PV автоматически обновляется при изменении дохода партнера (см. `AUTO_PV_UPDATE_GUIDE.md`)
3. **Выплаты:** Одобрение выплат не означает фактическую выплату - это нужно делать отдельно
4. **Логирование:** Все действия логируются в `logs/admin_bot.log`

---

## 🆘 РЕШЕНИЕ ПРОБЛЕМ

### Команда не отвечает

1. Проверьте, что вы администратор: `echo $ADMIN_CHAT_ID`
2. Проверьте логи: `tail -f logs/admin_bot.log`
3. Перезапустите админ бот

### Ошибка "Revenue Share система временно недоступна"

1. Проверьте, что `partner_revenue_share.py` импортирован
2. Проверьте инициализацию `revenue_share` в `admin_bot.py`
3. Проверьте переменные окружения

### PV не обновляется

1. Проверьте, что триггер `trigger_auto_update_pv` создан в базе
2. Проверьте функцию `calculate_pv_by_income()` в базе
3. Запустите `python3 check_mlm_database.py`

---

## 📚 СВЯЗАННЫЕ ДОКУМЕНТЫ

- `NEXT_STEPS_MLM.md` - Следующие шаги по внедрению
- `PV_INDUSTRY_GUIDE.md` - Настройка PV по отраслям
- `AUTO_PV_UPDATE_GUIDE.md` - Автоматическое обновление PV
- `MLM_PARTNER_REVENUE_SHARE_GUIDE.md` - Общее руководство по MLM системе

---

**Дата создания:** Ноябрь 2025  
**Версия:** 1.0















