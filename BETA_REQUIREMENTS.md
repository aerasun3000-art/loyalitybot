# 📋 Требования для бета-версии всей системы

## 🎯 Обзор

Этот документ содержит полный список всех требований для запуска бета-версии системы LoyalityBot.

---

## 1. 🗄️ База данных (Supabase)

### 1.1. SQL Миграции

**Обязательные миграции (выполнить в указанном порядке):**

1. **Основные таблицы** (если ещё не созданы):
   - `users` - клиенты
   - `partners` - партнёры
   - `partner_applications` - заявки партнёров
   - `transactions` - транзакции
   - `nps_ratings` - оценки NPS
   - `promotions` - акции
   - `services` - услуги

2. **MLM Реферальная система:**
   ```sql
   -- Выполнить: supabase_mlm_referral_system_clean.sql
   -- Создаёт таблицы: referral_tree, referral_rewards
   ```

3. **Промоутеры, UGC и Лидерборд:**
   ```sql
   -- Выполнить: supabase_promoters_ugc_leaderboard.sql
   -- Создаёт таблицы: promoters, ugc_content, promo_materials,
   --                  leaderboard_periods, leaderboard_rankings,
   --                  leaderboard_metrics, prize_distributions
   ```

4. **Инициализация данных:**
   ```sql
   -- Выполнить: init_beta_data.sql
   -- Создаёт первый период лидерборда и промо-материалы
   ```

### 1.2. Проверка таблиц

После выполнения миграций проверьте наличие всех таблиц:

```sql
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN (
    'users', 'partners', 'transactions', 'nps_ratings',
    'promotions', 'services', 'partner_applications',
    'promoters', 'ugc_content', 'promo_materials',
    'leaderboard_periods', 'leaderboard_rankings',
    'leaderboard_metrics', 'prize_distributions',
    'referral_tree', 'referral_rewards'
)
ORDER BY table_name;
```

**Ожидаемый результат:** 16 таблиц

---

## 2. ⚙️ Переменные окружения

### 2.1. Backend (Python боты)

Создайте файл `.env` в корне проекта со следующими переменными:

**Обязательные:**
```env
# Telegram боты
TOKEN_CLIENT=your_client_bot_token
TOKEN_PARTNER=your_partner_bot_token
ADMIN_BOT_TOKEN=your_admin_bot_token
ADMIN_CHAT_ID=your_telegram_chat_id

# Supabase
SUPABASE_URL=https://xxxxx.supabase.co
SUPABASE_KEY=your_supabase_anon_key

# Бизнес-логика
WELCOME_BONUS_AMOUNT=100
```

**Опциональные:**
```env
# AI помощник (GigaChat)
GIGACHAT_API_KEY=

# Frontend URL
FRONTEND_URL=https://loyalitybot.vercel.app

# Мониторинг (Sentry)
SENTRY_DSN=https://...
SENTRY_ENVIRONMENT=production
SENTRY_ALERT_TELEGRAM_TOKEN=...
SENTRY_ALERT_CHAT_ID=...
SENTRY_WEBHOOK_SECRET=...

# Логирование
LOG_LEVEL=INFO
```

### 2.2. Frontend (Vercel/другой хостинг)

Переменные окружения для фронтенда:

```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
VITE_BOT_USERNAME=your_client_bot_username
```

---

## 3. 🤖 Telegram боты

### 3.1. Создание ботов

Создайте 3 бота через [@BotFather](https://t.me/BotFather):

1. **Клиентский бот** (`TOKEN_CLIENT`)
   - Для клиентов
   - Должен иметь кнопку Web App

2. **Партнёрский бот** (`TOKEN_PARTNER`)
   - Для партнёров
   - Начисление баллов, управление услугами

3. **Админский бот** (`ADMIN_BOT_TOKEN`)
   - Для администраторов
   - Модерация, управление системой

### 3.2. Настройка Web App

Для клиентского бота настройте Web App кнопку:

1. В @BotFather: `/mybots` → выберите клиентского бота
2. **Bot Settings** → **Menu Button**
3. Укажите URL фронтенда: `https://loyalitybot.vercel.app`

### 3.3. Получение Chat ID администратора

1. Напишите [@userinfobot](https://t.me/userinfobot)
2. Скопируйте ваш Chat ID
3. Добавьте в `.env` как `ADMIN_CHAT_ID`

---

## 4. 🚀 Запуск системы

### 4.1. Установка зависимостей

```bash
cd /Users/ghbi/Downloads/loyalitybot

# Активировать виртуальное окружение
source venv/bin/activate

# Установить зависимости (если ещё не установлены)
pip install -r requirements.txt
```

### 4.2. Запуск ботов

**Автоматический запуск (рекомендуется):**
```bash
./start_beta.sh
```

**Или вручную:**
```bash
# Клиентский бот
nohup python3 client_handler.py > client_handler_output.log 2>&1 &

# Партнёрский бот
nohup python3 bot.py > bot_output.log 2>&1 &

# Админский бот
nohup python3 admin_bot.py > admin_bot_output.log 2>&1 &
```

### 4.3. Проверка работы ботов

```bash
# Проверить запущенные процессы
ps aux | grep -E "(client_handler|bot\.py|admin_bot)" | grep python

# Просмотр логов
tail -f client_handler_output.log
tail -f bot_output.log
tail -f admin_bot_output.log
```

---

## 5. 🌐 Frontend

### 5.1. Деплой на Vercel

1. **Подключите репозиторий** к Vercel
2. **Настройте переменные окружения** в Vercel Dashboard
3. **Деплой** автоматически запустится при push в main

**Или вручную:**
```bash
cd frontend
npm install
npm run build
# Загрузите dist/ на Vercel
```

### 5.2. Проверка работы

- Откройте бота в Telegram
- Нажмите кнопку Web App
- Должен открыться фронтенд

---

## 6. 🧪 Тестирование

### 6.1. Автоматические тесты

```bash
# Запустить тесты бета-версии
python3 test_beta_flow.py
```

Тесты проверяют:
- ✅ Переменные окружения
- ✅ Подключение к базе данных
- ✅ Наличие всех таблиц
- ✅ Работу функций
- ✅ Активный период лидерборда
- ✅ Промо-материалы

### 6.2. Ручное тестирование

**Сценарий 1: Регистрация клиента**
1. Откройте клиентского бота
2. Используйте `/start` или реферальную ссылку
3. ✅ Клиент должен получить приветственный бонус

**Сценарий 2: Начисление баллов (партнёр)**
1. Откройте партнёрского бота
2. Используйте команду начисления баллов
3. ✅ Баллы должны начислиться клиенту

**Сценарий 3: NPS оценка**
1. После начисления баллов клиент получает запрос на NPS
2. Клиент ставит оценку 10
3. ✅ Клиент автоматически становится промоутером

**Сценарий 4: UGC контент**
1. Промоутер использует `/ugc_add <ссылка> <платформа>`
2. ✅ Контент отправляется на модерацию
3. Админ модерирует через админ-бота
4. ✅ После одобрения начисляются баллы

**Сценарий 5: Реферальная программа**
1. Клиент использует `/referral`
2. ✅ Получает реферальную ссылку и QR-код
3. Друг регистрируется по ссылке
4. ✅ Клиент получает бонус за регистрацию

**Сценарий 6: Лидерборд**
1. Клиент использует `/leaderboard`
2. ✅ Видит свой рейтинг и топ участников

---

## 7. 📊 Мониторинг

### 7.1. Проверка логов

```bash
# Клиентский бот
tail -f client_handler_output.log

# Партнёрский бот
tail -f bot_output.log

# Админский бот
tail -f admin_bot_output.log
```

### 7.2. Проверка базы данных

```sql
-- Количество пользователей
SELECT COUNT(*) FROM users;

-- Количество партнёров
SELECT COUNT(*) FROM partners;

-- Активные транзакции
SELECT COUNT(*) FROM transactions WHERE date_time > NOW() - INTERVAL '24 hours';

-- Промоутеры
SELECT COUNT(*) FROM promoters;

-- UGC контент
SELECT status, COUNT(*) FROM ugc_content GROUP BY status;

-- Активный период лидерборда
SELECT * FROM leaderboard_periods WHERE status = 'active';

-- Рефералы
SELECT COUNT(*) FROM referral_tree;
```

---

## 8. ✅ Чеклист готовности

### Обязательные шаги:

- [ ] SQL миграции выполнены (все 3 файла)
- [ ] Все таблицы созданы (16 таблиц)
- [ ] Переменные окружения настроены (`.env` файл)
- [ ] Все 3 бота созданы и токены получены
- [ ] Web App кнопка настроена для клиентского бота
- [ ] Frontend задеплоен и доступен
- [ ] Все боты запущены и работают
- [ ] Тесты пройдены (`test_beta_flow.py`)
- [ ] Первый период лидерборда создан
- [ ] Промо-материалы добавлены

### Рекомендуемые шаги:

- [ ] Создан тестовый промоутер
- [ ] Протестировано добавление UGC контента
- [ ] Протестирована модерация через админ-бот
- [ ] Протестирована реферальная программа
- [ ] Протестирован лидерборд
- [ ] Настроен мониторинг (Sentry)
- [ ] Настроены автоматические бэкапы БД

---

## 9. 🔧 Известные ограничения

1. **QR-декодирование**: Может не работать на arm64 без установки `libzbar`
   - Решение: Партнёры могут вводить CLIENT_ID вручную
   - Или установить: `arch -arm64 brew install zbar`

2. **RLS политики**: Убедитесь, что политики настроены корректно

3. **Автоматическая активация периодов**: Функция `activate_upcoming_periods()` должна вызываться периодически (через cron или планировщик задач)

---

## 10. 🆘 Поддержка

При возникновении проблем:

1. **Проверьте логи ботов** (см. раздел 7.1)
2. **Проверьте логи Supabase** (Postgres Logs в Dashboard)
3. **Проверьте переменные окружения** (все ли заполнены)
4. **Проверьте статус ботов** (`ps aux | grep python`)
5. **Запустите тесты** (`python3 test_beta_flow.py`)

---

## 11. 📝 Следующие шаги после запуска

1. **Мониторинг**: Следить за логами и ошибками
2. **Сбор метрик**: Отслеживать количество промоутеров, UGC контента, рефералов
3. **Обратная связь**: Собирать отзывы от тестировщиков
4. **Итерации**: Вносить улучшения на основе отзывов

---

## 📚 Полезные файлы

- `BETA_LAUNCH_CHECKLIST.md` - Детальный чеклист запуска
- `BETA_READY.md` - Статус готовности системы
- `start_beta.sh` - Скрипт автоматического запуска
- `test_beta_flow.py` - Автоматические тесты
- `init_beta_data.sql` - Инициализация данных
- `README.md` - Общая документация проекта

---

**Версия:** Beta 1.0  
**Дата создания:** 2025-01-XX  
**Статус:** 📋 Требования

